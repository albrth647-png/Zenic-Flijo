"""
Conector Pix Brazil — Pagos Instantaneos via Pix (BCB) con mTLS REAL.
=====================================================================

Pix exige mTLS con certificado cliente ICP-Brasil A1 (PFX).
Fix Fase 2B: validar currency BRL en create_cob / create_cobv.
Reemplaza HttpClient plano por MTLSHttpClient + cert_loader.

Funcionalidades:
- create_cob / get_cob (cobrança imediata)
- create_cobv (cobrança com vencimento)
- create_devolution / get_devolution (reembolsos)
- get_pix (consulta pagamento)

Conformidade:
- Bacen Resolução 1/2020, Manual de Padrões para Iniciação do Pix (v6.8)
- Bacen DOC 3020/2020 — mTLS obrigatório desde 14-out-2020
- DIC 01/2020 (Diretrizes), DAD 01/2020 (Padrões de Dados)
"""

from __future__ import annotations

import contextlib
import tempfile
from pathlib import Path
from typing import Any

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.crypto.cert_loader import CertBundle, load_pem, load_pfx
from src.sdk.crypto.mtls_client import MTLSHttpClient
from src.sdk.exceptions import ConnectorError
from src.sdk.http_client import HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)

# Pix só suporta BRL (Real brasileiro). Resolução Bacen 1/2020 art. 9.
PIX_CURRENCY = "BRL"


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁPixBrazilConnectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁPixBrazilConnectorǁ_get_creds__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPixBrazilConnectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPixBrazilConnectorǁ_auth_headers__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPixBrazilConnectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPixBrazilConnectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPixBrazilConnectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPixBrazilConnectorǁ_validate_currency__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPixBrazilConnectorǁ_create_cob__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPixBrazilConnectorǁ_get_cob__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPixBrazilConnectorǁ_create_cobv__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPixBrazilConnectorǁ_create_devolution__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPixBrazilConnectorǁ_get_devolution__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut: MutantDict = {}  # type: ignore


class PixBrazilConnector(BaseConnector):
    """Conector para Pix Brazil: cobranças e pagamentos instantâneos via BCB com mTLS REAL."""

    name = "pix_brazil"
    version = "2.0.0"
    description = "Cria e gestiona cobranças e pagamentos instantâneos via Pix do Brasil (mTLS ICP-Brasil)"
    category = "latam"
    icon = "zap"
    author = "Zenic-Flujo"

    # Hosts oficiais BCB Pix (homologação e produção).
    # Fix Foso 2 SF1: host anterior https://api.pix.gov.br/v2 não existe.
    # Referência: Manual de Segurança do Pix v2.0, seção 4.1 (BCB).
    API_BASE_HOMO = "https://api.hm-pix.bcb.gov.br/v2"
    API_BASE_PROD = "https://api-pix.bcb.gov.br/v2"

    @_mutmut_mutated(mutants_xǁPixBrazilConnectorǁ__init____mutmut)
    def __init__(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_orig(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_1(self, environment: str = "XXhomoXX", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_2(self, environment: str = "HOMO", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_3(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = None
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_4(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").upper()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_5(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment and "homo").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_6(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "XXhomoXX").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_7(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "HOMO").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_8(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_9(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("XXhomoXX", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_10(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("HOMO", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_11(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "XXprodXX"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_12(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "PROD"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_13(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = None
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_14(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "XXhomoXX"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_15(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "HOMO"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_16(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = None
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_17(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = None
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_18(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env != "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_19(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "XXprodXX" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_20(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "PROD" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_21(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = ""
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_22(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = ""
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_23(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = ""
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_24(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = ""
        self._access_token: str = ""
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_25(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = None
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_26(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = "XXXX"
        self._client_id: str = ""

    def xǁPixBrazilConnectorǁ__init____mutmut_27(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = None

    def xǁPixBrazilConnectorǁ__init____mutmut_28(self, environment: str = "homo", **kwargs: Any) -> None:
        super().__init__(**kwargs)
        env = (environment or "homo").lower()
        if env not in ("homo", "prod"):
            env = "homo"
        self._environment: str = env
        self._base_url: str = (
            self.API_BASE_PROD if env == "prod" else self.API_BASE_HOMO
        )
        self._mtls: MTLSHttpClient | None = None
        self._cert_bundle: CertBundle | None = None
        # Arquivos PEM temporários criados em connect() para passar ao MTLSHttpClient
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._access_token: str = ""
        self._client_id: str = "XXXX"

    # ── Helpers de credenciais ──────────────────────────────────────

    @_mutmut_mutated(mutants_xǁPixBrazilConnectorǁ_get_creds__mutmut)
    def _get_creds(self) -> dict[str, Any]:
        """Obtém credenciais do auth_provider ou kwargs (compatível mypy)."""
        if self._auth_provider is None:
            return {}
        getter = getattr(self._auth_provider, "get_credentials", None)
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers de credenciais ──────────────────────────────────────

    def xǁPixBrazilConnectorǁ_get_creds__mutmut_orig(self) -> dict[str, Any]:
        """Obtém credenciais do auth_provider ou kwargs (compatível mypy)."""
        if self._auth_provider is None:
            return {}
        getter = getattr(self._auth_provider, "get_credentials", None)
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers de credenciais ──────────────────────────────────────

    def xǁPixBrazilConnectorǁ_get_creds__mutmut_1(self) -> dict[str, Any]:
        """Obtém credenciais do auth_provider ou kwargs (compatível mypy)."""
        if self._auth_provider is not None:
            return {}
        getter = getattr(self._auth_provider, "get_credentials", None)
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers de credenciais ──────────────────────────────────────

    def xǁPixBrazilConnectorǁ_get_creds__mutmut_2(self) -> dict[str, Any]:
        """Obtém credenciais do auth_provider ou kwargs (compatível mypy)."""
        if self._auth_provider is None:
            return {}
        getter = None
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers de credenciais ──────────────────────────────────────

    def xǁPixBrazilConnectorǁ_get_creds__mutmut_3(self) -> dict[str, Any]:
        """Obtém credenciais do auth_provider ou kwargs (compatível mypy)."""
        if self._auth_provider is None:
            return {}
        getter = getattr(None, "get_credentials", None)
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers de credenciais ──────────────────────────────────────

    def xǁPixBrazilConnectorǁ_get_creds__mutmut_4(self) -> dict[str, Any]:
        """Obtém credenciais do auth_provider ou kwargs (compatível mypy)."""
        if self._auth_provider is None:
            return {}
        getter = getattr(self._auth_provider, None, None)
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers de credenciais ──────────────────────────────────────

    def xǁPixBrazilConnectorǁ_get_creds__mutmut_5(self) -> dict[str, Any]:
        """Obtém credenciais do auth_provider ou kwargs (compatível mypy)."""
        if self._auth_provider is None:
            return {}
        getter = getattr("get_credentials", None)
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers de credenciais ──────────────────────────────────────

    def xǁPixBrazilConnectorǁ_get_creds__mutmut_6(self) -> dict[str, Any]:
        """Obtém credenciais do auth_provider ou kwargs (compatível mypy)."""
        if self._auth_provider is None:
            return {}
        getter = getattr(self._auth_provider, None)
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers de credenciais ──────────────────────────────────────

    def xǁPixBrazilConnectorǁ_get_creds__mutmut_7(self) -> dict[str, Any]:
        """Obtém credenciais do auth_provider ou kwargs (compatível mypy)."""
        if self._auth_provider is None:
            return {}
        getter = getattr(self._auth_provider, "get_credentials", )
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers de credenciais ──────────────────────────────────────

    def xǁPixBrazilConnectorǁ_get_creds__mutmut_8(self) -> dict[str, Any]:
        """Obtém credenciais do auth_provider ou kwargs (compatível mypy)."""
        if self._auth_provider is None:
            return {}
        getter = getattr(self._auth_provider, "XXget_credentialsXX", None)
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers de credenciais ──────────────────────────────────────

    def xǁPixBrazilConnectorǁ_get_creds__mutmut_9(self) -> dict[str, Any]:
        """Obtém credenciais do auth_provider ou kwargs (compatível mypy)."""
        if self._auth_provider is None:
            return {}
        getter = getattr(self._auth_provider, "GET_CREDENTIALS", None)
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers de credenciais ──────────────────────────────────────

    def xǁPixBrazilConnectorǁ_get_creds__mutmut_10(self) -> dict[str, Any]:
        """Obtém credenciais do auth_provider ou kwargs (compatível mypy)."""
        if self._auth_provider is None:
            return {}
        getter = getattr(self._auth_provider, "get_credentials", None)
        if callable(None):
            return getter() or {}
        return {}

    # ── Helpers de credenciais ──────────────────────────────────────

    def xǁPixBrazilConnectorǁ_get_creds__mutmut_11(self) -> dict[str, Any]:
        """Obtém credenciais do auth_provider ou kwargs (compatível mypy)."""
        if self._auth_provider is None:
            return {}
        getter = getattr(self._auth_provider, "get_credentials", None)
        if callable(getter):
            return getter() and {}
        return {}

    @_mutmut_mutated(mutants_xǁPixBrazilConnectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_orig(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_1(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = None
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_2(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_3(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error(None)
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_4(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("XXPixBrazilConnector: credenciais não configuradasXX")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_5(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("pixbrazilconnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_6(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PIXBRAZILCONNECTOR: CREDENCIAIS NÃO CONFIGURADAS")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_7(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return True

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_8(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = None
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_9(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get(None, "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_10(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", None)
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_11(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_12(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", )
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_13(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("XXclient_idXX", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_14(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("CLIENT_ID", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_15(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "XXXX")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_16(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = None
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_17(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get(None, "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_18(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", None)
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_19(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_20(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", )
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_21(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("XXclient_secretXX", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_22(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("CLIENT_SECRET", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_23(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "XXXX")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_24(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = None
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_25(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") and ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_26(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") and creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_27(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get(None) or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_28(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("XXcert_pathXX") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_29(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("CERT_PATH") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_30(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get(None) or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_31(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("XXpfx_pathXX") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_32(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("PFX_PATH") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_33(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or "XXXX"
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_34(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = None
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_35(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get(None, "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_36(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", None)
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_37(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_38(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", )
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_39(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("XXkey_pathXX", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_40(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("KEY_PATH", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_41(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "XXXX")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_42(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = None

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_43(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") and ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_44(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") and creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_45(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get(None) or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_46(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("XXcert_passwordXX") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_47(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("CERT_PASSWORD") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_48(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get(None) or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_49(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("XXpfx_passwordXX") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_50(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("PFX_PASSWORD") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_51(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or "XXXX"

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_52(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path and not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_53(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_54(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_55(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error(None)
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_56(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("XXPixBrazilConnector: cert_path/client_id obrigatóriosXX")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_57(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("pixbrazilconnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_58(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PIXBRAZILCONNECTOR: CERT_PATH/CLIENT_ID OBRIGATÓRIOS")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_59(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return True

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_60(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith(None):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_61(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).upper().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_62(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(None).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_63(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith(("XX.pfxXX", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_64(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".PFX", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_65(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", "XX.p12XX")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_66(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".P12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_67(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_68(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error(None)
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_69(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("XXPixBrazilConnector: pfx_password obrigatório para .pfx/.p12XX")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_70(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("pixbrazilconnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_71(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PIXBRAZILCONNECTOR: PFX_PASSWORD OBRIGATÓRIO PARA .PFX/.P12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_72(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return True
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_73(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = None
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_74(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(None, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_75(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, None)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_76(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_77(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, )
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_78(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_79(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error(None)
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_80(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("XXPixBrazilConnector: key_path obrigatório para PEMXX")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_81(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("pixbrazilconnector: key_path obrigatório para pem")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_82(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PIXBRAZILCONNECTOR: KEY_PATH OBRIGATÓRIO PARA PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_83(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return True
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_84(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = None

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_85(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(None, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_86(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, None)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_87(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_88(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, )

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_89(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error(None)
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_90(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("XXPixBrazilConnector: certificado ICP-Brasil expiradoXX")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_91(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("pixbrazilconnector: certificado icp-brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_92(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PIXBRAZILCONNECTOR: CERTIFICADO ICP-BRASIL EXPIRADO")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_93(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return True

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_94(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = None  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_95(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(None)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_96(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=None, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_97(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=None).name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_98(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_99(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, ).name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_100(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=True, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_101(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix="XX.pemXX").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_102(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".PEM").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_103(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = None  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_104(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(None)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_105(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=None, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_106(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=None).name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_107(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_108(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, ).name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_109(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=True, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_110(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix="XX.keyXX").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_111(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".KEY").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_112(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(None)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_113(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(None)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_114(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = None
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_115(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=None,
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_116(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=None,
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_117(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=None,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_118(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=None,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_119(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_120(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_121(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_122(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_123(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(None),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_124(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(None),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_125(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=31,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_126(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=False,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_127(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = None

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_128(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = None
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_129(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(None, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_130(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, None)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_131(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_132(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, )
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_133(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = None
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_134(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = None
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_135(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = False
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_136(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation(None, f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_137(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", None)
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_138(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation(f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_139(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", )
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_140(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("XXconnectXX", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_141(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("CONNECT", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_142(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = None
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_143(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = False
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_144(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation(None, "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_145(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", None)
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_146(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_147(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", )
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_148(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("XXconnectXX", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_149(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("CONNECT", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_150(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "XXmTLS configurado (OAuth2 fallback)XX")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_151(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mtls configurado (oauth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_152(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "MTLS CONFIGURADO (OAUTH2 FALLBACK)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_153(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return False

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_154(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(None)
            return False

    def xǁPixBrazilConnectorǁconnect__mutmut_155(self) -> bool:
        """Configura mTLS com certificado ICP-Brasil A1 e obtém OAuth2 token do BCB."""
        creds = self._get_creds()
        if not creds:
            logger.error("PixBrazilConnector: credenciais não configuradas")
            return False

        client_id = creds.get("client_id", "")
        client_secret = creds.get("client_secret", "")
        cert_path = creds.get("cert_path") or creds.get("pfx_path") or ""
        key_path = creds.get("key_path", "")
        cert_password = creds.get("cert_password") or creds.get("pfx_password") or ""

        if not cert_path or not client_id:
            logger.error("PixBrazilConnector: cert_path/client_id obrigatórios")
            return False

        try:
            # Carregar certificado via cert_loader (PFX ou PEM)
            if str(cert_path).lower().endswith((".pfx", ".p12")):
                if not cert_password:
                    logger.error("PixBrazilConnector: pfx_password obrigatório para .pfx/.p12")
                    return False
                self._cert_bundle = load_pfx(cert_path, cert_password)
            else:
                if not key_path:
                    logger.error("PixBrazilConnector: key_path obrigatório para PEM")
                    return False
                self._cert_bundle = load_pem(key_path, cert_path)

            if self._cert_bundle.is_expired:
                logger.error("PixBrazilConnector: certificado ICP-Brasil expirado")
                return False

            # Escrever PEM em arquivos temporários (requests precisa de paths)
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)

            # Inicializar MTLSHttpClient com cert cliente
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._client_id = client_id

            # Obter OAuth2 token (client_credentials) via mTLS
            token = self._fetch_oauth_token(client_id, client_secret)
            if token:
                self._access_token = token
                self._connected = True
                self._log_operation("connect", f"OAuth2 token obtido client_id={client_id}")
            else:
                # Sem token OAuth2, ainda configurado para chamadas diretas com mTLS
                self._connected = True
                self._log_operation("connect", "mTLS configurado (OAuth2 fallback)")
            return True

        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"PixBrazilConnector: erro de conexão - {e}")
            return True

    @_mutmut_mutated(mutants_xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut)
    def _fetch_oauth_token(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_orig(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_1(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is not None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_2(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return "XXXX"
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_3(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_4(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                None,
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_5(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data=None,
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_6(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers=None,
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_7(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_8(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_9(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_10(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "XXgrant_typeXX": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_11(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "GRANT_TYPE": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_12(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "XXclient_credentialsXX",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_13(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "CLIENT_CREDENTIALS",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_14(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "XXclient_idXX": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_15(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "CLIENT_ID": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_16(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "XXclient_secretXX": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_17(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "CLIENT_SECRET": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_18(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"XXContent-TypeXX": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_19(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"content-type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_20(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"CONTENT-TYPE": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_21(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "XXapplication/x-www-form-urlencodedXX"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_22(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "APPLICATION/X-WWW-FORM-URLENCODED"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_23(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = None
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_24(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() and {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_25(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get(None, "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_26(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", None)
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_27(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_28(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", )
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_29(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("XXaccess_tokenXX", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_30(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("ACCESS_TOKEN", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_31(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "XXXX")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_32(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(None)
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_33(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return "XXXX"
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_34(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(None)
            return ""

    def xǁPixBrazilConnectorǁ_fetch_oauth_token__mutmut_35(self, client_id: str, client_secret: str) -> str:
        """Obtém access_token OAuth2 client_credentials via mTLS."""
        if self._mtls is None:
            return ""
        try:
            resp = self._mtls.post(
                f"{self._base_url}/oauth/token",
                data={
                    "grant_type": "client_credentials",
                    "client_id": client_id,
                    "client_secret": client_secret,
                },
                headers={"Content-Type": "application/x-www-form-urlencoded"},
            )
            if resp.ok:
                data = resp.json() or {}
                return data.get("access_token", "")
            logger.warning(f"PixBrazilConnector: OAuth2 falhou HTTP {resp.status_code}")
            return ""
        except Exception as e:
            logger.warning(f"PixBrazilConnector: OAuth2 exception - {e}")
            return "XXXX"

    @_mutmut_mutated(mutants_xǁPixBrazilConnectorǁ_auth_headers__mutmut)
    def _auth_headers(self) -> dict[str, str]:
        """Headers com Bearer token OAuth2."""
        if self._access_token:
            return {"Authorization": f"Bearer {self._access_token}"}
        return {}

    def xǁPixBrazilConnectorǁ_auth_headers__mutmut_orig(self) -> dict[str, str]:
        """Headers com Bearer token OAuth2."""
        if self._access_token:
            return {"Authorization": f"Bearer {self._access_token}"}
        return {}

    def xǁPixBrazilConnectorǁ_auth_headers__mutmut_1(self) -> dict[str, str]:
        """Headers com Bearer token OAuth2."""
        if self._access_token:
            return {"XXAuthorizationXX": f"Bearer {self._access_token}"}
        return {}

    def xǁPixBrazilConnectorǁ_auth_headers__mutmut_2(self) -> dict[str, str]:
        """Headers com Bearer token OAuth2."""
        if self._access_token:
            return {"authorization": f"Bearer {self._access_token}"}
        return {}

    def xǁPixBrazilConnectorǁ_auth_headers__mutmut_3(self) -> dict[str, str]:
        """Headers com Bearer token OAuth2."""
        if self._access_token:
            return {"AUTHORIZATION": f"Bearer {self._access_token}"}
        return {}

    @_mutmut_mutated(mutants_xǁPixBrazilConnectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "XXcreate_cobXX": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "CREATE_COB": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "XXget_cobXX": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "GET_COB": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "XXcreate_cobvXX": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "CREATE_COBV": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "XXcreate_devolutionXX": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "CREATE_DEVOLUTION": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "XXget_devolutionXX": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "GET_DEVOLUTION": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "XXget_pixXX": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "GET_PIX": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = None
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(None)}
        return handler(params)

    def xǁPixBrazilConnectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            "create_cob": self._create_cob,
            "get_cob": self._get_cob,
            "create_cobv": self._create_cobv,
            "create_devolution": self._create_devolution,
            "get_devolution": self._get_devolution,
            "get_pix": self._get_pix,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Ação '{action}' não suportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁPixBrazilConnectorǁvalidate__mutmut)
    def validate(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("client_id") and (creds.get("cert_path") or creds.get("pfx_path")))

    def xǁPixBrazilConnectorǁvalidate__mutmut_orig(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("client_id") and (creds.get("cert_path") or creds.get("pfx_path")))

    def xǁPixBrazilConnectorǁvalidate__mutmut_1(self) -> bool:
        creds = None
        return bool(creds.get("client_id") and (creds.get("cert_path") or creds.get("pfx_path")))

    def xǁPixBrazilConnectorǁvalidate__mutmut_2(self) -> bool:
        creds = self._get_creds()
        return bool(None)

    def xǁPixBrazilConnectorǁvalidate__mutmut_3(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("client_id") or (creds.get("cert_path") or creds.get("pfx_path")))

    def xǁPixBrazilConnectorǁvalidate__mutmut_4(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get(None) and (creds.get("cert_path") or creds.get("pfx_path")))

    def xǁPixBrazilConnectorǁvalidate__mutmut_5(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("XXclient_idXX") and (creds.get("cert_path") or creds.get("pfx_path")))

    def xǁPixBrazilConnectorǁvalidate__mutmut_6(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("CLIENT_ID") and (creds.get("cert_path") or creds.get("pfx_path")))

    def xǁPixBrazilConnectorǁvalidate__mutmut_7(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("client_id") and (creds.get("cert_path") and creds.get("pfx_path")))

    def xǁPixBrazilConnectorǁvalidate__mutmut_8(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("client_id") and (creds.get(None) or creds.get("pfx_path")))

    def xǁPixBrazilConnectorǁvalidate__mutmut_9(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("client_id") and (creds.get("XXcert_pathXX") or creds.get("pfx_path")))

    def xǁPixBrazilConnectorǁvalidate__mutmut_10(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("client_id") and (creds.get("CERT_PATH") or creds.get("pfx_path")))

    def xǁPixBrazilConnectorǁvalidate__mutmut_11(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("client_id") and (creds.get("cert_path") or creds.get(None)))

    def xǁPixBrazilConnectorǁvalidate__mutmut_12(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("client_id") and (creds.get("cert_path") or creds.get("XXpfx_pathXX")))

    def xǁPixBrazilConnectorǁvalidate__mutmut_13(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("client_id") and (creds.get("cert_path") or creds.get("PFX_PATH")))

    @_mutmut_mutated(mutants_xǁPixBrazilConnectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        # Limpar arquivos PEM temporários
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁPixBrazilConnectorǁdisconnect__mutmut_orig(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        # Limpar arquivos PEM temporários
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁPixBrazilConnectorǁdisconnect__mutmut_1(self) -> bool:
        if self._mtls is None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        # Limpar arquivos PEM temporários
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁPixBrazilConnectorǁdisconnect__mutmut_2(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(None):
                self._mtls.close()
            self._mtls = None
        # Limpar arquivos PEM temporários
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁPixBrazilConnectorǁdisconnect__mutmut_3(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = ""
        # Limpar arquivos PEM temporários
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁPixBrazilConnectorǁdisconnect__mutmut_4(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        # Limpar arquivos PEM temporários
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None or f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁPixBrazilConnectorǁdisconnect__mutmut_5(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        # Limpar arquivos PEM temporários
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁPixBrazilConnectorǁdisconnect__mutmut_6(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        # Limpar arquivos PEM temporários
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(None):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁPixBrazilConnectorǁdisconnect__mutmut_7(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        # Limpar arquivos PEM temporários
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = ""
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁPixBrazilConnectorǁdisconnect__mutmut_8(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        # Limpar arquivos PEM temporários
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = ""
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁPixBrazilConnectorǁdisconnect__mutmut_9(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        # Limpar arquivos PEM temporários
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = None
        self._log_operation("disconnect")
        return True

    def xǁPixBrazilConnectorǁdisconnect__mutmut_10(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        # Limpar arquivos PEM temporários
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = True
        self._log_operation("disconnect")
        return True

    def xǁPixBrazilConnectorǁdisconnect__mutmut_11(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        # Limpar arquivos PEM temporários
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation(None)
        return True

    def xǁPixBrazilConnectorǁdisconnect__mutmut_12(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        # Limpar arquivos PEM temporários
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("XXdisconnectXX")
        return True

    def xǁPixBrazilConnectorǁdisconnect__mutmut_13(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        # Limpar arquivos PEM temporários
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("DISCONNECT")
        return True

    def xǁPixBrazilConnectorǁdisconnect__mutmut_14(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        # Limpar arquivos PEM temporários
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return False

    # ── Ações ──────────────────────────────────────────────────────

    @_mutmut_mutated(mutants_xǁPixBrazilConnectorǁ_validate_currency__mutmut)
    def _validate_currency(self, params: dict[str, Any]) -> str | None:
        """Valida que a moeda é BRL. Retorna mensagem de erro ou None."""
        currency = params.get("currency", PIX_CURRENCY)
        if currency and currency.upper() != PIX_CURRENCY:
            return (
                f"Moeda '{currency}' não suportada pelo Pix. "
                f"O Pix só aceita BRL (Real brasileiro) conforme Resolução Bacen 1/2020."
            )
        return None

    # ── Ações ──────────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_validate_currency__mutmut_orig(self, params: dict[str, Any]) -> str | None:
        """Valida que a moeda é BRL. Retorna mensagem de erro ou None."""
        currency = params.get("currency", PIX_CURRENCY)
        if currency and currency.upper() != PIX_CURRENCY:
            return (
                f"Moeda '{currency}' não suportada pelo Pix. "
                f"O Pix só aceita BRL (Real brasileiro) conforme Resolução Bacen 1/2020."
            )
        return None

    # ── Ações ──────────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_validate_currency__mutmut_1(self, params: dict[str, Any]) -> str | None:
        """Valida que a moeda é BRL. Retorna mensagem de erro ou None."""
        currency = None
        if currency and currency.upper() != PIX_CURRENCY:
            return (
                f"Moeda '{currency}' não suportada pelo Pix. "
                f"O Pix só aceita BRL (Real brasileiro) conforme Resolução Bacen 1/2020."
            )
        return None

    # ── Ações ──────────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_validate_currency__mutmut_2(self, params: dict[str, Any]) -> str | None:
        """Valida que a moeda é BRL. Retorna mensagem de erro ou None."""
        currency = params.get(None, PIX_CURRENCY)
        if currency and currency.upper() != PIX_CURRENCY:
            return (
                f"Moeda '{currency}' não suportada pelo Pix. "
                f"O Pix só aceita BRL (Real brasileiro) conforme Resolução Bacen 1/2020."
            )
        return None

    # ── Ações ──────────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_validate_currency__mutmut_3(self, params: dict[str, Any]) -> str | None:
        """Valida que a moeda é BRL. Retorna mensagem de erro ou None."""
        currency = params.get("currency", None)
        if currency and currency.upper() != PIX_CURRENCY:
            return (
                f"Moeda '{currency}' não suportada pelo Pix. "
                f"O Pix só aceita BRL (Real brasileiro) conforme Resolução Bacen 1/2020."
            )
        return None

    # ── Ações ──────────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_validate_currency__mutmut_4(self, params: dict[str, Any]) -> str | None:
        """Valida que a moeda é BRL. Retorna mensagem de erro ou None."""
        currency = params.get(PIX_CURRENCY)
        if currency and currency.upper() != PIX_CURRENCY:
            return (
                f"Moeda '{currency}' não suportada pelo Pix. "
                f"O Pix só aceita BRL (Real brasileiro) conforme Resolução Bacen 1/2020."
            )
        return None

    # ── Ações ──────────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_validate_currency__mutmut_5(self, params: dict[str, Any]) -> str | None:
        """Valida que a moeda é BRL. Retorna mensagem de erro ou None."""
        currency = params.get("currency", )
        if currency and currency.upper() != PIX_CURRENCY:
            return (
                f"Moeda '{currency}' não suportada pelo Pix. "
                f"O Pix só aceita BRL (Real brasileiro) conforme Resolução Bacen 1/2020."
            )
        return None

    # ── Ações ──────────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_validate_currency__mutmut_6(self, params: dict[str, Any]) -> str | None:
        """Valida que a moeda é BRL. Retorna mensagem de erro ou None."""
        currency = params.get("XXcurrencyXX", PIX_CURRENCY)
        if currency and currency.upper() != PIX_CURRENCY:
            return (
                f"Moeda '{currency}' não suportada pelo Pix. "
                f"O Pix só aceita BRL (Real brasileiro) conforme Resolução Bacen 1/2020."
            )
        return None

    # ── Ações ──────────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_validate_currency__mutmut_7(self, params: dict[str, Any]) -> str | None:
        """Valida que a moeda é BRL. Retorna mensagem de erro ou None."""
        currency = params.get("CURRENCY", PIX_CURRENCY)
        if currency and currency.upper() != PIX_CURRENCY:
            return (
                f"Moeda '{currency}' não suportada pelo Pix. "
                f"O Pix só aceita BRL (Real brasileiro) conforme Resolução Bacen 1/2020."
            )
        return None

    # ── Ações ──────────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_validate_currency__mutmut_8(self, params: dict[str, Any]) -> str | None:
        """Valida que a moeda é BRL. Retorna mensagem de erro ou None."""
        currency = params.get("currency", PIX_CURRENCY)
        if currency or currency.upper() != PIX_CURRENCY:
            return (
                f"Moeda '{currency}' não suportada pelo Pix. "
                f"O Pix só aceita BRL (Real brasileiro) conforme Resolução Bacen 1/2020."
            )
        return None

    # ── Ações ──────────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_validate_currency__mutmut_9(self, params: dict[str, Any]) -> str | None:
        """Valida que a moeda é BRL. Retorna mensagem de erro ou None."""
        currency = params.get("currency", PIX_CURRENCY)
        if currency and currency.lower() != PIX_CURRENCY:
            return (
                f"Moeda '{currency}' não suportada pelo Pix. "
                f"O Pix só aceita BRL (Real brasileiro) conforme Resolução Bacen 1/2020."
            )
        return None

    # ── Ações ──────────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_validate_currency__mutmut_10(self, params: dict[str, Any]) -> str | None:
        """Valida que a moeda é BRL. Retorna mensagem de erro ou None."""
        currency = params.get("currency", PIX_CURRENCY)
        if currency and currency.upper() == PIX_CURRENCY:
            return (
                f"Moeda '{currency}' não suportada pelo Pix. "
                f"O Pix só aceita BRL (Real brasileiro) conforme Resolução Bacen 1/2020."
            )
        return None

    @_mutmut_mutated(mutants_xǁPixBrazilConnectorǁ_create_cob__mutmut)
    def _create_cob(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = None
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(None)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"XXsuccessXX": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"SUCCESS": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": True, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "XXerrorXX": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "ERROR": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "XXreject_codeXX": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "REJECT_CODE": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "XXZF-PIX-CUR-001XX"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "zf-pix-cur-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = None
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get(None, "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", None)
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", )
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("XXtxidXX", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("TXID", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "XXXX")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = None
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get(None, {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", None)
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get({})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", )
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("XXvalorXX", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("VALOR", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid and not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"XXsuccessXX": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"SUCCESS": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": True, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "XXerrorXX": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "ERROR": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "XXParâmetros obrigatórios: txid, valorXX"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "PARÂMETROS OBRIGATÓRIOS: TXID, VALOR"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation(None, f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", None)

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation(f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", )

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("XXcreate_cobXX", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("CREATE_COB", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = None
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "XXcalendarioXX": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "CALENDARIO": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get(None, {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", None),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get({"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", ),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("XXcalendarioXX", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("CALENDARIO", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"XXexpiracaoXX": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"EXPIRACAO": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3601}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "XXvalorXX": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "VALOR": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "XXchaveXX": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "CHAVE": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get(None, ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", None),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get(""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("XXchaveXX", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("CHAVE", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", "XXXX"),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get(None):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("XXsolicitacaoPagadorXX"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaopagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("SOLICITACAOPAGADOR"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = None
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["XXsolicitacaoPagadorXX"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaopagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["SOLICITACAOPAGADOR"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["XXsolicitacaoPagadorXX"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaopagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["SOLICITACAOPAGADOR"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get(None):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("XXdevedorXX"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("DEVEDOR"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = None
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["XXdevedorXX"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["DEVEDOR"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["XXdevedorXX"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["DEVEDOR"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get(None):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("XXinfoAdicionaisXX"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoadicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("INFOADICIONAIS"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = None

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["XXinfoAdicionaisXX"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoadicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["INFOADICIONAIS"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["XXinfoAdicionaisXX"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoadicionais"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["INFOADICIONAIS"]

        return self._send_request("PUT", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request(None, f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", None, payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", None)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request(f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cob/{txid}", )

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("XXPUTXX", f"/cob/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cob__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança imediata via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cob", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {"expiracao": 3600}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("put", f"/cob/{txid}", payload)

    @_mutmut_mutated(mutants_xǁPixBrazilConnectorǁ_get_cob__mutmut)
    def _get_cob(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = None
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get(None, "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", None)
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", )
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("XXtxidXX", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("TXID", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "XXXX")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"XXsuccessXX": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"SUCCESS": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": True, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "XXerrorXX": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "ERROR": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "XXParâmetro obrigatório: txidXX"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "PARÂMETRO OBRIGATÓRIO: TXID"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation(None, f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", None)
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation(f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", )
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("XXget_cobXX", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("GET_COB", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = None
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get(None):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("XXrevisaoXX"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("REVISAO"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = None
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["XXrevisaoXX"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["REVISAO"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["XXrevisaoXX"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["REVISAO"]
        return self._send_request("GET", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request(None, f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", None, None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, params=None)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request(f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("GET", f"/cob/{txid}", None, )

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("XXGETXX", f"/cob/{txid}", None, params=query)

    def xǁPixBrazilConnectorǁ_get_cob__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma cobrança imediata por txid."""
        txid = params.get("txid", "")
        if not txid:
            return {"success": False, "error": "Parâmetro obrigatório: txid"}
        self._log_operation("get_cob", f"txid={txid}")
        query: dict[str, Any] = {}
        if params.get("revisao"):
            query["revisao"] = params["revisao"]
        return self._send_request("get", f"/cob/{txid}", None, params=query)

    @_mutmut_mutated(mutants_xǁPixBrazilConnectorǁ_create_cobv__mutmut)
    def _create_cobv(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = None
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(None)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"XXsuccessXX": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"SUCCESS": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": True, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "XXerrorXX": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "ERROR": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "XXreject_codeXX": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "REJECT_CODE": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "XXZF-PIX-CUR-001XX"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "zf-pix-cur-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = None
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get(None, "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", None)
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", )
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("XXtxidXX", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("TXID", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "XXXX")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = None
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get(None, {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", None)
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get({})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", )
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("XXvalorXX", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("VALOR", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid and not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"XXsuccessXX": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"SUCCESS": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": True, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "XXerrorXX": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "ERROR": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "XXParâmetros obrigatórios: txid, valorXX"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "PARÂMETROS OBRIGATÓRIOS: TXID, VALOR"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation(None, f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", None)

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation(f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", )

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("XXcreate_cobvXX", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("CREATE_COBV", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = None
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "XXcalendarioXX": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "CALENDARIO": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get(None, {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", None),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get({}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", ),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("XXcalendarioXX", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("CALENDARIO", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "XXvalorXX": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "VALOR": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "XXchaveXX": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "CHAVE": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get(None, ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", None),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get(""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("XXchaveXX", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("CHAVE", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", "XXXX"),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get(None):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("XXsolicitacaoPagadorXX"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaopagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("SOLICITACAOPAGADOR"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = None
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["XXsolicitacaoPagadorXX"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaopagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["SOLICITACAOPAGADOR"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["XXsolicitacaoPagadorXX"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaopagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["SOLICITACAOPAGADOR"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get(None):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("XXdevedorXX"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("DEVEDOR"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = None
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["XXdevedorXX"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["DEVEDOR"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["XXdevedorXX"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["DEVEDOR"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get(None):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("XXrecebedorXX"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("RECEBEDOR"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = None
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["XXrecebedorXX"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["RECEBEDOR"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["XXrecebedorXX"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["RECEBEDOR"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get(None):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("XXinfoAdicionaisXX"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoadicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("INFOADICIONAIS"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = None

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["XXinfoAdicionaisXX"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoadicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["INFOADICIONAIS"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["XXinfoAdicionaisXX"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoadicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["INFOADICIONAIS"]

        return self._send_request("PUT", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request(None, f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", None, payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", None)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request(f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("PUT", f"/cobv/{txid}", )

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("XXPUTXX", f"/cobv/{txid}", payload)

    def xǁPixBrazilConnectorǁ_create_cobv__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma cobrança com vencimento via Pix (somente BRL)."""
        err = self._validate_currency(params)
        if err:
            return {"success": False, "error": err, "reject_code": "ZF-PIX-CUR-001"}

        txid = params.get("txid", "")
        valor = params.get("valor", {})
        if not txid or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: txid, valor"}
        self._log_operation("create_cobv", f"txid={txid} currency=BRL")

        payload: dict[str, Any] = {
            "calendario": params.get("calendario", {}),
            "valor": valor,
            "chave": params.get("chave", ""),
        }
        if params.get("solicitacaoPagador"):
            payload["solicitacaoPagador"] = params["solicitacaoPagador"]
        if params.get("devedor"):
            payload["devedor"] = params["devedor"]
        if params.get("recebedor"):
            payload["recebedor"] = params["recebedor"]
        if params.get("infoAdicionais"):
            payload["infoAdicionais"] = params["infoAdicionais"]

        return self._send_request("put", f"/cobv/{txid}", payload)

    @_mutmut_mutated(mutants_xǁPixBrazilConnectorǁ_create_devolution__mutmut)
    def _create_devolution(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = None
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get(None, "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", None)
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", )
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("XXe2eidXX", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("E2EID", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "XXXX")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = None
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get(None, "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", None)
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", )
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("XXidXX", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("ID", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "XXXX")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = None
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get(None, "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", None)
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", )
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("XXvalorXX", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("VALOR", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "XXXX")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id and not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid and not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"XXsuccessXX": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"SUCCESS": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": True, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "XXerrorXX": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "ERROR": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "XXParâmetros obrigatórios: e2eid, id, valorXX"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "PARÂMETROS OBRIGATÓRIOS: E2EID, ID, VALOR"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation(None, f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", None)

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation(f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", )

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("XXcreate_devolutionXX", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("CREATE_DEVOLUTION", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = None
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"XXvalorXX": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"VALOR": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get(None):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("XXdescricaoXX"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("DESCRICAO"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = None
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["XXdescricaoXX"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["DESCRICAO"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["XXdescricaoXX"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["DESCRICAO"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request(None, f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", None, payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request(f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("PUT", f"/pix/{e2eid}/devolucao/{dev_id}", )

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("XXPUTXX", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    def xǁPixBrazilConnectorǁ_create_devolution__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Cria uma devolução (reembolso) de um pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        valor = params.get("valor", "")
        if not e2eid or not dev_id or not valor:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id, valor"}
        self._log_operation("create_devolution", f"e2eid={e2eid}")

        payload: dict[str, Any] = {"valor": valor}
        if params.get("descricao"):
            payload["descricao"] = params["descricao"]
        return self._send_request("put", f"/pix/{e2eid}/devolucao/{dev_id}", payload)

    @_mutmut_mutated(mutants_xǁPixBrazilConnectorǁ_get_devolution__mutmut)
    def _get_devolution(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = None
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get(None, "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", None)
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", )
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("XXe2eidXX", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("E2EID", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "XXXX")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = None
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get(None, "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", None)
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", )
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("XXidXX", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("ID", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "XXXX")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid and not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"XXsuccessXX": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"SUCCESS": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": True, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "XXerrorXX": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "ERROR": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "XXParâmetros obrigatórios: e2eid, idXX"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "PARÂMETROS OBRIGATÓRIOS: E2EID, ID"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation(None, f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", None)
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation(f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", )
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("XXget_devolutionXX", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("GET_DEVOLUTION", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request(None, f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", None, None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request(f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}/devolucao/{dev_id}", )

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("XXGETXX", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    def xǁPixBrazilConnectorǁ_get_devolution__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma devolução de pagamento Pix."""
        e2eid = params.get("e2eid", "")
        dev_id = params.get("id", "")
        if not e2eid or not dev_id:
            return {"success": False, "error": "Parâmetros obrigatórios: e2eid, id"}
        self._log_operation("get_devolution", f"e2eid={e2eid}")
        return self._send_request("get", f"/pix/{e2eid}/devolucao/{dev_id}", None)

    @_mutmut_mutated(mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut)
    def _get_pix(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = None
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get(None, "")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", None)
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", )
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("XXe2eidXX", "")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("E2EID", "")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "XXXX")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"XXsuccessXX": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"SUCCESS": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": True, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "XXerrorXX": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "ERROR": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "error": "XXParâmetro obrigatório: e2eidXX"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "error": "parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "error": "PARÂMETRO OBRIGATÓRIO: E2EID"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation(None, f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", None)
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation(f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", )
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("XXget_pixXX", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("GET_PIX", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request(None, f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", None, None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request(f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("GET", f"/pix/{e2eid}", )

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("XXGETXX", f"/pix/{e2eid}", None)

    def xǁPixBrazilConnectorǁ_get_pix__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta um pagamento Pix recebido por e2eid."""
        e2eid = params.get("e2eid", "")
        if not e2eid:
            return {"success": False, "error": "Parâmetro obrigatório: e2eid"}
        self._log_operation("get_pix", f"e2eid={e2eid}")
        return self._send_request("get", f"/pix/{e2eid}", None)

    # ── HTTP helper ────────────────────────────────────────────────

    @_mutmut_mutated(mutants_xǁPixBrazilConnectorǁ_send_request__mutmut)
    def _send_request(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_orig(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_1(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is not None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_2(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"XXsuccessXX": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_3(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"SUCCESS": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_4(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": True, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_5(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "XXerrorXX": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_6(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "ERROR": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_7(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "XXConector não conectado (mTLS não inicializado)XX"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_8(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "conector não conectado (mtls não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_9(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "CONECTOR NÃO CONECTADO (MTLS NÃO INICIALIZADO)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_10(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = None
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_11(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = None
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_12(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_13(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = None

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_14(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["XXContent-TypeXX"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_15(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["content-type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_16(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["CONTENT-TYPE"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_17(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "XXapplication/jsonXX"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_18(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "APPLICATION/JSON"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_19(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method != "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_20(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "XXGETXX":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_21(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "get":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_22(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = None
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_23(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(None, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_24(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=None, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_25(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=None)
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_26(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_27(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_28(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, )
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_29(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params and {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_30(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method != "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_31(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "XXPUTXX":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_32(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "put":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_33(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = None
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_34(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(None, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_35(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=None, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_36(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=None, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_37(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=None)
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_38(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_39(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_40(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_41(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, )
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_42(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params and {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_43(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = None

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_44(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(None, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_45(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=None, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_46(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=None, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_47(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=None)

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_48(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_49(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_50(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_51(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, )

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_52(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params and {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_53(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = None
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_54(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"XXsuccessXX": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_55(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"SUCCESS": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_56(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": False, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_57(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "XXdataXX": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_58(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "DATA": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_59(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = None
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_60(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = None
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_61(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"XXrawXX": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_62(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"RAW": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_63(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "XXsuccessXX": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_64(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "SUCCESS": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_65(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": True,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_66(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "XXerrorXX": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_67(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "ERROR": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_68(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "XXstatus_codeXX": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_69(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "STATUS_CODE": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_70(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_71(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_72(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_73(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_74(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_75(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_76(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_77(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_78(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Erro inesperado: {e}"}

    # ── HTTP helper ────────────────────────────────────────────────

    def xǁPixBrazilConnectorǁ_send_request__mutmut_79(
        self,
        method: str,
        path: str,
        payload: dict[str, Any] | None,
        params: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        """Envia request HTTP via mTLS com Bearer token."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado (mTLS não inicializado)"}

        url = f"{self._base_url}{path}"
        headers = self._auth_headers()
        if payload is not None:
            headers["Content-Type"] = "application/json"

        try:
            if method == "GET":
                resp = self._mtls.get(url, headers=headers, params=params or {})
            elif method == "PUT":
                resp = self._mtls.put(url, data=payload, headers=headers, params=params or {})
            else:
                resp = self._mtls.post(url, data=payload, headers=headers, params=params or {})

            if resp.ok:
                data = resp.json() if resp.content else {}
                return {"success": True, "data": data}
            try:
                err_body = resp.json() if resp.content else {}
            except Exception:
                err_body = {"raw": resp.text}
            return {
                "success": False,
                "error": f"HTTP {resp.status_code}: {err_body}",
                "status_code": resp.status_code,
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Erro inesperado: {e}"}

mutants_xǁPixBrazilConnectorǁ__init____mutmut['_mutmut_orig'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_1'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_2'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_3'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_4'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_5'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_6'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_7'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_8'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_8 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_9'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_9 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_10'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_10 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_11'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_11 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_12'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_12 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_13'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_13 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_14'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_14 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_15'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_15 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_16'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_16 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_17'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_17 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_18'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_18 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_19'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_19 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_20'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_20 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_21'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_21 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_22'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_22 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_23'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_23 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_24'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut_24 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ__init____mutmut['xǁPixBrazilConnectorǁ__init____mutmut_25'] = PixBrazilConnector.xǁPixBrazilConnectorǁ__init____mutmut
# type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_devolution__mutmut['xǁPixBrazilConnectorǁ_get_devolution__mutmut_28'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_devolution__mutmut_28 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_devolution__mutmut['xǁPixBrazilConnectorǁ_get_devolution__mutmut_29'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_devolution__mutmut_29 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_devolution__mutmut['xǁPixBrazilConnectorǁ_get_devolution__mutmut_30'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_devolution__mutmut_30 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_devolution__mutmut['xǁPixBrazilConnectorǁ_get_devolution__mutmut_31'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_devolution__mutmut_31 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_devolution__mutmut['xǁPixBrazilConnectorǁ_get_devolution__mutmut_32'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_devolution__mutmut_32 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_devolution__mutmut['xǁPixBrazilConnectorǁ_get_devolution__mutmut_33'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_devolution__mutmut_33 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_devolution__mutmut['xǁPixBrazilConnectorǁ_get_devolution__mutmut_34'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_devolution__mutmut_34 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_devolution__mutmut['xǁPixBrazilConnectorǁ_get_devolution__mutmut_35'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_devolution__mutmut_35 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_devolution__mutmut['xǁPixBrazilConnectorǁ_get_devolution__mutmut_36'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_devolution__mutmut_36 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_devolution__mutmut['xǁPixBrazilConnectorǁ_get_devolution__mutmut_37'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_devolution__mutmut_37 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_devolution__mutmut['xǁPixBrazilConnectorǁ_get_devolution__mutmut_38'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_devolution__mutmut_38 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_devolution__mutmut['xǁPixBrazilConnectorǁ_get_devolution__mutmut_39'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_devolution__mutmut_39 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_devolution__mutmut['xǁPixBrazilConnectorǁ_get_devolution__mutmut_40'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_devolution__mutmut_40 # type: ignore # mutmut generated

mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['_mutmut_orig'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_orig # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_1'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_1 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_2'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_2 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_3'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_3 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_4'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_4 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_5'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_5 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_6'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_6 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_7'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_7 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_8'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_8 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_9'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_9 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_10'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_10 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_11'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_11 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_12'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_12 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_13'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_13 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_14'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_14 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_15'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_15 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_16'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_16 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_17'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_17 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_18'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_18 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_19'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_19 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_20'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_20 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_21'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_21 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_22'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_22 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_23'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_23 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_24'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_24 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_25'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_25 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_26'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_26 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_27'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_27 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_28'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_28 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_29'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_29 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_get_pix__mutmut['xǁPixBrazilConnectorǁ_get_pix__mutmut_30'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_get_pix__mutmut_30 # type: ignore # mutmut generated

mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['_mutmut_orig'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_orig # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_1'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_1 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_2'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_2 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_3'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_3 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_4'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_4 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_5'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_5 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_6'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_6 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_7'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_7 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_8'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_8 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_9'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_9 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_10'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_10 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_11'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_11 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_12'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_12 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_13'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_13 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_14'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_14 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_15'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_15 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_16'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_16 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_17'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_17 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_18'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_18 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_19'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_19 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_20'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_20 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_21'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_21 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_22'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_22 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_23'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_23 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_24'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_24 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_25'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_25 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_26'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_26 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_27'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_27 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_28'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_28 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_29'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_29 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_30'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_30 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_31'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_31 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_32'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_32 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_33'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_33 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_34'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_34 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_35'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_35 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_36'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_36 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_37'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_37 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_38'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_38 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_39'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_39 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_40'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_40 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_41'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_41 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_42'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_42 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_43'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_43 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_44'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_44 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_45'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_45 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_46'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_46 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_47'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_47 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_48'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_48 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_49'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_49 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_50'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_50 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_51'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_51 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_52'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_52 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_53'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_53 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_54'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_54 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_55'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_55 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_56'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_56 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_57'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_57 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_58'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_58 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_59'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_59 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_60'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_60 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_61'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_61 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_62'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_62 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_63'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_63 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_64'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_64 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_65'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_65 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_66'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_66 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_67'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_67 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_68'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_68 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_69'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_69 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_70'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_70 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_71'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_71 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_72'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_72 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_73'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_73 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_74'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_74 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_75'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_75 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_76'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_76 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_77'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_77 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_78'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_78 # type: ignore # mutmut generated
mutants_xǁPixBrazilConnectorǁ_send_request__mutmut['xǁPixBrazilConnectorǁ_send_request__mutmut_79'] = PixBrazilConnector.xǁPixBrazilConnectorǁ_send_request__mutmut_79 # type: ignore # mutmut generated


PIX_BRAZIL_SCHEMA = ConnectorSchema(
    name="pix_brazil",
    version="2.0.0",
    description="Cria e gestiona cobranças e pagamentos instantâneos via Pix do Brasil (mTLS ICP-Brasil)",
    category="latam",
    icon="zap",
    author="Zenic-Flujo",
    actions=[
        ActionDefinition(name="create_cob", description="Cria cobrança imediata (BRL)", category="write"),
        ActionDefinition(name="get_cob", description="Consulta cobrança", category="read"),
        ActionDefinition(name="create_cobv", description="Cria cobrança com vencimento (BRL)", category="write"),
        ActionDefinition(name="create_devolution", description="Cria devolução", category="write"),
        ActionDefinition(name="get_devolution", description="Consulta devolução", category="read"),
        ActionDefinition(name="get_pix", description="Consulta pagamento Pix", category="read"),
    ],
    auth_requirements=[
        AuthRequirement(
            auth_type="mtls",
            required_fields=["client_id", "client_secret", "cert_path", "key_path"],
            description="Credenciais Pix BCB + Certificado ICP-Brasil A1 (mTLS obrigatório)",
        )
    ],
)
