"""
Conector Discord — Bot de Mensajes para Discord
==================================================

Permite enviar mensajes a canales de Discord, gestionar roles,
crear embeds y administrar servidores via la API de Discord Bot.
"""

from __future__ import annotations

from typing import Any

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.http_client import HttpClient, HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁDiscordConnectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁDiscordConnectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDiscordConnectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDiscordConnectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDiscordConnectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDiscordConnectorǁ_send_message__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDiscordConnectorǁ_send_embed__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDiscordConnectorǁ_list_channels__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDiscordConnectorǁ_add_role__mutmut: MutantDict = {}  # type: ignore
mutants_xǁDiscordConnectorǁ_remove_role__mutmut: MutantDict = {}  # type: ignore


class DiscordConnector(BaseConnector):
    """Conector para Discord: mensajes, roles y gestion de servidor."""

    name = "discord"
    version = "1.0.0"
    description = "Envia mensajes, gestiona roles y administra servidores de Discord"
    category = "communication"
    icon = "message-circle"
    author = "Zenic-Flijo"

    @_mutmut_mutated(mutants_xǁDiscordConnectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._bot_token: str = ""
        self._base_url: str = "https://discord.com/api/v10"
        self._http: HttpClient | None = None

    def xǁDiscordConnectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._bot_token: str = ""
        self._base_url: str = "https://discord.com/api/v10"
        self._http: HttpClient | None = None

    def xǁDiscordConnectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._bot_token: str = None
        self._base_url: str = "https://discord.com/api/v10"
        self._http: HttpClient | None = None

    def xǁDiscordConnectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._bot_token: str = "XXXX"
        self._base_url: str = "https://discord.com/api/v10"
        self._http: HttpClient | None = None

    def xǁDiscordConnectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._bot_token: str = ""
        self._base_url: str = None
        self._http: HttpClient | None = None

    def xǁDiscordConnectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._bot_token: str = ""
        self._base_url: str = "XXhttps://discord.com/api/v10XX"
        self._http: HttpClient | None = None

    def xǁDiscordConnectorǁ__init____mutmut_5(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._bot_token: str = ""
        self._base_url: str = "HTTPS://DISCORD.COM/API/V10"
        self._http: HttpClient | None = None

    def xǁDiscordConnectorǁ__init____mutmut_6(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._bot_token: str = ""
        self._base_url: str = "https://discord.com/api/v10"
        self._http: HttpClient | None = ""

    @_mutmut_mutated(mutants_xǁDiscordConnectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_orig(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_1(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider and not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_2(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_3(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_4(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error(None)
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_5(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("XXDiscordConnector: Bot Token no configuradoXX")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_6(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("discordconnector: bot token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_7(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DISCORDCONNECTOR: BOT TOKEN NO CONFIGURADO")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_8(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return True

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_9(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = None

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_10(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(None, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_11(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, None, "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_12(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", None)

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_13(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr("_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_14(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_15(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", )

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_16(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "XX_api_keyXX", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_17(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_API_KEY", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_18(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "XXXX")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_19(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_20(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error(None)
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_21(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("XXDiscordConnector: Bot Token es requeridoXX")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_22(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("discordconnector: bot token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_23(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DISCORDCONNECTOR: BOT TOKEN ES REQUERIDO")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_24(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return True

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_25(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = None
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_26(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=None,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_27(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=None,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_28(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_29(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_30(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth(None, token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_31(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=None)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_32(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth(token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_33(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", )
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_34(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("XXBearerXX", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_35(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_36(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("BEARER", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_37(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header(None, f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_38(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", None)

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_39(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header(f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_40(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", )

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_41(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("XXAuthorizationXX", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_42(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_43(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("AUTHORIZATION", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_44(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = None
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_45(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = False
        self._log_operation("connect", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_46(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation(None, "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_47(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", None)
        return True

    def xǁDiscordConnectorǁconnect__mutmut_48(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_49(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", )
        return True

    def xǁDiscordConnectorǁconnect__mutmut_50(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("XXconnectXX", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_51(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("CONNECT", "Bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_52(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "XXBot token configuradoXX")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_53(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "bot token configurado")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_54(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "BOT TOKEN CONFIGURADO")
        return True

    def xǁDiscordConnectorǁconnect__mutmut_55(self) -> bool:
        """Establece conexion con la API de Discord Bot."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("DiscordConnector: Bot Token no configurado")
            return False

        self._bot_token = getattr(self._auth_provider, "_api_key", "")

        if not self._bot_token:
            logger.error("DiscordConnector: Bot Token es requerido")
            return False

        # Set up HttpClient with Bot token auth
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        # Discord uses "Bot" prefix for bot tokens
        self._http.set_auth("Bearer", token=self._bot_token)
        # Override the Authorization header to use "Bot" prefix instead of "Bearer"
        self._http.set_header("Authorization", f"Bot {self._bot_token}")

        self._connected = True
        self._log_operation("connect", "Bot token configurado")
        return False

    @_mutmut_mutated(mutants_xǁDiscordConnectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "XXsend_messageXX": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "SEND_MESSAGE": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "XXsend_embedXX": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "SEND_EMBED": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "XXlist_channelsXX": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "LIST_CHANNELS": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "XXget_channel_messagesXX": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "GET_CHANNEL_MESSAGES": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "XXadd_roleXX": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "ADD_ROLE": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "XXremove_roleXX": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "REMOVE_ROLE": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = None
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁDiscordConnectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Discord.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_message": self._send_message,
            "send_embed": self._send_embed,
            "list_channels": self._list_channels,
            "get_channel_messages": self._get_channel_messages,
            "add_role": self._add_role,
            "remove_role": self._remove_role,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁDiscordConnectorǁvalidate__mutmut)
    def validate(self) -> bool:
        """Valida que el Bot Token de Discord este configurado."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁDiscordConnectorǁvalidate__mutmut_orig(self) -> bool:
        """Valida que el Bot Token de Discord este configurado."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁDiscordConnectorǁvalidate__mutmut_1(self) -> bool:
        """Valida que el Bot Token de Discord este configurado."""
        if self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁDiscordConnectorǁvalidate__mutmut_2(self) -> bool:
        """Valida que el Bot Token de Discord este configurado."""
        if not self._auth_provider:
            return True
        return self._auth_provider.validate()

    @_mutmut_mutated(mutants_xǁDiscordConnectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        """Cierra la conexion con Discord."""
        self._connected = False
        self._bot_token = ""
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁDiscordConnectorǁdisconnect__mutmut_orig(self) -> bool:
        """Cierra la conexion con Discord."""
        self._connected = False
        self._bot_token = ""
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁDiscordConnectorǁdisconnect__mutmut_1(self) -> bool:
        """Cierra la conexion con Discord."""
        self._connected = None
        self._bot_token = ""
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁDiscordConnectorǁdisconnect__mutmut_2(self) -> bool:
        """Cierra la conexion con Discord."""
        self._connected = True
        self._bot_token = ""
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁDiscordConnectorǁdisconnect__mutmut_3(self) -> bool:
        """Cierra la conexion con Discord."""
        self._connected = False
        self._bot_token = None
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁDiscordConnectorǁdisconnect__mutmut_4(self) -> bool:
        """Cierra la conexion con Discord."""
        self._connected = False
        self._bot_token = "XXXX"
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁDiscordConnectorǁdisconnect__mutmut_5(self) -> bool:
        """Cierra la conexion con Discord."""
        self._connected = False
        self._bot_token = ""
        self._http = ""
        self._log_operation("disconnect")
        return True

    def xǁDiscordConnectorǁdisconnect__mutmut_6(self) -> bool:
        """Cierra la conexion con Discord."""
        self._connected = False
        self._bot_token = ""
        self._http = None
        self._log_operation(None)
        return True

    def xǁDiscordConnectorǁdisconnect__mutmut_7(self) -> bool:
        """Cierra la conexion con Discord."""
        self._connected = False
        self._bot_token = ""
        self._http = None
        self._log_operation("XXdisconnectXX")
        return True

    def xǁDiscordConnectorǁdisconnect__mutmut_8(self) -> bool:
        """Cierra la conexion con Discord."""
        self._connected = False
        self._bot_token = ""
        self._http = None
        self._log_operation("DISCONNECT")
        return True

    def xǁDiscordConnectorǁdisconnect__mutmut_9(self) -> bool:
        """Cierra la conexion con Discord."""
        self._connected = False
        self._bot_token = ""
        self._http = None
        self._log_operation("disconnect")
        return False

    @_mutmut_mutated(mutants_xǁDiscordConnectorǁ_send_message__mutmut)
    def _send_message(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = None
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get(None, "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", None)
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", )
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("XXchannel_idXX", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("CHANNEL_ID", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "XXXX")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = None
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get(None, "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", None)
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", )
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("XXcontentXX", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("CONTENT", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "XXXX")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id and not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"SUCCESS": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": True, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "XXerrorXX": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "ERROR": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "XXParametros requeridos: channel_id, contentXX"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: CHANNEL_ID, CONTENT"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation(None, f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation(f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", )

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("XXsend_messageXX", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("SEND_MESSAGE", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": True, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connected. Call connect() first.XX"}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "connector not connected. call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED. CALL CONNECT() FIRST."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = None
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"XXcontentXX": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"CONTENT": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get(None):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("XXttsXX"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("TTS"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = None

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["XXttsXX"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["TTS"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["XXttsXX"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["TTS"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = None

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(None, json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=None)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = None
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() and {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "XXsuccessXX": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "SUCCESS": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": False,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "XXmessage_idXX": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "MESSAGE_ID": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get(None, ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", None),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get(""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("XXidXX", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("ID", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", "XXXX"),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "XXchannel_idXX": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "CHANNEL_ID": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get(None, channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", None),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get(channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", ),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("XXchannel_idXX", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("CHANNEL_ID", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "XXcontentXX": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "CONTENT": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get(None, ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", None),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get(""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("XXcontentXX", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("CONTENT", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", "XXXX"),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "XXauthorXX": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "AUTHOR": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get(None, ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", None),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get(""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get(None, {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", None).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get({}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", ).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("XXauthorXX", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("AUTHOR", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("XXusernameXX", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("USERNAME", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", "XXXX"),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "XXtimestampXX": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "TIMESTAMP": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get(None, ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", None),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get(""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("XXtimestampXX", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("TIMESTAMP", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", "XXXX"),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = None
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() and {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "XXsuccessXX": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "SUCCESS": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": True,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "XXerrorXX": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "ERROR": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get(None, f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", None),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get(f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", ),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("XXmessageXX", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("MESSAGE", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "XXstatus_codeXX": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "STATUS_CODE": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "XXerror_codeXX": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "ERROR_CODE": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get(None),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("XXcodeXX"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("CODE"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_message__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje de texto a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'content'
        """
        channel_id = params.get("channel_id", "")
        content = params.get("content", "")
        if not channel_id or not content:
            return {"success": False, "error": "Parametros requeridos: channel_id, content"}

        self._log_operation("send_message", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"content": content}
            if params.get("tts"):
                payload["tts"] = params["tts"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "content": data.get("content", ""),
                    "author": data.get("author", {}).get("username", ""),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Unexpected error: {e}"}

    @_mutmut_mutated(mutants_xǁDiscordConnectorǁ_send_embed__mutmut)
    def _send_embed(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = None
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get(None, "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", None)
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", )
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("XXchannel_idXX", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("CHANNEL_ID", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "XXXX")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = None
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get(None, {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", None)
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get({})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", )
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("XXembedXX", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("EMBED", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id and not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"SUCCESS": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": True, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "XXerrorXX": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "ERROR": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "XXParametros requeridos: channel_id, embedXX"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: CHANNEL_ID, EMBED"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation(None, f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation(f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", )

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("XXsend_embedXX", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("SEND_EMBED", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": True, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connected. Call connect() first.XX"}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "connector not connected. call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED. CALL CONNECT() FIRST."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = None
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"XXembedsXX": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"EMBEDS": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get(None):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("XXcontentXX"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("CONTENT"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = None

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["XXcontentXX"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["CONTENT"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["XXcontentXX"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["CONTENT"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = None

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(None, json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=None)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", )

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = None
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() and {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "XXsuccessXX": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "SUCCESS": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": False,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "XXmessage_idXX": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "MESSAGE_ID": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get(None, ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", None),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get(""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("XXidXX", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("ID", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", "XXXX"),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "XXchannel_idXX": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "CHANNEL_ID": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get(None, channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", None),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get(channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", ),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("XXchannel_idXX", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("CHANNEL_ID", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "XXembedsXX": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "EMBEDS": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get(None, []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", None),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get([]),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", ),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("XXembedsXX", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("EMBEDS", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "XXtimestampXX": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "TIMESTAMP": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get(None, ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", None),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get(""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("XXtimestampXX", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("TIMESTAMP", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", "XXXX"),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = None
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() and {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "XXsuccessXX": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "SUCCESS": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": True,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "XXerrorXX": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "ERROR": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get(None, f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", None),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get(f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", ),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("XXmessageXX", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("MESSAGE", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "XXstatus_codeXX": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "STATUS_CODE": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "XXerror_codeXX": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "ERROR_CODE": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get(None),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("XXcodeXX"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("CODE"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_send_embed__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un mensaje con embed a un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y 'embed' (dict con title, description, etc.)
        """
        channel_id = params.get("channel_id", "")
        embed = params.get("embed", {})
        if not channel_id or not embed:
            return {"success": False, "error": "Parametros requeridos: channel_id, embed"}

        self._log_operation("send_embed", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {"embeds": [embed]}
            if params.get("content"):
                payload["content"] = params["content"]

            response = self._http.post(f"/channels/{channel_id}/messages", json=payload)

            if response.ok:
                data = response.json() or {}
                return {
                    "success": True,
                    "message_id": data.get("id", ""),
                    "channel_id": data.get("channel_id", channel_id),
                    "embeds": data.get("embeds", []),
                    "timestamp": data.get("timestamp", ""),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Unexpected error: {e}"}

    @_mutmut_mutated(mutants_xǁDiscordConnectorǁ_list_channels__mutmut)
    def _list_channels(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = None
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get(None, "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", None)
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", )
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("XXguild_idXX", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("GUILD_ID", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "XXXX")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"XXsuccessXX": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"SUCCESS": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": True, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "XXerrorXX": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "ERROR": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "XXParametro requerido: guild_idXX"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "PARAMETRO REQUERIDO: GUILD_ID"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation(None, f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation(f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", )

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("XXlist_channelsXX", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("LIST_CHANNELS", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": True, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connected. Call connect() first.XX"}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "connector not connected. call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED. CALL CONNECT() FIRST."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = None

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(None)

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = None
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() and []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = None
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "XXidXX": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "ID": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get(None, ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", None),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get(""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("XXidXX", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("ID", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", "XXXX"),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "XXnameXX": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "NAME": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get(None, ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", None),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get(""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("XXnameXX", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("NAME", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", "XXXX"),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "XXtypeXX": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "TYPE": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get(None, 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", None),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get(0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", ),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("XXtypeXX", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("TYPE", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 1),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "XXpositionXX": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "POSITION": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get(None, 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", None),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get(0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", ),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("XXpositionXX", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("POSITION", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 1),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "XXparent_idXX": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "PARENT_ID": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get(None),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("XXparent_idXX"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("PARENT_ID"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "XXtopicXX": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "TOPIC": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get(None, ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", None),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get(""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("XXtopicXX", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("TOPIC", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", "XXXX"),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "XXnsfwXX": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "NSFW": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get(None, False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", None),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get(False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", ),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("XXnsfwXX", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("NSFW", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", True),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "XXsuccessXX": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "SUCCESS": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": False,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "XXchannelsXX": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "CHANNELS": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "XXguild_idXX": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "GUILD_ID": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "XXtotalXX": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "TOTAL": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = None
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() and {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "XXsuccessXX": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "SUCCESS": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": True,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "XXerrorXX": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "ERROR": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get(None, f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", None),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get(f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", ),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("XXmessageXX", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("MESSAGE", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "XXstatus_codeXX": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "STATUS_CODE": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "XXerror_codeXX": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "ERROR_CODE": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get(None),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("XXcodeXX"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("CODE"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_list_channels__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista los canales de un servidor de Discord.

        Args:
            params: Debe contener 'guild_id'
        """
        guild_id = params.get("guild_id", "")
        if not guild_id:
            return {"success": False, "error": "Parametro requerido: guild_id"}

        self._log_operation("list_channels", f"guild={guild_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.get(f"/guilds/{guild_id}/channels")

            if response.ok:
                data = response.json() or []
                channels = [
                    {
                        "id": ch.get("id", ""),
                        "name": ch.get("name", ""),
                        "type": ch.get("type", 0),
                        "position": ch.get("position", 0),
                        "parent_id": ch.get("parent_id"),
                        "topic": ch.get("topic", ""),
                        "nsfw": ch.get("nsfw", False),
                    }
                    for ch in data
                ]
                return {
                    "success": True,
                    "channels": channels,
                    "guild_id": guild_id,
                    "total": len(channels),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Unexpected error: {e}"}

    @_mutmut_mutated(mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut)
    def _get_channel_messages(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = None
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get(None, "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", None)
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", )
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("XXchannel_idXX", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("CHANNEL_ID", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "XXXX")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"XXsuccessXX": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"SUCCESS": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": True, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "XXerrorXX": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "ERROR": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "XXParametro requerido: channel_idXX"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "PARAMETRO REQUERIDO: CHANNEL_ID"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = None
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get(None, 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", None)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get(50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", )
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("XXlimitXX", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("LIMIT", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 51)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation(None, f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation(f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", )

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("XXget_channel_messagesXX", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("GET_CHANNEL_MESSAGES", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": True, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connected. Call connect() first.XX"}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "connector not connected. call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED. CALL CONNECT() FIRST."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = None
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"XXlimitXX": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"LIMIT": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(None, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, None)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, )}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 101)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get(None):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("XXbeforeXX"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("BEFORE"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = None
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["XXbeforeXX"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["BEFORE"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["XXbeforeXX"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["BEFORE"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get(None):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("XXafterXX"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("AFTER"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = None

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["XXafterXX"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["AFTER"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["XXafterXX"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["AFTER"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = None

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(None, params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=None)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", )

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = None
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() and []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = None
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "XXidXX": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "ID": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get(None, ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", None),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get(""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("XXidXX", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("ID", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", "XXXX"),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "XXcontentXX": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "CONTENT": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get(None, ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", None),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get(""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("XXcontentXX", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("CONTENT", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", "XXXX"),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "XXauthorXX": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "AUTHOR": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "XXidXX": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "ID": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get(None, ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", None),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get(""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get(None, {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", None).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get({}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", ).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("XXauthorXX", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("AUTHOR", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("XXidXX", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("ID", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", "XXXX"),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "XXusernameXX": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "USERNAME": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get(None, ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", None),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get(""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get(None, {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", None).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get({}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", ).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("XXauthorXX", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("AUTHOR", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("XXusernameXX", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("USERNAME", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", "XXXX"),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "XXbotXX": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "BOT": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get(None, False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", None),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get(False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", ),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get(None, {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", None).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get({}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", ).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("XXauthorXX", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("AUTHOR", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("XXbotXX", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("BOT", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", True),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "XXtimestampXX": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "TIMESTAMP": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get(None, ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", None),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get(""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("XXtimestampXX", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("TIMESTAMP", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", "XXXX"),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "XXedited_timestampXX": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "EDITED_TIMESTAMP": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get(None),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("XXedited_timestampXX"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("EDITED_TIMESTAMP"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "XXtypeXX": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "TYPE": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get(None, 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", None),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get(0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", ),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("XXtypeXX", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("TYPE", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 1),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "XXattachmentsXX": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "ATTACHMENTS": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get(None, []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", None),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get([]),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", ),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("XXattachmentsXX", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("ATTACHMENTS", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "XXsuccessXX": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "SUCCESS": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": False,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "XXmessagesXX": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "MESSAGES": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "XXchannel_idXX": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "CHANNEL_ID": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "XXtotalXX": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "TOTAL": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = None
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() and {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "XXsuccessXX": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "SUCCESS": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": True,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "XXerrorXX": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "ERROR": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get(None, f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_186(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", None),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_187(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get(f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_188(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", ),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_189(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("XXmessageXX", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_190(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("MESSAGE", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_191(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "XXstatus_codeXX": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_192(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "STATUS_CODE": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_193(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "XXerror_codeXX": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_194(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "ERROR_CODE": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_195(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get(None),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_196(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("XXcodeXX"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_197(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("CODE"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_198(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_199(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_200(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_201(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_202(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_203(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_204(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_205(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_206(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_get_channel_messages__mutmut_207(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene mensajes de un canal de Discord.

        Args:
            params: Debe contener 'channel_id' y opcionalmente 'limit'
        """
        channel_id = params.get("channel_id", "")
        if not channel_id:
            return {"success": False, "error": "Parametro requerido: channel_id"}

        limit = params.get("limit", 50)
        self._log_operation("get_channel_messages", f"channel={channel_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"limit": min(limit, 100)}
            if params.get("before"):
                query_params["before"] = params["before"]
            if params.get("after"):
                query_params["after"] = params["after"]

            response = self._http.get(f"/channels/{channel_id}/messages", params=query_params)

            if response.ok:
                data = response.json() or []
                messages = [
                    {
                        "id": msg.get("id", ""),
                        "content": msg.get("content", ""),
                        "author": {
                            "id": msg.get("author", {}).get("id", ""),
                            "username": msg.get("author", {}).get("username", ""),
                            "bot": msg.get("author", {}).get("bot", False),
                        },
                        "timestamp": msg.get("timestamp", ""),
                        "edited_timestamp": msg.get("edited_timestamp"),
                        "type": msg.get("type", 0),
                        "attachments": msg.get("attachments", []),
                    }
                    for msg in data
                ]
                return {
                    "success": True,
                    "messages": messages,
                    "channel_id": channel_id,
                    "total": len(messages),
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Unexpected error: {e}"}

    @_mutmut_mutated(mutants_xǁDiscordConnectorǁ_add_role__mutmut)
    def _add_role(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = None
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get(None, "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", None)
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", )
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("XXguild_idXX", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("GUILD_ID", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "XXXX")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = None
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get(None, "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", None)
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", )
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("XXuser_idXX", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("USER_ID", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "XXXX")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = None
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get(None, "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", None)
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", )
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("XXrole_idXX", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("ROLE_ID", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "XXXX")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id and not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id and not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"SUCCESS": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": True, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "XXerrorXX": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "ERROR": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "XXParametros requeridos: guild_id, user_id, role_idXX"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: GUILD_ID, USER_ID, ROLE_ID"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation(None, f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation(f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", )

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("XXadd_roleXX", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("ADD_ROLE", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": True, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connected. Call connect() first.XX"}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "connector not connected. call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED. CALL CONNECT() FIRST."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = None

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(None)

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok and response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code != 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 205:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "XXsuccessXX": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "SUCCESS": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": False,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "XXguild_idXX": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "GUILD_ID": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "XXuser_idXX": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "USER_ID": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "XXrole_idXX": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "ROLE_ID": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = None
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() and {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "XXsuccessXX": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "SUCCESS": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": True,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "XXerrorXX": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "ERROR": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get(None, f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", None),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get(f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", ),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("XXmessageXX", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("MESSAGE", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "XXstatus_codeXX": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "STATUS_CODE": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "XXerror_codeXX": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "ERROR_CODE": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get(None),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("XXcodeXX"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("CODE"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_add_role__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Asigna un rol a un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("add_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.put(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Unexpected error: {e}"}

    @_mutmut_mutated(mutants_xǁDiscordConnectorǁ_remove_role__mutmut)
    def _remove_role(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = None
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get(None, "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", None)
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", )
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("XXguild_idXX", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("GUILD_ID", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "XXXX")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = None
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get(None, "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", None)
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", )
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("XXuser_idXX", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("USER_ID", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "XXXX")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = None
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get(None, "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", None)
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", )
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("XXrole_idXX", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("ROLE_ID", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "XXXX")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id and not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id and not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"SUCCESS": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": True, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "XXerrorXX": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "ERROR": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "XXParametros requeridos: guild_id, user_id, role_idXX"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: GUILD_ID, USER_ID, ROLE_ID"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation(None, f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation(f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", )

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("XXremove_roleXX", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("REMOVE_ROLE", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": True, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connected. Call connect() first.XX"}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "connector not connected. call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED. CALL CONNECT() FIRST."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = None

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(None)

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok and response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code != 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 205:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "XXsuccessXX": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "SUCCESS": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": False,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "XXguild_idXX": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "GUILD_ID": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "XXuser_idXX": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "USER_ID": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "XXrole_idXX": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "ROLE_ID": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = None
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() and {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "XXsuccessXX": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "SUCCESS": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": True,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "XXerrorXX": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "ERROR": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get(None, f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", None),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get(f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", ),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("XXmessageXX", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("MESSAGE", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "XXstatus_codeXX": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "STATUS_CODE": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "XXerror_codeXX": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "ERROR_CODE": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get(None),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("XXcodeXX"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("CODE"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Unexpected error: {e}"}

    def xǁDiscordConnectorǁ_remove_role__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Remueve un rol de un miembro del servidor.

        Args:
            params: Debe contener 'guild_id', 'user_id' y 'role_id'
        """
        guild_id = params.get("guild_id", "")
        user_id = params.get("user_id", "")
        role_id = params.get("role_id", "")
        if not guild_id or not user_id or not role_id:
            return {"success": False, "error": "Parametros requeridos: guild_id, user_id, role_id"}

        self._log_operation("remove_role", f"user={user_id}, role={role_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            response = self._http.delete(f"/guilds/{guild_id}/members/{user_id}/roles/{role_id}")

            if response.ok or response.status_code == 204:
                return {
                    "success": True,
                    "guild_id": guild_id,
                    "user_id": user_id,
                    "role_id": role_id,
                }
            else:
                error_data = response.json() or {}
                return {
                    "success": False,
                    "error": error_data.get("message", f"HTTP {response.status_code}"),
                    "status_code": response.status_code,
                    "error_code": error_data.get("code"),
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Unexpected error: {e}"}

mutants_xǁDiscordConnectorǁ__init____mutmut['_mutmut_orig'] = DiscordConnector.xǁDiscordConnectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ__init____mutmut['xǁDiscordConnectorǁ__init____mutmut_1'] = DiscordConnector.xǁDiscordConnectorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ__init____mutmut['xǁDiscordConnectorǁ__init____mutmut_2'] = DiscordConnector.xǁDiscordConnectorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ
] = DiscordConnector.xǁDiscordConnectorǁ_get_channel_messages__mutmut_190 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut['xǁDiscordConnectorǁ_get_channel_messages__mutmut_191'] = DiscordConnector.xǁDiscordConnectorǁ_get_channel_messages__mutmut_191 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut['xǁDiscordConnectorǁ_get_channel_messages__mutmut_192'] = DiscordConnector.xǁDiscordConnectorǁ_get_channel_messages__mutmut_192 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut['xǁDiscordConnectorǁ_get_channel_messages__mutmut_193'] = DiscordConnector.xǁDiscordConnectorǁ_get_channel_messages__mutmut_193 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut['xǁDiscordConnectorǁ_get_channel_messages__mutmut_194'] = DiscordConnector.xǁDiscordConnectorǁ_get_channel_messages__mutmut_194 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut['xǁDiscordConnectorǁ_get_channel_messages__mutmut_195'] = DiscordConnector.xǁDiscordConnectorǁ_get_channel_messages__mutmut_195 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut['xǁDiscordConnectorǁ_get_channel_messages__mutmut_196'] = DiscordConnector.xǁDiscordConnectorǁ_get_channel_messages__mutmut_196 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut['xǁDiscordConnectorǁ_get_channel_messages__mutmut_197'] = DiscordConnector.xǁDiscordConnectorǁ_get_channel_messages__mutmut_197 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut['xǁDiscordConnectorǁ_get_channel_messages__mutmut_198'] = DiscordConnector.xǁDiscordConnectorǁ_get_channel_messages__mutmut_198 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut['xǁDiscordConnectorǁ_get_channel_messages__mutmut_199'] = DiscordConnector.xǁDiscordConnectorǁ_get_channel_messages__mutmut_199 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut['xǁDiscordConnectorǁ_get_channel_messages__mutmut_200'] = DiscordConnector.xǁDiscordConnectorǁ_get_channel_messages__mutmut_200 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut['xǁDiscordConnectorǁ_get_channel_messages__mutmut_201'] = DiscordConnector.xǁDiscordConnectorǁ_get_channel_messages__mutmut_201 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut['xǁDiscordConnectorǁ_get_channel_messages__mutmut_202'] = DiscordConnector.xǁDiscordConnectorǁ_get_channel_messages__mutmut_202 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut['xǁDiscordConnectorǁ_get_channel_messages__mutmut_203'] = DiscordConnector.xǁDiscordConnectorǁ_get_channel_messages__mutmut_203 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut['xǁDiscordConnectorǁ_get_channel_messages__mutmut_204'] = DiscordConnector.xǁDiscordConnectorǁ_get_channel_messages__mutmut_204 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut['xǁDiscordConnectorǁ_get_channel_messages__mutmut_205'] = DiscordConnector.xǁDiscordConnectorǁ_get_channel_messages__mutmut_205 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut['xǁDiscordConnectorǁ_get_channel_messages__mutmut_206'] = DiscordConnector.xǁDiscordConnectorǁ_get_channel_messages__mutmut_206 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_get_channel_messages__mutmut['xǁDiscordConnectorǁ_get_channel_messages__mutmut_207'] = DiscordConnector.xǁDiscordConnectorǁ_get_channel_messages__mutmut_207 # type: ignore # mutmut generated

mutants_xǁDiscordConnectorǁ_add_role__mutmut['_mutmut_orig'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_1'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_2'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_3'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_4'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_5'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_6'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_7'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_8'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_9'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_10'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_11'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_12'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_13'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_14'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_15'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_16'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_17'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_17 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_18'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_18 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_19'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_19 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_20'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_20 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_21'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_21 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_22'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_22 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_23'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_23 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_24'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_24 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_25'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_25 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_26'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_26 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_27'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_27 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_28'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_28 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_29'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_29 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_30'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_30 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_31'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_31 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_32'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_32 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_33'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_33 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_34'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_34 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_35'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_35 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_36'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_36 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_37'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_37 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_38'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_38 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_39'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_39 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_40'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_40 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_41'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_41 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_42'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_42 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_43'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_43 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_44'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_44 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_45'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_45 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_46'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_46 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_47'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_47 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_48'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_48 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_49'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_49 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_50'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_50 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_51'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_51 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_52'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_52 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_53'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_53 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_54'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_54 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_55'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_55 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_56'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_56 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_57'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_57 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_58'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_58 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_59'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_59 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_60'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_60 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_61'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_61 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_62'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_62 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_63'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_63 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_64'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_64 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_65'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_65 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_66'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_66 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_67'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_67 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_68'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_68 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_69'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_69 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_70'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_70 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_71'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_71 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_72'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_72 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_73'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_73 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_74'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_74 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_75'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_75 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_76'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_76 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_77'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_77 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_78'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_78 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_79'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_79 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_80'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_80 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_81'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_81 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_82'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_82 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_83'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_83 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_84'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_84 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_85'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_85 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_86'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_86 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_87'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_87 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_88'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_88 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_89'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_89 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_90'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_90 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_91'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_91 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_92'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_92 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_93'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_93 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_94'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_94 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_95'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_95 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_add_role__mutmut['xǁDiscordConnectorǁ_add_role__mutmut_96'] = DiscordConnector.xǁDiscordConnectorǁ_add_role__mutmut_96 # type: ignore # mutmut generated

mutants_xǁDiscordConnectorǁ_remove_role__mutmut['_mutmut_orig'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_orig # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_1'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_1 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_2'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_2 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_3'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_3 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_4'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_4 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_5'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_5 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_6'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_6 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_7'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_7 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_8'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_8 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_9'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_9 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_10'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_10 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_11'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_11 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_12'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_12 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_13'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_13 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_14'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_14 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_15'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_15 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_16'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_16 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_17'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_17 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_18'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_18 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_19'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_19 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_20'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_20 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_21'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_21 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_22'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_22 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_23'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_23 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_24'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_24 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_25'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_25 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_26'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_26 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_27'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_27 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_28'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_28 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_29'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_29 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_30'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_30 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_31'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_31 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_32'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_32 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_33'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_33 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_34'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_34 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_35'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_35 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_36'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_36 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_37'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_37 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_38'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_38 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_39'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_39 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_40'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_40 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_41'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_41 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_42'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_42 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_43'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_43 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_44'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_44 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_45'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_45 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_46'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_46 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_47'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_47 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_48'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_48 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_49'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_49 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_50'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_50 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_51'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_51 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_52'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_52 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_53'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_53 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_54'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_54 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_55'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_55 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_56'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_56 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_57'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_57 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_58'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_58 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_59'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_59 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_60'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_60 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_61'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_61 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_62'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_62 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_63'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_63 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_64'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_64 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_65'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_65 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_66'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_66 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_67'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_67 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_68'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_68 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_69'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_69 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_70'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_70 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_71'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_71 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_72'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_72 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_73'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_73 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_74'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_74 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_75'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_75 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_76'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_76 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_77'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_77 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_78'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_78 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_79'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_79 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_80'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_80 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_81'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_81 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_82'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_82 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_83'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_83 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_84'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_84 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_85'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_85 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_86'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_86 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_87'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_87 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_88'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_88 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_89'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_89 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_90'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_90 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_91'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_91 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_92'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_92 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_93'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_93 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_94'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_94 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_95'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_95 # type: ignore # mutmut generated
mutants_xǁDiscordConnectorǁ_remove_role__mutmut['xǁDiscordConnectorǁ_remove_role__mutmut_96'] = DiscordConnector.xǁDiscordConnectorǁ_remove_role__mutmut_96 # type: ignore # mutmut generated


DISCORD_SCHEMA = ConnectorSchema(
    name="discord",
    version="1.0.0",
    description="Envia mensajes, gestiona roles y administra servidores de Discord",
    category="communication",
    icon="message-circle",
    author="Zenic-Flijo",
    actions=[
        ActionDefinition(name="send_message", description="Envia un mensaje de texto", category="write"),
        ActionDefinition(name="send_embed", description="Envia un mensaje con embed", category="write"),
        ActionDefinition(name="list_channels", description="Lista canales del servidor", category="read"),
        ActionDefinition(name="get_channel_messages", description="Obtiene mensajes del canal", category="read"),
        ActionDefinition(name="add_role", description="Asigna un rol a un miembro", category="write"),
        ActionDefinition(name="remove_role", description="Remueve un rol de un miembro", category="write"),
    ],
    auth_requirements=[
        AuthRequirement(auth_type="api_key", required_fields=["bot_token"], description="Discord Bot Token")
    ],
)
