"""
Conector SendGrid — Email via SendGrid API
============================================

Permite enviar correos electronicos, gestionar listas de
contactos, crear plantillas y trackear eventos de email
usando la API de SendGrid.
"""

from __future__ import annotations

from typing import Any

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.http_client import HttpClient, HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁSendGridConnectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁSendGridConnectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSendGridConnectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSendGridConnectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSendGridConnectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSendGridConnectorǁ_send_email__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSendGridConnectorǁ_send_template_email__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSendGridConnectorǁ_add_contact__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSendGridConnectorǁ_list_contacts__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSendGridConnectorǁ_create_template__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSendGridConnectorǁ_get_email_stats__mutmut: MutantDict = {}  # type: ignore


class SendGridConnector(BaseConnector):
    """Conector para SendGrid: envio de emails y gestion de contactos."""

    name = "sendgrid"
    version = "1.0.0"
    description = "Envia emails, gestiona contactos y plantillas via SendGrid"
    category = "communication"
    icon = "mail"
    author = "Zenic-Flijo"

    @_mutmut_mutated(mutants_xǁSendGridConnectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._api_key: str = ""
        self._base_url: str = "https://api.sendgrid.com/v3"
        self._http: HttpClient | None = None

    def xǁSendGridConnectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._api_key: str = ""
        self._base_url: str = "https://api.sendgrid.com/v3"
        self._http: HttpClient | None = None

    def xǁSendGridConnectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._api_key: str = None
        self._base_url: str = "https://api.sendgrid.com/v3"
        self._http: HttpClient | None = None

    def xǁSendGridConnectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._api_key: str = "XXXX"
        self._base_url: str = "https://api.sendgrid.com/v3"
        self._http: HttpClient | None = None

    def xǁSendGridConnectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._api_key: str = ""
        self._base_url: str = None
        self._http: HttpClient | None = None

    def xǁSendGridConnectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._api_key: str = ""
        self._base_url: str = "XXhttps://api.sendgrid.com/v3XX"
        self._http: HttpClient | None = None

    def xǁSendGridConnectorǁ__init____mutmut_5(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._api_key: str = ""
        self._base_url: str = "HTTPS://API.SENDGRID.COM/V3"
        self._http: HttpClient | None = None

    def xǁSendGridConnectorǁ__init____mutmut_6(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._api_key: str = ""
        self._base_url: str = "https://api.sendgrid.com/v3"
        self._http: HttpClient | None = ""

    @_mutmut_mutated(mutants_xǁSendGridConnectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_orig(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_1(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider and not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_2(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_3(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_4(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error(None)
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_5(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("XXSendGridConnector: API key no configuradaXX")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_6(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("sendgridconnector: api key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_7(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SENDGRIDCONNECTOR: API KEY NO CONFIGURADA")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_8(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return True

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_9(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = None

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_10(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(None, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_11(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, None, "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_12(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", None)

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_13(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr("_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_14(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_15(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", )

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_16(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "XX_api_keyXX", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_17(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_API_KEY", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_18(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "XXXX")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_19(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_20(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error(None)
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_21(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("XXSendGridConnector: API key es requeridaXX")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_22(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("sendgridconnector: api key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_23(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SENDGRIDCONNECTOR: API KEY ES REQUERIDA")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_24(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return True

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_25(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = None
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_26(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=None,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_27(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=None,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_28(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_29(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_30(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth(None, token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_31(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=None)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_32(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth(token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_33(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", )

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_34(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("XXBearerXX", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_35(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_36(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("BEARER", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_37(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = None
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_38(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = False
        self._log_operation("connect", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_39(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation(None, "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_40(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", None)
        return True

    def xǁSendGridConnectorǁconnect__mutmut_41(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_42(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", )
        return True

    def xǁSendGridConnectorǁconnect__mutmut_43(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("XXconnectXX", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_44(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("CONNECT", "API key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_45(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "XXAPI key configuradaXX")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_46(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "api key configurada")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_47(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API KEY CONFIGURADA")
        return True

    def xǁSendGridConnectorǁconnect__mutmut_48(self) -> bool:
        """Establece conexion con la API de SendGrid."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SendGridConnector: API key no configurada")
            return False

        self._api_key = getattr(self._auth_provider, "_api_key", "")

        if not self._api_key:
            logger.error("SendGridConnector: API key es requerida")
            return False

        # Set up HttpClient with Bearer auth (SendGrid uses Bearer token)
        self._http = HttpClient(
            base_url=self._base_url,
            connector_name=self.name,
        )
        self._http.set_auth("Bearer", token=self._api_key)

        self._connected = True
        self._log_operation("connect", "API key configurada")
        return False

    @_mutmut_mutated(mutants_xǁSendGridConnectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "XXsend_emailXX": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "SEND_EMAIL": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "XXsend_template_emailXX": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "SEND_TEMPLATE_EMAIL": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "XXadd_contactXX": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "ADD_CONTACT": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "XXlist_contactsXX": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "LIST_CONTACTS": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "XXcreate_templateXX": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "CREATE_TEMPLATE": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "XXget_email_statsXX": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "GET_EMAIL_STATS": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = None
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁSendGridConnectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector SendGrid.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "send_email": self._send_email,
            "send_template_email": self._send_template_email,
            "add_contact": self._add_contact,
            "list_contacts": self._list_contacts,
            "create_template": self._create_template,
            "get_email_stats": self._get_email_stats,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁSendGridConnectorǁvalidate__mutmut)
    def validate(self) -> bool:
        """Valida que la API key de SendGrid este configurada."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁSendGridConnectorǁvalidate__mutmut_orig(self) -> bool:
        """Valida que la API key de SendGrid este configurada."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁSendGridConnectorǁvalidate__mutmut_1(self) -> bool:
        """Valida que la API key de SendGrid este configurada."""
        if self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁSendGridConnectorǁvalidate__mutmut_2(self) -> bool:
        """Valida que la API key de SendGrid este configurada."""
        if not self._auth_provider:
            return True
        return self._auth_provider.validate()

    @_mutmut_mutated(mutants_xǁSendGridConnectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        """Cierra la conexion con SendGrid."""
        self._connected = False
        self._api_key = ""
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁSendGridConnectorǁdisconnect__mutmut_orig(self) -> bool:
        """Cierra la conexion con SendGrid."""
        self._connected = False
        self._api_key = ""
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁSendGridConnectorǁdisconnect__mutmut_1(self) -> bool:
        """Cierra la conexion con SendGrid."""
        self._connected = None
        self._api_key = ""
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁSendGridConnectorǁdisconnect__mutmut_2(self) -> bool:
        """Cierra la conexion con SendGrid."""
        self._connected = True
        self._api_key = ""
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁSendGridConnectorǁdisconnect__mutmut_3(self) -> bool:
        """Cierra la conexion con SendGrid."""
        self._connected = False
        self._api_key = None
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁSendGridConnectorǁdisconnect__mutmut_4(self) -> bool:
        """Cierra la conexion con SendGrid."""
        self._connected = False
        self._api_key = "XXXX"
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁSendGridConnectorǁdisconnect__mutmut_5(self) -> bool:
        """Cierra la conexion con SendGrid."""
        self._connected = False
        self._api_key = ""
        self._http = ""
        self._log_operation("disconnect")
        return True

    def xǁSendGridConnectorǁdisconnect__mutmut_6(self) -> bool:
        """Cierra la conexion con SendGrid."""
        self._connected = False
        self._api_key = ""
        self._http = None
        self._log_operation(None)
        return True

    def xǁSendGridConnectorǁdisconnect__mutmut_7(self) -> bool:
        """Cierra la conexion con SendGrid."""
        self._connected = False
        self._api_key = ""
        self._http = None
        self._log_operation("XXdisconnectXX")
        return True

    def xǁSendGridConnectorǁdisconnect__mutmut_8(self) -> bool:
        """Cierra la conexion con SendGrid."""
        self._connected = False
        self._api_key = ""
        self._http = None
        self._log_operation("DISCONNECT")
        return True

    def xǁSendGridConnectorǁdisconnect__mutmut_9(self) -> bool:
        """Cierra la conexion con SendGrid."""
        self._connected = False
        self._api_key = ""
        self._http = None
        self._log_operation("disconnect")
        return False

    @_mutmut_mutated(mutants_xǁSendGridConnectorǁ_send_email__mutmut)
    def _send_email(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = None
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get(None, "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", None)
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", )
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("XXtoXX", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("TO", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "XXXX")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = None
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get(None, "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", None)
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", )
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("XXfromXX", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("FROM", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "XXXX")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = None
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get(None, "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", None)
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", )
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("XXsubjectXX", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("SUBJECT", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "XXXX")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = None
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get(None, "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", None)
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", )
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("XXcontentXX", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("CONTENT", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "XXXX")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = None

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get(None, "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", None)

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", )

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("XXcontent_typeXX", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("CONTENT_TYPE", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "XXtext/plainXX")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "TEXT/PLAIN")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email and not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to and not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"SUCCESS": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": True, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "XXerrorXX": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "ERROR": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "XXParametros requeridos: to, from, subjectXX"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: TO, FROM, SUBJECT"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation(None, f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation(f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", )

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("XXsend_emailXX", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("SEND_EMAIL", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": True, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connected. Call connect() first.XX"}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "connector not connected. call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED. CALL CONNECT() FIRST."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = None

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "XXpersonalizationsXX": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "PERSONALIZATIONS": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"XXtoXX": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"TO": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"XXemailXX": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"EMAIL": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "XXfromXX": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "FROM": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"XXemailXX": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"EMAIL": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "XXsubjectXX": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "SUBJECT": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = None

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["XXcontentXX"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["CONTENT"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"XXtypeXX": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"TYPE": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "XXvalueXX": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "VALUE": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get(None):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("XXccXX"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("CC"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = None
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["XXpersonalizationsXX"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["PERSONALIZATIONS"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][1]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["XXccXX"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["CC"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"XXemailXX": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"EMAIL": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["XXccXX"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["CC"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get(None):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("XXbccXX"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("BCC"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = None
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["XXpersonalizationsXX"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["PERSONALIZATIONS"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][1]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["XXbccXX"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["BCC"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"XXemailXX": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"EMAIL": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["XXbccXX"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["BCC"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get(None):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("XXreply_toXX"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("REPLY_TO"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = None

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["XXreply_toXX"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["REPLY_TO"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"XXemailXX": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"EMAIL": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["XXreply_toXX"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["REPLY_TO"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = None

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post(None, json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=None)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post(json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", )

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("XX/mail/sendXX", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/MAIL/SEND", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok and response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code != 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 203:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = None
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get(None, "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", None)
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", )
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("XXX-Message-IdXX", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("x-message-id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-MESSAGE-ID", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "XXXX")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "XXsuccessXX": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "SUCCESS": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": False,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "XXmessage_idXX": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "MESSAGE_ID": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "XXstatusXX": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "STATUS": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "XXacceptedXX",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "ACCEPTED",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "XXtoXX": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "TO": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "XXfromXX": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "FROM": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "XXsubjectXX": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "SUBJECT": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = None
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() and {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = None
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get(None, [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", None)
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get([])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", )
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("XXerrorsXX", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("ERRORS", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = None
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(None, f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", None) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", ) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[1].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("XXmessageXX", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("MESSAGE", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "XXsuccessXX": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "SUCCESS": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": True,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "XXerrorXX": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "ERROR": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "XXstatus_codeXX": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "STATUS_CODE": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_186(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_187(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_188(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_189(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_190(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_191(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_192(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_email__mutmut_193(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email via SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'subject' y 'content'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        subject = params.get("subject", "")
        content = params.get("content", "")
        content_type = params.get("content_type", "text/plain")

        if not to or not from_email or not subject:
            return {"success": False, "error": "Parametros requeridos: to, from, subject"}

        self._log_operation("send_email", f"to={to}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [{"to": [{"email": to}]}],
                "from": {"email": from_email},
                "subject": subject,
            }

            if content:
                payload["content"] = [{"type": content_type, "value": content}]

            # Add optional fields
            if params.get("cc"):
                payload["personalizations"][0]["cc"] = [{"email": e} for e in params["cc"]]
            if params.get("bcc"):
                payload["personalizations"][0]["bcc"] = [{"email": e} for e in params["bcc"]]
            if params.get("reply_to"):
                payload["reply_to"] = {"email": params["reply_to"]}

            response = self._http.post("/mail/send", json=payload)

            # SendGrid returns 202 Accepted on success with no body
            if response.ok or response.status_code == 202:
                # Extract message ID from headers if available
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "from": from_email,
                    "subject": subject,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Unexpected error: {e}"}

    @_mutmut_mutated(mutants_xǁSendGridConnectorǁ_send_template_email__mutmut)
    def _send_template_email(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = None
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get(None, "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", None)
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", )
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("XXtoXX", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("TO", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "XXXX")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = None
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get(None, "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", None)
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", )
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("XXfromXX", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("FROM", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "XXXX")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = None
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get(None, "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", None)
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", )
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("XXtemplate_idXX", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("TEMPLATE_ID", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "XXXX")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = None

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get(None, {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", None)

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get({})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", )

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("XXdynamic_dataXX", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("DYNAMIC_DATA", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email and not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to and not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"SUCCESS": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": True, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "XXerrorXX": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "ERROR": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "XXParametros requeridos: to, from, template_idXX"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: TO, FROM, TEMPLATE_ID"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation(None, f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation(f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", )

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("XXsend_template_emailXX", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("SEND_TEMPLATE_EMAIL", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": True, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connected. Call connect() first.XX"}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "connector not connected. call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED. CALL CONNECT() FIRST."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = None

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "XXpersonalizationsXX": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "PERSONALIZATIONS": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "XXtoXX": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "TO": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"XXemailXX": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"EMAIL": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "XXdynamic_template_dataXX": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "DYNAMIC_TEMPLATE_DATA": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "XXfromXX": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "FROM": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"XXemailXX": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"EMAIL": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "XXtemplate_idXX": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "TEMPLATE_ID": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = None

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post(None, json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=None)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post(json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", )

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("XX/mail/sendXX", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/MAIL/SEND", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok and response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code != 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 203:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = None
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get(None, "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", None)
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", )
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("XXX-Message-IdXX", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("x-message-id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-MESSAGE-ID", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "XXXX")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "XXsuccessXX": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "SUCCESS": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": False,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "XXmessage_idXX": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "MESSAGE_ID": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "XXstatusXX": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "STATUS": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "XXacceptedXX",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "ACCEPTED",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "XXtoXX": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "TO": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "XXtemplate_idXX": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "TEMPLATE_ID": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = None
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() and {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = None
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get(None, [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", None)
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get([])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", )
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("XXerrorsXX", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("ERRORS", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = None
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(None, f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", None) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", ) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[1].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("XXmessageXX", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("MESSAGE", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "XXsuccessXX": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "SUCCESS": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": True,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "XXerrorXX": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "ERROR": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "XXstatus_codeXX": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "STATUS_CODE": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_send_template_email__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Envia un email usando una plantilla de SendGrid.

        Args:
            params: Debe contener 'to', 'from', 'template_id' y 'dynamic_data'
        """
        to = params.get("to", "")
        from_email = params.get("from", "")
        template_id = params.get("template_id", "")
        dynamic_data = params.get("dynamic_data", {})

        if not to or not from_email or not template_id:
            return {"success": False, "error": "Parametros requeridos: to, from, template_id"}

        self._log_operation("send_template_email", f"template={template_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            payload: dict[str, Any] = {
                "personalizations": [
                    {
                        "to": [{"email": to}],
                        "dynamic_template_data": dynamic_data,
                    }
                ],
                "from": {"email": from_email},
                "template_id": template_id,
            }

            response = self._http.post("/mail/send", json=payload)

            if response.ok or response.status_code == 202:
                message_id = response.headers.get("X-Message-Id", "")
                return {
                    "success": True,
                    "message_id": message_id,
                    "status": "accepted",
                    "to": to,
                    "template_id": template_id,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Unexpected error: {e}"}

    @_mutmut_mutated(mutants_xǁSendGridConnectorǁ_add_contact__mutmut)
    def _add_contact(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = None
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get(None, "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", None)
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", )
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("XXemailXX", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("EMAIL", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "XXXX")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"XXsuccessXX": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"SUCCESS": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": True, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "XXerrorXX": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "ERROR": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "XXParametro requerido: emailXX"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "PARAMETRO REQUERIDO: EMAIL"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation(None, f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation(f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", )

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("XXadd_contactXX", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("ADD_CONTACT", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": True, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connected. Call connect() first.XX"}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "connector not connected. call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED. CALL CONNECT() FIRST."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = None
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"XXemailXX": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"EMAIL": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get(None):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("XXfirst_nameXX"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("FIRST_NAME"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = None
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[1]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["XXfirst_nameXX"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["FIRST_NAME"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["XXfirst_nameXX"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["FIRST_NAME"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get(None):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("XXlast_nameXX"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("LAST_NAME"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = None
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[1]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["XXlast_nameXX"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["LAST_NAME"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["XXlast_nameXX"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["LAST_NAME"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get(None):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("XXcustom_fieldsXX"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("CUSTOM_FIELDS"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = None

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[1]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["XXcustom_fieldsXX"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["CUSTOM_FIELDS"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["XXcustom_fieldsXX"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["CUSTOM_FIELDS"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = None
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"XXcontactsXX": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"CONTACTS": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get(None):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("XXlist_idsXX"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("LIST_IDS"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = None

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["XXlist_idsXX"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["LIST_IDS"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["XXlist_idsXX"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["LIST_IDS"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = None

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put(None, json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=None)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put(json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", )

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("XX/marketing/contactsXX", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/MARKETING/CONTACTS", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok and response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code != 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 203:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = None
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() and {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "XXsuccessXX": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "SUCCESS": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": False,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "XXjob_idXX": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "JOB_ID": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get(None, ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", None),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get(""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("XXjob_idXX", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("JOB_ID", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", "XXXX"),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "XXemailXX": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "EMAIL": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = None
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() and {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = None
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get(None, [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", None)
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get([])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", )
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("XXerrorsXX", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("ERRORS", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = None
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(None, f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", None) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", ) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[1].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("XXmessageXX", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("MESSAGE", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "XXsuccessXX": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "SUCCESS": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": True,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "XXerrorXX": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "ERROR": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "XXstatus_codeXX": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "STATUS_CODE": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_add_contact__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Anade un contacto a una lista de SendGrid.

        Args:
            params: Debe contener 'email' y opcionalmente 'first_name', 'last_name', 'list_ids'
        """
        email = params.get("email", "")
        if not email:
            return {"success": False, "error": "Parametro requerido: email"}

        self._log_operation("add_contact", f"email={email}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            contacts = [{"email": email}]
            if params.get("first_name"):
                contacts[0]["first_name"] = params["first_name"]
            if params.get("last_name"):
                contacts[0]["last_name"] = params["last_name"]
            if params.get("custom_fields"):
                contacts[0]["custom_fields"] = params["custom_fields"]

            payload: dict[str, Any] = {"contacts": contacts}
            if params.get("list_ids"):
                payload["list_ids"] = params["list_ids"]

            response = self._http.put("/marketing/contacts", json=payload)

            if response.ok or response.status_code == 202:
                data = response.json() or {}
                return {
                    "success": True,
                    "job_id": data.get("job_id", ""),
                    "email": email,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Unexpected error: {e}"}

    @_mutmut_mutated(mutants_xǁSendGridConnectorǁ_list_contacts__mutmut)
    def _list_contacts(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = None
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get(None, 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", None)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get(20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", )
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("XXlimitXX", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("LIMIT", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 21)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation(None, f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation(f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", )

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("XXlist_contactsXX", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("LIST_CONTACTS", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": True, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connected. Call connect() first.XX"}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "connector not connected. call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED. CALL CONNECT() FIRST."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = None

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"XXpage_sizeXX": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"PAGE_SIZE": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "XXpage_tokenXX": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "PAGE_TOKEN": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get(None, "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", None)}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", )}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("XXpage_tokenXX", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("PAGE_TOKEN", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "XXXX")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = None

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get(None, params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=None)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get(params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", )

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("XX/marketing/contactsXX", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/MARKETING/CONTACTS", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = None
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() and {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = None
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get(None, [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", None)
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get([])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", )
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("XXresultXX", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("RESULT", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "XXsuccessXX": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "SUCCESS": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": False,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "XXcontactsXX": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "CONTACTS": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "XXidXX": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "ID": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get(None, ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", None),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get(""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("XXidXX", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("ID", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", "XXXX"),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "XXemailXX": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "EMAIL": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get(None, ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", None),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get(""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("XXemailXX", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("EMAIL", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", "XXXX"),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "XXfirst_nameXX": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "FIRST_NAME": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get(None, ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", None),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get(""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("XXfirst_nameXX", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("FIRST_NAME", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", "XXXX"),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "XXlast_nameXX": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "LAST_NAME": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get(None, ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", None),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get(""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("XXlast_nameXX", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("LAST_NAME", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", "XXXX"),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "XXcreated_atXX": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "CREATED_AT": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get(None, ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", None),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get(""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("XXcreated_atXX", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("CREATED_AT", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", "XXXX"),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "XXupdated_atXX": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "UPDATED_AT": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get(None, ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", None),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get(""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("XXupdated_atXX", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("UPDATED_AT", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", "XXXX"),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "XXtotalXX": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "TOTAL": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "XXcontact_countXX": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "CONTACT_COUNT": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get(None, 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", None),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get(0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", ),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("XXcontact_countXX", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("CONTACT_COUNT", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 1),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = None
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() and {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = None
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get(None, [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", None)
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get([])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", )
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("XXerrorsXX", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("ERRORS", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = None
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(None, f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", None) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", ) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[1].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("XXmessageXX", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("MESSAGE", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "XXsuccessXX": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "SUCCESS": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": True,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "XXerrorXX": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "ERROR": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "XXstatus_codeXX": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "STATUS_CODE": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_list_contacts__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista contactos de SendGrid.

        Args:
            params: Opcionalmente 'limit' y 'page'
        """
        limit = params.get("limit", 20)
        self._log_operation("list_contacts", f"limit={limit}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {"page_size": limit, "page_token": params.get("page_token", "")}

            response = self._http.get("/marketing/contacts", params=query_params)

            if response.ok:
                data = response.json() or {}
                contacts = data.get("result", [])
                return {
                    "success": True,
                    "contacts": [
                        {
                            "id": c.get("id", ""),
                            "email": c.get("email", ""),
                            "first_name": c.get("first_name", ""),
                            "last_name": c.get("last_name", ""),
                            "created_at": c.get("created_at", ""),
                            "updated_at": c.get("updated_at", ""),
                        }
                        for c in contacts
                    ],
                    "total": len(contacts),
                    "contact_count": data.get("contact_count", 0),
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Unexpected error: {e}"}

    @_mutmut_mutated(mutants_xǁSendGridConnectorǁ_create_template__mutmut)
    def _create_template(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = None
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get(None, "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", None)
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", )
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("XXnameXX", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("NAME", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "XXXX")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"XXsuccessXX": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"SUCCESS": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": True, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "XXerrorXX": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "ERROR": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "XXParametro requerido: nameXX"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "PARAMETRO REQUERIDO: NAME"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation(None, f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation(f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", )

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("XXcreate_templateXX", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("CREATE_TEMPLATE", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": True, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connected. Call connect() first.XX"}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "connector not connected. call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED. CALL CONNECT() FIRST."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = None

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"XXnameXX": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"NAME": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "XXgenerationXX": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "GENERATION": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "XXdynamicXX"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "DYNAMIC"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = None

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post(None, json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=None)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post(json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", )

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("XX/templatesXX", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/TEMPLATES", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = None
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() and {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = None

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get(None, "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", None)

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", )

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("XXidXX", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("ID", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "XXXX")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") or template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get(None) and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("XXhtml_contentXX") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("HTML_CONTENT") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = None
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "XXnameXX": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "NAME": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "XXhtml_contentXX": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "HTML_CONTENT": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["XXhtml_contentXX"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["HTML_CONTENT"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "XXplain_contentXX": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "PLAIN_CONTENT": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get(None, ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", None),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get(""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("XXplain_contentXX", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("PLAIN_CONTENT", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", "XXXX"),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "XXsubjectXX": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "SUBJECT": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get(None, name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", None),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get(name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", ),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("XXsubjectXX", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("SUBJECT", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "XXactiveXX": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "ACTIVE": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 2,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = None
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        None,
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=None,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            None
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "XXsuccessXX": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "SUCCESS": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": False,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "XXtemplate_idXX": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "TEMPLATE_ID": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "XXnameXX": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "NAME": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = None
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() and {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = None
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get(None, [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", None)
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get([])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", )
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("XXerrorsXX", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("ERRORS", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = None
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(None, f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", None) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", ) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[1].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("XXmessageXX", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("MESSAGE", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "XXsuccessXX": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "SUCCESS": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": True,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "XXerrorXX": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "ERROR": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "XXstatus_codeXX": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "STATUS_CODE": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_create_template__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una plantilla de email en SendGrid.

        Args:
            params: Debe contener 'name' y 'html_content'
        """
        name = params.get("name", "")
        if not name:
            return {"success": False, "error": "Parametro requerido: name"}

        self._log_operation("create_template", f"name={name}")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            # Step 1: Create the template
            payload: dict[str, Any] = {"name": name, "generation": "dynamic"}

            response = self._http.post("/templates", json=payload)

            if response.ok:
                data = response.json() or {}
                template_id = data.get("id", "")

                # Step 2: Add a version to the template if html_content provided
                if params.get("html_content") and template_id:
                    version_payload: dict[str, Any] = {
                        "name": f"{name} - Version 1",
                        "html_content": params["html_content"],
                        "plain_content": params.get("plain_content", ""),
                        "subject": params.get("subject", name),
                        "active": 1,
                    }
                    version_response = self._http.post(
                        f"/templates/{template_id}/versions",
                        json=version_payload,
                    )
                    if not version_response.ok:
                        logger.warning(
                            f"SendGridConnector: template created but version failed: {version_response.status_code}"
                        )

                return {
                    "success": True,
                    "template_id": template_id,
                    "name": name,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Unexpected error: {e}"}

    @_mutmut_mutated(mutants_xǁSendGridConnectorǁ_get_email_stats__mutmut)
    def _get_email_stats(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation(None)

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("XXget_email_statsXX")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("GET_EMAIL_STATS")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": True, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "XXConnector not connected. Call connect() first.XX"}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "connector not connected. call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED. CALL CONNECT() FIRST."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = None
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get(None):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("XXstart_dateXX"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("START_DATE"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = None
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["XXstart_dateXX"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["START_DATE"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["XXstart_dateXX"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["START_DATE"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get(None):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("XXend_dateXX"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("END_DATE"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = None
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["XXend_dateXX"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["END_DATE"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["XXend_dateXX"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["END_DATE"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get(None):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("XXaggregated_byXX"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("AGGREGATED_BY"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = None

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["XXaggregated_byXX"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["AGGREGATED_BY"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["XXaggregated_byXX"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["AGGREGATED_BY"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = None

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get(None, params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=None)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get(params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", )

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("XX/statsXX", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/STATS", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = None
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() and []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = None
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = None
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "XXdateXX": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "DATE": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get(None, ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", None),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get(""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("XXdateXX", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("DATE", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", "XXXX"),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get(None, []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", None):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get([]):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", ):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("XXstatsXX", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("STATS", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = None
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get(None, {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", None)
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get({})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", )
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("XXmetricsXX", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("METRICS", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update(None)
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "XXdeliveredXX": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "DELIVERED": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get(None, 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", None),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get(0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", ),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("XXdeliveredXX", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("DELIVERED", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 1),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "XXopensXX": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "OPENS": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get(None, 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", None),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get(0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", ),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("XXopensXX", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("OPENS", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 1),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "XXclicksXX": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "CLICKS": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get(None, 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", None),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get(0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", ),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("XXclicksXX", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("CLICKS", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 1),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "XXbouncesXX": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "BOUNCES": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get(None, 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", None),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get(0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", ),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("XXbouncesXX", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("BOUNCES", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 1),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "XXspam_reportsXX": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "SPAM_REPORTS": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get(None, 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", None),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get(0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", ),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("XXspam_reportsXX", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("SPAM_REPORTS", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 1),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "XXblocksXX": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "BLOCKS": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get(None, 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", None),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get(0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", ),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("XXblocksXX", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("BLOCKS", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 1),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "XXrequestsXX": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "REQUESTS": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get(None, 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", None),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get(0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", ),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("XXrequestsXX", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("REQUESTS", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 1),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(None)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "XXsuccessXX": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "SUCCESS": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": False,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "XXstatsXX": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "STATS": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = None
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() and {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = None
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get(None, [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", None)
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get([])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", )
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("XXerrorsXX", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("ERRORS", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = None
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(None, f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", None) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get(f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", ) if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[1].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("XXmessageXX", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("MESSAGE", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "XXsuccessXX": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "SUCCESS": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": True,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "XXerrorXX": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "ERROR": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "XXstatus_codeXX": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "STATUS_CODE": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"XXsuccessXX": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"SUCCESS": False, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": True, "error": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "XXerrorXX": f"Unexpected error: {e}"}

    def xǁSendGridConnectorǁ_get_email_stats__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene estadisticas de envio de emails.

        Args:
            params: Opcionalmente 'start_date' y 'end_date'
        """
        self._log_operation("get_email_stats")

        if not self._http:
            return {"success": False, "error": "Connector not connected. Call connect() first."}

        try:
            query_params: dict[str, Any] = {}
            if params.get("start_date"):
                query_params["start_date"] = params["start_date"]
            if params.get("end_date"):
                query_params["end_date"] = params["end_date"]
            if params.get("aggregated_by"):
                query_params["aggregated_by"] = params["aggregated_by"]

            response = self._http.get("/stats", params=query_params)

            if response.ok:
                data = response.json() or []
                stats_list = []
                for entry in data:
                    stats_item = {
                        "date": entry.get("date", ""),
                    }
                    for stat in entry.get("stats", []):
                        metrics = stat.get("metrics", {})
                        stats_item.update({
                            "delivered": metrics.get("delivered", 0),
                            "opens": metrics.get("opens", 0),
                            "clicks": metrics.get("clicks", 0),
                            "bounces": metrics.get("bounces", 0),
                            "spam_reports": metrics.get("spam_reports", 0),
                            "blocks": metrics.get("blocks", 0),
                            "requests": metrics.get("requests", 0),
                        })
                    stats_list.append(stats_item)

                return {
                    "success": True,
                    "stats": stats_list,
                }
            else:
                error_data = response.json() or {}
                errors = error_data.get("errors", [])
                error_msg = errors[0].get("message", f"HTTP {response.status_code}") if errors else f"HTTP {response.status_code}"
                return {
                    "success": False,
                    "error": error_msg,
                    "status_code": response.status_code,
                }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except Exception as e:
            return {"success": False, "ERROR": f"Unexpected error: {e}"}

mutants_xǁSendGridConnectorǁ__init____mutmut['_mutmut_orig'] = SendGridConnector.xǁSendGridConnectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁSendGridConnectorǁ__init____mutmut['xǁSendGridConnectorǁ__init____mutmut_1'] = SendGridConnector.xǁSendGridConnectorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁSendGridConnectorǁ__init____mutmut['xǁSendGridConnectorǁ__init____mutmut_2'] = SendGridConnector.xǁSendGridConnectorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁSendGridConnectorǁ