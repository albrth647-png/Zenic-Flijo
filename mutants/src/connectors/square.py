"""
Conector Square — Pagos y Punto de Venta via Square API
==========================================================

Permite procesar pagos, gestionar clientes, inventario
y transacciones via la API de Square.
"""

from __future__ import annotations

from typing import Any

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.http_client import HttpClient, HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁSquareConnectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁSquareConnectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSquareConnectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSquareConnectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSquareConnectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSquareConnectorǁ_create_payment__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSquareConnectorǁ_list_payments__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSquareConnectorǁ_create_customer__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSquareConnectorǁ_list_customers__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSquareConnectorǁ_create_refund__mutmut: MutantDict = {}  # type: ignore
mutants_xǁSquareConnectorǁ_list_catalog__mutmut: MutantDict = {}  # type: ignore


class SquareConnector(BaseConnector):
    """Conector para Square: pagos, clientes y punto de venta."""

    name = "square"
    version = "1.0.0"
    description = "Procesa pagos y gestiona clientes e inventario via Square"
    category = "finance_payments"
    icon = "shopping-cart"
    author = "Zenic-Flijo"

    @_mutmut_mutated(mutants_xǁSquareConnectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://connect.squareup.com/v2"
        self._http: HttpClient | None = None

    def xǁSquareConnectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://connect.squareup.com/v2"
        self._http: HttpClient | None = None

    def xǁSquareConnectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = None
        self._http: HttpClient | None = None

    def xǁSquareConnectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "XXhttps://connect.squareup.com/v2XX"
        self._http: HttpClient | None = None

    def xǁSquareConnectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "HTTPS://CONNECT.SQUAREUP.COM/V2"
        self._http: HttpClient | None = None

    def xǁSquareConnectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://connect.squareup.com/v2"
        self._http: HttpClient | None = ""

    @_mutmut_mutated(mutants_xǁSquareConnectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_orig(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_1(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider and not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_2(self) -> bool:
        """Establece conexion con la API de Square."""
        if self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_3(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_4(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error(None)
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_5(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("XXSquareConnector: Access Token no configuradoXX")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_6(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("squareconnector: access token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_7(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SQUARECONNECTOR: ACCESS TOKEN NO CONFIGURADO")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_8(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return True
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_9(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = None
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_10(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=None,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_11(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=None,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_12(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_13(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_14(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = None
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_15(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = None
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_16(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get(None, "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_17(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", None)
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_18(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_19(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", )
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_20(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("XXaccess_tokenXX", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_21(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("ACCESS_TOKEN", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_22(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "XXXX")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_23(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth(None, token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_24(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=None)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_25(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth(token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_26(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", )

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_27(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("XXBearerXX", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_28(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_29(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("BEARER", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_30(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header(None, "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_31(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", None)
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_32(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_33(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", )
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_34(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("XXSquare-VersionXX", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_35(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("square-version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_36(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("SQUARE-VERSION", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_37(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "XX2024-01-18XX")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_38(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = None
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_39(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get(None, "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_40(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", None)
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_41(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_42(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", )
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_43(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("XXenvironmentXX", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_44(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("ENVIRONMENT", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_45(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "XXproductionXX")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_46(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "PRODUCTION")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_47(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment != "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_48(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "XXsandboxXX":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_49(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "SANDBOX":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_50(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header(None, "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_51(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", None)

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_52(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_53(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", )

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_54(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("XXX-Square-EnvironmentXX", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_55(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("x-square-environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_56(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-SQUARE-ENVIRONMENT", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_57(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "XXsandboxXX")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_58(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "SANDBOX")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_59(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = None
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_60(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get(None)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_61(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("XX/locationsXX")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_62(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/LOCATIONS")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_63(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = None
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_64(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = False
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_65(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation(None, "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_66(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", None)
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_67(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_68(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", )
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_69(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("XXconnectXX", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_70(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("CONNECT", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_71(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "XXAccess Token configurado y validadoXX")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_72(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "access token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_73(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "ACCESS TOKEN CONFIGURADO Y VALIDADO")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_74(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return False
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_75(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = None
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_76(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = False
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_77(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation(None, f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_78(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", None)
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_79(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation(f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_80(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", )
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_81(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("XXconnectXX", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_82(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("CONNECT", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_83(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return False
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_84(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(None)
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_85(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = None
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_86(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=None,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_87(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=None,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_88(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_89(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_90(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = None
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_91(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = None
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_92(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get(None, "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_93(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", None)
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_94(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_95(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", )
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_96(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("XXaccess_tokenXX", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_97(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("ACCESS_TOKEN", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_98(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "XXXX")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_99(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth(None, token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_100(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=None)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_101(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth(token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_102(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", )
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_103(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("XXBearerXX", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_104(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_105(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("BEARER", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_106(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header(None, "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_107(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", None)
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_108(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_109(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", )
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_110(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("XXSquare-VersionXX", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_111(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("square-version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_112(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("SQUARE-VERSION", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_113(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "XX2024-01-18XX")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_114(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = None
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_115(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = False
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_116(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation(None, f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_117(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", None)
            return True

    def xǁSquareConnectorǁconnect__mutmut_118(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation(f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_119(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", )
            return True

    def xǁSquareConnectorǁconnect__mutmut_120(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("XXconnectXX", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_121(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("CONNECT", f"Access Token configurado (validacion fallo: {e})")
            return True

    def xǁSquareConnectorǁconnect__mutmut_122(self) -> bool:
        """Establece conexion con la API de Square."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("SquareConnector: Access Token no configurado")
            return False
        try:
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            # Square uses Bearer token (access_token)
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)

            # Set Square-specific headers
            self._http.set_header("Square-Version", "2024-01-18")
            environment = creds.get("environment", "production")
            if environment == "sandbox":
                self._http.set_header("X-Square-Environment", "sandbox")

            # Validate by listing locations (basic health check)
            resp = self._http.get("/locations")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Access Token configurado y validado")
                return True
            else:
                # Still connect even if validation fails
                self._connected = True
                self._log_operation("connect", f"Access Token configurado (validacion fallo: HTTP {resp.status_code})")
                return True
        except HTTPClientError as e:
            logger.warning(f"SquareConnector: error durante conexion - {e}")
            self._http = HttpClient(
                base_url=self._base_url,
                connector_name=self.name,
            )
            creds = self._auth_provider.get_credentials()
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._http.set_header("Square-Version", "2024-01-18")
            self._connected = True
            self._log_operation("connect", f"Access Token configurado (validacion fallo: {e})")
            return False

    @_mutmut_mutated(mutants_xǁSquareConnectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "XXcreate_paymentXX": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "CREATE_PAYMENT": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "XXlist_paymentsXX": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "LIST_PAYMENTS": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "XXcreate_customerXX": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "CREATE_CUSTOMER": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "XXlist_customersXX": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "LIST_CUSTOMERS": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "XXcreate_refundXX": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "CREATE_REFUND": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "XXlist_catalogXX": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "LIST_CATALOG": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = None
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁSquareConnectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector Square.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_payment": self._create_payment,
            "list_payments": self._list_payments,
            "create_customer": self._create_customer,
            "list_customers": self._list_customers,
            "create_refund": self._create_refund,
            "list_catalog": self._list_catalog,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁSquareConnectorǁvalidate__mutmut)
    def validate(self) -> bool:
        """Valida que el Access Token de Square este configurado."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁSquareConnectorǁvalidate__mutmut_orig(self) -> bool:
        """Valida que el Access Token de Square este configurado."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁSquareConnectorǁvalidate__mutmut_1(self) -> bool:
        """Valida que el Access Token de Square este configurado."""
        if self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁSquareConnectorǁvalidate__mutmut_2(self) -> bool:
        """Valida que el Access Token de Square este configurado."""
        if not self._auth_provider:
            return True
        return self._auth_provider.validate()

    @_mutmut_mutated(mutants_xǁSquareConnectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        """Cierra la conexion con Square."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁSquareConnectorǁdisconnect__mutmut_orig(self) -> bool:
        """Cierra la conexion con Square."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁSquareConnectorǁdisconnect__mutmut_1(self) -> bool:
        """Cierra la conexion con Square."""
        self._http = ""
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁSquareConnectorǁdisconnect__mutmut_2(self) -> bool:
        """Cierra la conexion con Square."""
        self._http = None
        self._connected = None
        self._log_operation("disconnect")
        return True

    def xǁSquareConnectorǁdisconnect__mutmut_3(self) -> bool:
        """Cierra la conexion con Square."""
        self._http = None
        self._connected = True
        self._log_operation("disconnect")
        return True

    def xǁSquareConnectorǁdisconnect__mutmut_4(self) -> bool:
        """Cierra la conexion con Square."""
        self._http = None
        self._connected = False
        self._log_operation(None)
        return True

    def xǁSquareConnectorǁdisconnect__mutmut_5(self) -> bool:
        """Cierra la conexion con Square."""
        self._http = None
        self._connected = False
        self._log_operation("XXdisconnectXX")
        return True

    def xǁSquareConnectorǁdisconnect__mutmut_6(self) -> bool:
        """Cierra la conexion con Square."""
        self._http = None
        self._connected = False
        self._log_operation("DISCONNECT")
        return True

    def xǁSquareConnectorǁdisconnect__mutmut_7(self) -> bool:
        """Cierra la conexion con Square."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return False

    @_mutmut_mutated(mutants_xǁSquareConnectorǁ_create_payment__mutmut)
    def _create_payment(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = None
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get(None, "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", None)
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", )
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("XXsource_idXX", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("SOURCE_ID", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "XXXX")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = None
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get(None, {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", None)
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get({})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", )
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("XXamount_moneyXX", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("AMOUNT_MONEY", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id and not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"SUCCESS": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": True, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "XXerrorXX": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "ERROR": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "XXParametros requeridos: source_id, amount_moneyXX"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: SOURCE_ID, AMOUNT_MONEY"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation(None, f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", None)

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation(f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", )

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("XXcreate_paymentXX", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("CREATE_PAYMENT", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = None
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "XXsource_idXX": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "SOURCE_ID": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "XXamount_moneyXX": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "AMOUNT_MONEY": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get(None):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("XXidempotency_keyXX"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("IDEMPOTENCY_KEY"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = None
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["XXidempotency_keyXX"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["IDEMPOTENCY_KEY"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["XXidempotency_keyXX"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["IDEMPOTENCY_KEY"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get(None):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("XXreference_idXX"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("REFERENCE_ID"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = None
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["XXreference_idXX"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["REFERENCE_ID"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["XXreference_idXX"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["REFERENCE_ID"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get(None):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("XXnoteXX"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("NOTE"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = None
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["XXnoteXX"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["NOTE"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["XXnoteXX"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["NOTE"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get(None) is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("XXautocompleteXX") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("AUTOCOMPLETE") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = None
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["XXautocompleteXX"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["AUTOCOMPLETE"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["XXautocompleteXX"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["AUTOCOMPLETE"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get(None):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("XXcustomer_idXX"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("CUSTOMER_ID"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = None
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["XXcustomer_idXX"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["CUSTOMER_ID"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["XXcustomer_idXX"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["CUSTOMER_ID"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get(None):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("XXlocation_idXX"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("LOCATION_ID"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = None
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["XXlocation_idXX"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["LOCATION_ID"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["XXlocation_idXX"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["LOCATION_ID"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get(None):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("XXapp_fee_moneyXX"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("APP_FEE_MONEY"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = None

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["XXapp_fee_moneyXX"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["APP_FEE_MONEY"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["XXapp_fee_moneyXX"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["APP_FEE_MONEY"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post(None, json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=None)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post(json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", )
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("XX/paymentsXX", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/PAYMENTS", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = None
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() and {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = None
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get(None, {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", None)
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get({})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", )
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("XXpaymentXX", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("PAYMENT", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "XXsuccessXX": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "SUCCESS": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": False,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "XXpayment_idXX": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "PAYMENT_ID": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get(None, ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", None),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get(""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("XXidXX", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("ID", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", "XXXX"),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "XXstatusXX": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "STATUS": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get(None, "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("XXstatusXX", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("STATUS", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "XXCOMPLETEDXX"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "completed"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = None
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = None
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get(None, [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", None)
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get([])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", )
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("XXerrorsXX", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("ERRORS", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get(None, str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", None) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get(str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", ) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[1].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("XXdetailXX", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("DETAIL", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(None)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(None)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": True, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_payment__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Procesa un pago via Square.

        Args:
            params: Debe contener 'source_id', 'amount_money' (dict con amount y currency)
                    y opcionalmente 'idempotency_key'
        """
        source_id = params.get("source_id", "")
        amount_money = params.get("amount_money", {})
        if not source_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: source_id, amount_money"}
        self._log_operation("create_payment", f"amount={amount_money}")

        payload: dict[str, Any] = {
            "source_id": source_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]
        if params.get("autocomplete") is not None:
            payload["autocomplete"] = params["autocomplete"]
        if params.get("customer_id"):
            payload["customer_id"] = params["customer_id"]
        if params.get("location_id"):
            payload["location_id"] = params["location_id"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/payments", json=payload)
            if resp.ok:
                data = resp.json() or {}
                payment = data.get("payment", {})
                return {
                    "success": True,
                    "payment_id": payment.get("id", ""),
                    "status": payment.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁSquareConnectorǁ_list_payments__mutmut)
    def _list_payments(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation(None)

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("XXlist_paymentsXX")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("LIST_PAYMENTS")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = None
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get(None):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("XXbegin_timeXX"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("BEGIN_TIME"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = None
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["XXbegin_timeXX"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["BEGIN_TIME"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["XXbegin_timeXX"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["BEGIN_TIME"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get(None):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("XXend_timeXX"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("END_TIME"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = None
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["XXend_timeXX"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["END_TIME"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["XXend_timeXX"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["END_TIME"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get(None):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("XXlimitXX"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("LIMIT"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = None
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["XXlimitXX"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["LIMIT"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["XXlimitXX"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["LIMIT"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get(None):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("XXcursorXX"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("CURSOR"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = None
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["XXcursorXX"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["CURSOR"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["XXcursorXX"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["CURSOR"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get(None):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("XXlocation_idXX"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("LOCATION_ID"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = None
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["XXlocation_idXX"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["LOCATION_ID"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["XXlocation_idXX"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["LOCATION_ID"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get(None):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("XXsort_orderXX"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("SORT_ORDER"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = None

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["XXsort_orderXX"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["SORT_ORDER"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["XXsort_orderXX"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["SORT_ORDER"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get(None, params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get(params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("XX/paymentsXX", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/PAYMENTS", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = None
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() and {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "XXsuccessXX": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "SUCCESS": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": False,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "XXpaymentsXX": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "PAYMENTS": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get(None, []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", None),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get([]),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", ),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("XXpaymentsXX", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("PAYMENTS", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "XXcursorXX": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "CURSOR": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get(None, None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get(None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("XXcursorXX", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("CURSOR", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = None
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = None
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get(None, [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", None)
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get([])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", )
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("XXerrorsXX", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("ERRORS", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get(None, str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", None) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get(str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", ) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[1].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("XXdetailXX", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("DETAIL", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(None)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(None)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": True, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_payments__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pagos de Square.

        Args:
            params: Opcionalmente 'begin_time', 'end_time', 'limit', 'cursor', 'location_id'
        """
        self._log_operation("list_payments")

        query_params: dict[str, Any] = {}
        if params.get("begin_time"):
            query_params["begin_time"] = params["begin_time"]
        if params.get("end_time"):
            query_params["end_time"] = params["end_time"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("location_id"):
            query_params["location_id"] = params["location_id"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/payments", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payments": data.get("payments", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁSquareConnectorǁ_create_customer__mutmut)
    def _create_customer(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = None
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get(None, "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", None)
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", )
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("XXemail_addressXX", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("EMAIL_ADDRESS", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "XXXX")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = None
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get(None, "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", None)
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", )
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("XXphone_numberXX", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("PHONE_NUMBER", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "XXXX")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email or not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"XXsuccessXX": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"SUCCESS": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": True, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "XXerrorXX": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "ERROR": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "XXRequiere email_address o phone_numberXX"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "REQUIERE EMAIL_ADDRESS O PHONE_NUMBER"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation(None, f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", None)

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation(f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", )

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("XXcreate_customerXX", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("CREATE_CUSTOMER", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email and phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = None
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = None
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["XXemail_addressXX"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["EMAIL_ADDRESS"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = None
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["XXphone_numberXX"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["PHONE_NUMBER"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get(None):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("XXgiven_nameXX"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("GIVEN_NAME"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = None
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["XXgiven_nameXX"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["GIVEN_NAME"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["XXgiven_nameXX"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["GIVEN_NAME"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get(None):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("XXfamily_nameXX"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("FAMILY_NAME"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = None
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["XXfamily_nameXX"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["FAMILY_NAME"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["XXfamily_nameXX"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["FAMILY_NAME"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get(None):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("XXnicknameXX"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("NICKNAME"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = None
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["XXnicknameXX"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["NICKNAME"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["XXnicknameXX"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["NICKNAME"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get(None):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("XXidempotency_keyXX"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("IDEMPOTENCY_KEY"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = None
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["XXidempotency_keyXX"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["IDEMPOTENCY_KEY"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["XXidempotency_keyXX"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["IDEMPOTENCY_KEY"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get(None):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("XXaddressXX"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("ADDRESS"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = None
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["XXaddressXX"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["ADDRESS"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["XXaddressXX"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["ADDRESS"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get(None):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("XXbirthdayXX"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("BIRTHDAY"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = None
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["XXbirthdayXX"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["BIRTHDAY"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["XXbirthdayXX"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["BIRTHDAY"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get(None):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("XXcompany_nameXX"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("COMPANY_NAME"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = None
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["XXcompany_nameXX"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["COMPANY_NAME"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["XXcompany_nameXX"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["COMPANY_NAME"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get(None):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("XXreference_idXX"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("REFERENCE_ID"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = None
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["XXreference_idXX"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["REFERENCE_ID"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["XXreference_idXX"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["REFERENCE_ID"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get(None):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("XXnoteXX"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("NOTE"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = None

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["XXnoteXX"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["NOTE"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["XXnoteXX"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["NOTE"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(None, json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=None)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", )
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("XX/customersXX", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/CUSTOMERS", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = None
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() and {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = None
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get(None, {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", None)
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get({})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", )
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("XXcustomerXX", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("CUSTOMER", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "XXsuccessXX": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "SUCCESS": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": False,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "XXcustomer_idXX": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "CUSTOMER_ID": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get(None, ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get(""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("XXidXX", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("ID", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", "XXXX"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = None
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = None
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get(None, [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", None)
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get([])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", )
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("XXerrorsXX", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("ERRORS", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get(None, str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", None) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get(str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", ) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[1].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("XXdetailXX", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("DETAIL", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(None)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(None)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": True, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_customer__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un cliente en Square.

        Args:
            params: Debe contener 'email_address' o 'phone_number' y opcionalmente
                    'given_name', 'family_name'
        """
        email = params.get("email_address", "")
        phone = params.get("phone_number", "")
        if not email and not phone:
            return {"success": False, "error": "Requiere email_address o phone_number"}
        self._log_operation("create_customer", f"email={email or phone}")

        payload: dict[str, Any] = {}
        if email:
            payload["email_address"] = email
        if phone:
            payload["phone_number"] = phone
        if params.get("given_name"):
            payload["given_name"] = params["given_name"]
        if params.get("family_name"):
            payload["family_name"] = params["family_name"]
        if params.get("nickname"):
            payload["nickname"] = params["nickname"]
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("address"):
            payload["address"] = params["address"]
        if params.get("birthday"):
            payload["birthday"] = params["birthday"]
        if params.get("company_name"):
            payload["company_name"] = params["company_name"]
        if params.get("reference_id"):
            payload["reference_id"] = params["reference_id"]
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post("/customers", json=payload)
            if resp.ok:
                data = resp.json() or {}
                customer = data.get("customer", {})
                return {
                    "success": True,
                    "customer_id": customer.get("id", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁSquareConnectorǁ_list_customers__mutmut)
    def _list_customers(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation(None)

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("XXlist_customersXX")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("LIST_CUSTOMERS")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = None
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get(None):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("XXcursorXX"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("CURSOR"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = None
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["XXcursorXX"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["CURSOR"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["XXcursorXX"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["CURSOR"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get(None):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("XXlimitXX"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("LIMIT"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = None
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["XXlimitXX"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["LIMIT"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["XXlimitXX"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["LIMIT"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get(None):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("XXsort_fieldXX"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("SORT_FIELD"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = None
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["XXsort_fieldXX"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["SORT_FIELD"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["XXsort_fieldXX"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["SORT_FIELD"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get(None):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("XXsort_orderXX"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("SORT_ORDER"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = None

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["XXsort_orderXX"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["SORT_ORDER"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["XXsort_orderXX"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["SORT_ORDER"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get(None, params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get(params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("XX/customersXX", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/CUSTOMERS", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = None
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() and {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "XXsuccessXX": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "SUCCESS": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": False,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "XXcustomersXX": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "CUSTOMERS": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get(None, []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", None),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get([]),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", ),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("XXcustomersXX", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("CUSTOMERS", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "XXcursorXX": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "CURSOR": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get(None, None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get(None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("XXcursorXX", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("CURSOR", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = None
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = None
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get(None, [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", None)
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get([])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", )
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("XXerrorsXX", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("ERRORS", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get(None, str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", None) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get(str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", ) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[1].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("XXdetailXX", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("DETAIL", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(None)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(None)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": True, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_customers__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista clientes de Square.

        Args:
            params: Opcionalmente 'cursor', 'limit', 'sort_field', 'sort_order'
        """
        self._log_operation("list_customers")

        query_params: dict[str, Any] = {}
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("sort_field"):
            query_params["sort_field"] = params["sort_field"]
        if params.get("sort_order"):
            query_params["sort_order"] = params["sort_order"]

        try:
            resp = self._http.get("/customers", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "customers": data.get("customers", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁSquareConnectorǁ_create_refund__mutmut)
    def _create_refund(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = None
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get(None, "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", None)
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", )
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("XXpayment_idXX", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("PAYMENT_ID", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "XXXX")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = None
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get(None, {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", None)
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get({})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", )
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("XXamount_moneyXX", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("AMOUNT_MONEY", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id and not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"SUCCESS": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": True, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "XXerrorXX": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "ERROR": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "XXParametros requeridos: payment_id, amount_moneyXX"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: PAYMENT_ID, AMOUNT_MONEY"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation(None, f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", None)

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation(f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", )

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("XXcreate_refundXX", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("CREATE_REFUND", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = None
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "XXpayment_idXX": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "PAYMENT_ID": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "XXamount_moneyXX": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "AMOUNT_MONEY": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get(None):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("XXidempotency_keyXX"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("IDEMPOTENCY_KEY"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = None
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["XXidempotency_keyXX"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["IDEMPOTENCY_KEY"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["XXidempotency_keyXX"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["IDEMPOTENCY_KEY"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get(None):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("XXreasonXX"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("REASON"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = None
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["XXreasonXX"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["REASON"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["XXreasonXX"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["REASON"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get(None):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("XXapp_fee_moneyXX"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("APP_FEE_MONEY"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = None

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["XXapp_fee_moneyXX"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["APP_FEE_MONEY"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["XXapp_fee_moneyXX"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["APP_FEE_MONEY"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post(None, json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=None)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post(json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", )
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("XX/refundsXX", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/REFUNDS", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = None
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() and {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = None
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get(None, {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", None)
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get({})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", )
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("XXrefundXX", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("REFUND", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "XXsuccessXX": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "SUCCESS": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": False,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "XXrefund_idXX": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "REFUND_ID": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get(None, ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", None),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get(""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("XXidXX", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("ID", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", "XXXX"),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "XXstatusXX": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "STATUS": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get(None, "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("XXstatusXX", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("STATUS", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "XXCOMPLETEDXX"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "completed"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = None
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = None
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get(None, [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", None)
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get([])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", )
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("XXerrorsXX", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("ERRORS", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get(None, str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", None) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get(str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", ) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[1].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("XXdetailXX", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("DETAIL", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(None)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(None)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": True, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_create_refund__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un reembolso en Square.

        Args:
            params: Debe contener 'payment_id', 'amount_money' y opcionalmente 'reason'
        """
        payment_id = params.get("payment_id", "")
        amount_money = params.get("amount_money", {})
        if not payment_id or not amount_money:
            return {"success": False, "error": "Parametros requeridos: payment_id, amount_money"}
        self._log_operation("create_refund", f"payment={payment_id}")

        payload: dict[str, Any] = {
            "payment_id": payment_id,
            "amount_money": amount_money,
        }
        if params.get("idempotency_key"):
            payload["idempotency_key"] = params["idempotency_key"]
        if params.get("reason"):
            payload["reason"] = params["reason"]
        if params.get("app_fee_money"):
            payload["app_fee_money"] = params["app_fee_money"]

        try:
            resp = self._http.post("/refunds", json=payload)
            if resp.ok:
                data = resp.json() or {}
                refund = data.get("refund", {})
                return {
                    "success": True,
                    "refund_id": refund.get("id", ""),
                    "status": refund.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁSquareConnectorǁ_list_catalog__mutmut)
    def _list_catalog(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation(None)

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("XXlist_catalogXX")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("LIST_CATALOG")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = None
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get(None):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("XXtypesXX"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("TYPES"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = None
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["XXtypesXX"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["TYPES"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["XXtypesXX"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["TYPES"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get(None):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("XXlimitXX"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("LIMIT"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = None
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["XXlimitXX"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["LIMIT"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["XXlimitXX"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["LIMIT"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get(None):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("XXcursorXX"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("CURSOR"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = None

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["XXcursorXX"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["CURSOR"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["XXcursorXX"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["CURSOR"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get(None, params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get(params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("XX/catalog/listXX", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/CATALOG/LIST", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = None
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() and {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "XXsuccessXX": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "SUCCESS": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": False,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "XXobjectsXX": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "OBJECTS": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get(None, []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", None),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get([]),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", ),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("XXobjectsXX", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("OBJECTS", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "XXcursorXX": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "CURSOR": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get(None, None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get(None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("XXcursorXX", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("CURSOR", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = None
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = None
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get(None, [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", None)
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get([])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", )
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("XXerrorsXX", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("ERRORS", [])
                error_msg = errors[0].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get(None, str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", None) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get(str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("detail", ) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[1].get("detail", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("XXdetailXX", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "objects": data.get("objects", []),
                    "cursor": data.get("cursor", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                errors = error_body.get("errors", [])
                error_msg = errors[0].get("DETAIL", str(resp.body)) if errors else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {error_msg}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁSquareConnectorǁ_list_catalog__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos del catalogo de Square.

        Args:
            params: Opcionalmente 'types', 'limit', 'cursor'
        """
        self._log_operation("list_catalog")

        query_params: dict[str, Any] = {}
        if params.get("types"):
            query_params["types"] = params["types"]
        if params.get("limit"):
            query_params["limit"] = params["limit"]
        if params.get("cursor"):
            query_params["cursor"] = params["cursor"]

        try:
            resp = self._http.get("/catalog/list", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    