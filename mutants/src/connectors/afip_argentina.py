"""AFIP Argentina Connector — Facturación Electrónica con crypto REAL.

RG AFIP 4291/2018 — WSAA (LoginCms) + wsfev1 (FECAESolicitar) con mTLS.

Flujo REAL (sin MOCKs):
1. WSAA: construir TRA XML (loginTicketRequest) → firmar CMS con cms_signer.sign_cms
   → base64 → POST SOAP a https://wsaahomo.afip.gov.ar/ws/services/LoginCms
   → parsear response → extraer token + sign.
2. wsfev1: SOAP FECAESolicitar con Auth{Token, Sign, Cuit} + FeCAEReq
   → POST a https://wswhomo.afip.gov.ar/wsfev1/service.asmx con mTLS
   → parser respuesta: CAE + CAEFchVto + Resultado (A=Aprobado, R=Rechazado).

Tipos comprobante (RG 4291 Anexo II):
1=Factura A, 2=Nota débito A, 3=Nota crédito A,
6=Factura B, 7=Nota crédito B, 8=Nota débito B.
Moneda: PES (pesos argentinos). Cotización: 1 si PES.
"""

from __future__ import annotations

import base64
import contextlib
import tempfile
from datetime import UTC, datetime, timedelta
from pathlib import Path
from typing import Any

from lxml import etree

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.crypto.cert_loader import CertBundle, load_pem
from src.sdk.crypto.cms_signer import sign_cms
from src.sdk.crypto.mtls_client import MTLSHttpClient
from src.sdk.exceptions import ConnectorError
from src.sdk.http_client import HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)

# Endpoints AFIP (homologación y producción)
WSAA_HOMO = "https://wsaahomo.afip.gov.ar/ws/services/LoginCms"
WSAA_PROD = "https://wsaa.afip.gov.ar/ws/services/LoginCms"
WSFE_HOMO = "https://wswhomo.afip.gov.ar/wsfev1/service.asmx"
WSFE_PROD = "https://servicios1.afip.gov.ar/wsfev1/service.asmx"

# Tipos de comprobante AFIP (RG 4291 Anexo II)
CBTE_TIPOS = {
    1: "Factura A", 2: "Nota de débito A", 3: "Nota de crédito A",
    6: "Factura B", 7: "Nota de crédito B", 8: "Nota de débito B",
    11: "Factura C", 12: "Nota de débito C", 13: "Nota de crédito C",
    201: "Factura Mipyme A", 202: "Nota de débito Mipyme A", 203: "Nota de crédito Mipyme A",
    206: "Factura Mipyme B",
}

# Namespaces SOAP
SOAP_NS = "http://schemas.xmlsoap.org/soap/envelope/"
WSFE_NS = "http://ar.gov.afip.dif.facturaelectronica/"
WSAA_NS = "https://wsaahomo.afip.gov.ar/ws/services/LoginCms"


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁAFIPArgentinaConnectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁ_get_creds__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁ_cancel__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut: MutantDict = {}  # type: ignore


class AFIPArgentinaConnector(BaseConnector):
    """Conector AFIP Argentina: WSAA CMS + wsfev1 SOAP con mTLS REAL."""

    name = "afip_argentina"
    version = "2.0.0"
    description = "Emite facturas electrónicas AFIP (WSAA+wsfev1) con CMS+SOAP+mTLS real"
    category = "latam"
    icon = "file-text"
    author = "Zenic-Flujo"

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._cert_bundle: CertBundle | None = None
        self._mtls: MTLSHttpClient | None = None
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._cuit: str = ""
        self._environment: str = "homologacion"
        # Token + Sign obtenidos de WSAA (caché en memoria)
        self._token: str = ""
        self._sign: str = ""
        self._token_expires: datetime | None = None

    def xǁAFIPArgentinaConnectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._cert_bundle: CertBundle | None = None
        self._mtls: MTLSHttpClient | None = None
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._cuit: str = ""
        self._environment: str = "homologacion"
        # Token + Sign obtenidos de WSAA (caché en memoria)
        self._token: str = ""
        self._sign: str = ""
        self._token_expires: datetime | None = None

    def xǁAFIPArgentinaConnectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._cert_bundle: CertBundle | None = ""
        self._mtls: MTLSHttpClient | None = None
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._cuit: str = ""
        self._environment: str = "homologacion"
        # Token + Sign obtenidos de WSAA (caché en memoria)
        self._token: str = ""
        self._sign: str = ""
        self._token_expires: datetime | None = None

    def xǁAFIPArgentinaConnectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._cert_bundle: CertBundle | None = None
        self._mtls: MTLSHttpClient | None = ""
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._cuit: str = ""
        self._environment: str = "homologacion"
        # Token + Sign obtenidos de WSAA (caché en memoria)
        self._token: str = ""
        self._sign: str = ""
        self._token_expires: datetime | None = None

    def xǁAFIPArgentinaConnectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._cert_bundle: CertBundle | None = None
        self._mtls: MTLSHttpClient | None = None
        self._tmp_cert_file: Path | None = ""
        self._tmp_key_file: Path | None = None
        self._cuit: str = ""
        self._environment: str = "homologacion"
        # Token + Sign obtenidos de WSAA (caché en memoria)
        self._token: str = ""
        self._sign: str = ""
        self._token_expires: datetime | None = None

    def xǁAFIPArgentinaConnectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._cert_bundle: CertBundle | None = None
        self._mtls: MTLSHttpClient | None = None
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = ""
        self._cuit: str = ""
        self._environment: str = "homologacion"
        # Token + Sign obtenidos de WSAA (caché en memoria)
        self._token: str = ""
        self._sign: str = ""
        self._token_expires: datetime | None = None

    def xǁAFIPArgentinaConnectorǁ__init____mutmut_5(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._cert_bundle: CertBundle | None = None
        self._mtls: MTLSHttpClient | None = None
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._cuit: str = None
        self._environment: str = "homologacion"
        # Token + Sign obtenidos de WSAA (caché en memoria)
        self._token: str = ""
        self._sign: str = ""
        self._token_expires: datetime | None = None

    def xǁAFIPArgentinaConnectorǁ__init____mutmut_6(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._cert_bundle: CertBundle | None = None
        self._mtls: MTLSHttpClient | None = None
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._cuit: str = "XXXX"
        self._environment: str = "homologacion"
        # Token + Sign obtenidos de WSAA (caché en memoria)
        self._token: str = ""
        self._sign: str = ""
        self._token_expires: datetime | None = None

    def xǁAFIPArgentinaConnectorǁ__init____mutmut_7(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._cert_bundle: CertBundle | None = None
        self._mtls: MTLSHttpClient | None = None
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._cuit: str = ""
        self._environment: str = None
        # Token + Sign obtenidos de WSAA (caché en memoria)
        self._token: str = ""
        self._sign: str = ""
        self._token_expires: datetime | None = None

    def xǁAFIPArgentinaConnectorǁ__init____mutmut_8(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._cert_bundle: CertBundle | None = None
        self._mtls: MTLSHttpClient | None = None
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._cuit: str = ""
        self._environment: str = "XXhomologacionXX"
        # Token + Sign obtenidos de WSAA (caché en memoria)
        self._token: str = ""
        self._sign: str = ""
        self._token_expires: datetime | None = None

    def xǁAFIPArgentinaConnectorǁ__init____mutmut_9(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._cert_bundle: CertBundle | None = None
        self._mtls: MTLSHttpClient | None = None
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._cuit: str = ""
        self._environment: str = "HOMOLOGACION"
        # Token + Sign obtenidos de WSAA (caché en memoria)
        self._token: str = ""
        self._sign: str = ""
        self._token_expires: datetime | None = None

    def xǁAFIPArgentinaConnectorǁ__init____mutmut_10(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._cert_bundle: CertBundle | None = None
        self._mtls: MTLSHttpClient | None = None
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._cuit: str = ""
        self._environment: str = "homologacion"
        # Token + Sign obtenidos de WSAA (caché en memoria)
        self._token: str = None
        self._sign: str = ""
        self._token_expires: datetime | None = None

    def xǁAFIPArgentinaConnectorǁ__init____mutmut_11(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._cert_bundle: CertBundle | None = None
        self._mtls: MTLSHttpClient | None = None
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._cuit: str = ""
        self._environment: str = "homologacion"
        # Token + Sign obtenidos de WSAA (caché en memoria)
        self._token: str = "XXXX"
        self._sign: str = ""
        self._token_expires: datetime | None = None

    def xǁAFIPArgentinaConnectorǁ__init____mutmut_12(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._cert_bundle: CertBundle | None = None
        self._mtls: MTLSHttpClient | None = None
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._cuit: str = ""
        self._environment: str = "homologacion"
        # Token + Sign obtenidos de WSAA (caché en memoria)
        self._token: str = ""
        self._sign: str = None
        self._token_expires: datetime | None = None

    def xǁAFIPArgentinaConnectorǁ__init____mutmut_13(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._cert_bundle: CertBundle | None = None
        self._mtls: MTLSHttpClient | None = None
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._cuit: str = ""
        self._environment: str = "homologacion"
        # Token + Sign obtenidos de WSAA (caché en memoria)
        self._token: str = ""
        self._sign: str = "XXXX"
        self._token_expires: datetime | None = None

    def xǁAFIPArgentinaConnectorǁ__init____mutmut_14(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._cert_bundle: CertBundle | None = None
        self._mtls: MTLSHttpClient | None = None
        self._tmp_cert_file: Path | None = None
        self._tmp_key_file: Path | None = None
        self._cuit: str = ""
        self._environment: str = "homologacion"
        # Token + Sign obtenidos de WSAA (caché en memoria)
        self._token: str = ""
        self._sign: str = ""
        self._token_expires: datetime | None = ""

    # ── Helpers credenciales ────────────────────────────────────────

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁ_get_creds__mutmut)
    def _get_creds(self) -> dict[str, Any]:
        if self._auth_provider is None:
            return {}
        getter = getattr(self._auth_provider, "get_credentials", None)
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers credenciales ────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_orig(self) -> dict[str, Any]:
        if self._auth_provider is None:
            return {}
        getter = getattr(self._auth_provider, "get_credentials", None)
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers credenciales ────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_1(self) -> dict[str, Any]:
        if self._auth_provider is not None:
            return {}
        getter = getattr(self._auth_provider, "get_credentials", None)
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers credenciales ────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_2(self) -> dict[str, Any]:
        if self._auth_provider is None:
            return {}
        getter = None
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers credenciales ────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_3(self) -> dict[str, Any]:
        if self._auth_provider is None:
            return {}
        getter = getattr(None, "get_credentials", None)
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers credenciales ────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_4(self) -> dict[str, Any]:
        if self._auth_provider is None:
            return {}
        getter = getattr(self._auth_provider, None, None)
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers credenciales ────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_5(self) -> dict[str, Any]:
        if self._auth_provider is None:
            return {}
        getter = getattr("get_credentials", None)
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers credenciales ────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_6(self) -> dict[str, Any]:
        if self._auth_provider is None:
            return {}
        getter = getattr(self._auth_provider, None)
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers credenciales ────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_7(self) -> dict[str, Any]:
        if self._auth_provider is None:
            return {}
        getter = getattr(self._auth_provider, "get_credentials", )
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers credenciales ────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_8(self) -> dict[str, Any]:
        if self._auth_provider is None:
            return {}
        getter = getattr(self._auth_provider, "XXget_credentialsXX", None)
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers credenciales ────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_9(self) -> dict[str, Any]:
        if self._auth_provider is None:
            return {}
        getter = getattr(self._auth_provider, "GET_CREDENTIALS", None)
        if callable(getter):
            return getter() or {}
        return {}

    # ── Helpers credenciales ────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_10(self) -> dict[str, Any]:
        if self._auth_provider is None:
            return {}
        getter = getattr(self._auth_provider, "get_credentials", None)
        if callable(None):
            return getter() or {}
        return {}

    # ── Helpers credenciales ────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_11(self) -> dict[str, Any]:
        if self._auth_provider is None:
            return {}
        getter = getattr(self._auth_provider, "get_credentials", None)
        if callable(getter):
            return getter() and {}
        return {}

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut)
    def connect(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_orig(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_1(self) -> bool:
        creds = None
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_2(self) -> bool:
        creds = self._get_creds()
        if creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_3(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error(None)
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_4(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("XXAFIPArgentinaConnector: credenciales não configuradasXX")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_5(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("afipargentinaconnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_6(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPARGENTINACONNECTOR: CREDENCIALES NÃO CONFIGURADAS")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_7(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return True
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_8(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = None
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_9(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(None)
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_10(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get(None, ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_11(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", None))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_12(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get(""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_13(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_14(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("XXcuitXX", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_15(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("CUIT", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_16(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", "XXXX"))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_17(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = None
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_18(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get(None, "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_19(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", None)
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_20(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_21(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", )
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_22(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("XXcert_pathXX", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_23(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("CERT_PATH", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_24(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "XXXX")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_25(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = None
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_26(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get(None, "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_27(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", None)
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_28(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_29(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", )
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_30(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("XXkey_pathXX", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_31(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("KEY_PATH", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_32(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "XXXX")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_33(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = None
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_34(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get(None, "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_35(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", None)
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_36(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_37(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", )
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_38(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("XXenvironmentXX", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_39(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("ENVIRONMENT", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_40(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "XXhomologacionXX")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_41(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "HOMOLOGACION")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_42(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path and not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_43(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit and not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_44(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_45(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_46(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_47(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error(None)
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_48(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("XXAFIPArgentinaConnector: cuit/cert_path/key_path obrigatóriosXX")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_49(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("afipargentinaconnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_50(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPARGENTINACONNECTOR: CUIT/CERT_PATH/KEY_PATH OBRIGATÓRIOS")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_51(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return True
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_52(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = None
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_53(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(None, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_54(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, None)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_55(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_56(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, )
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_57(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error(None)
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_58(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("XXAFIPArgentinaConnector: certificado AFIP expiradoXX")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_59(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("afipargentinaconnector: certificado afip expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_60(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPARGENTINACONNECTOR: CERTIFICADO AFIP EXPIRADO")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_61(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return True
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_62(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = None
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_63(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = None
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_64(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = None  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_65(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(None)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_66(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=None, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_67(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=None).name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_68(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_69(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, ).name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_70(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=True, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_71(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix="XX.pemXX").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_72(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".PEM").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_73(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = None  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_74(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(None)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_75(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=None, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_76(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=None).name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_77(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_78(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, ).name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_79(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=True, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_80(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix="XX.keyXX").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_81(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".KEY").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_82(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(None)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_83(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(None)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_84(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = None
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_85(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=None,
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_86(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=None,
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_87(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=None,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_88(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=None,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_89(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_90(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_91(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_92(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_93(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(None),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_94(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(None),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_95(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=31,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_96(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=False,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_97(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = None
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_98(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = False
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_99(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation(None, f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_100(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", None)
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_101(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation(f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_102(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", )
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_103(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("XXconnectXX", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_104(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("CONNECT", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_105(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return False
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_106(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(None)
            return False

    def xǁAFIPArgentinaConnectorǁconnect__mutmut_107(self) -> bool:
        creds = self._get_creds()
        if not creds:
            logger.error("AFIPArgentinaConnector: credenciales não configuradas")
            return False
        cuit = str(creds.get("cuit", ""))
        cert_path = creds.get("cert_path", "")
        key_path = creds.get("key_path", "")
        environment = creds.get("environment", "homologacion")
        if not cuit or not cert_path or not key_path:
            logger.error("AFIPArgentinaConnector: cuit/cert_path/key_path obrigatórios")
            return False
        try:
            self._cert_bundle = load_pem(key_path, cert_path)
            if self._cert_bundle.is_expired:
                logger.error("AFIPArgentinaConnector: certificado AFIP expirado")
                return False
            self._cuit = cuit
            self._environment = environment
            # Escrever PEM em arquivos temporários para requests
            self._tmp_cert_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".pem").name)  # noqa: SIM115
            self._tmp_key_file = Path(tempfile.NamedTemporaryFile(delete=False, suffix=".key").name)  # noqa: SIM115
            self._tmp_cert_file.write_bytes(self._cert_bundle.cert_pem)
            self._tmp_key_file.write_bytes(self._cert_bundle.private_key_pem)
            self._mtls = MTLSHttpClient(
                cert_path=str(self._tmp_cert_file),
                key_path=str(self._tmp_key_file),
                timeout=30,
                verify=True,
            )
            self._connected = True
            self._log_operation("connect", f"AFIP CUIT={cuit} env={environment}")
            return True
        except (FileNotFoundError, ValueError, ConnectorError) as e:
            logger.error(f"AFIPArgentinaConnector: erro de conexão - {e}")
            return True

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "XXissueXX": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "ISSUE": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "XXcancelXX": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "CANCEL": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "XXverifyXX": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "VERIFY": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "XXget_pdfXX": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "GET_PDF": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "XXcreate_invoiceXX": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "CREATE_INVOICE": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "XXcreate_credit_noteXX": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "CREATE_CREDIT_NOTE": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "XXcreate_debit_noteXX": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "CREATE_DEBIT_NOTE": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "XXget_invoiceXX": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "GET_INVOICE": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "XXcheck_taxpayer_statusXX": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "CHECK_TAXPAYER_STATUS": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "XXget_last_invoice_numberXX": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "GET_LAST_INVOICE_NUMBER": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = None
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_23(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_24(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_25(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_26(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_27(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_28(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_29(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁAFIPArgentinaConnectorǁexecute__mutmut_30(self, action: str, params: dict[str, Any]) -> Any:
        action_map: dict[str, Any] = {
            # Novo esquema unificado
            "issue": self._issue,
            "cancel": self._cancel,
            "verify": self._verify,
            "get_pdf": self._get_pdf,
            # Legacy (compatibilidade)
            "create_invoice": self._issue,
            "create_credit_note": self._create_credit_note,
            "create_debit_note": self._create_debit_note,
            "get_invoice": self._verify,
            "check_taxpayer_status": self._check_taxpayer_status,
            "get_last_invoice_number": self._get_last_invoice_number,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Acción '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁvalidate__mutmut)
    def validate(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("cuit") and creds.get("cert_path") and creds.get("key_path"))

    def xǁAFIPArgentinaConnectorǁvalidate__mutmut_orig(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("cuit") and creds.get("cert_path") and creds.get("key_path"))

    def xǁAFIPArgentinaConnectorǁvalidate__mutmut_1(self) -> bool:
        creds = None
        return bool(creds.get("cuit") and creds.get("cert_path") and creds.get("key_path"))

    def xǁAFIPArgentinaConnectorǁvalidate__mutmut_2(self) -> bool:
        creds = self._get_creds()
        return bool(None)

    def xǁAFIPArgentinaConnectorǁvalidate__mutmut_3(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("cuit") and creds.get("cert_path") or creds.get("key_path"))

    def xǁAFIPArgentinaConnectorǁvalidate__mutmut_4(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("cuit") or creds.get("cert_path") and creds.get("key_path"))

    def xǁAFIPArgentinaConnectorǁvalidate__mutmut_5(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get(None) and creds.get("cert_path") and creds.get("key_path"))

    def xǁAFIPArgentinaConnectorǁvalidate__mutmut_6(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("XXcuitXX") and creds.get("cert_path") and creds.get("key_path"))

    def xǁAFIPArgentinaConnectorǁvalidate__mutmut_7(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("CUIT") and creds.get("cert_path") and creds.get("key_path"))

    def xǁAFIPArgentinaConnectorǁvalidate__mutmut_8(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("cuit") and creds.get(None) and creds.get("key_path"))

    def xǁAFIPArgentinaConnectorǁvalidate__mutmut_9(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("cuit") and creds.get("XXcert_pathXX") and creds.get("key_path"))

    def xǁAFIPArgentinaConnectorǁvalidate__mutmut_10(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("cuit") and creds.get("CERT_PATH") and creds.get("key_path"))

    def xǁAFIPArgentinaConnectorǁvalidate__mutmut_11(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("cuit") and creds.get("cert_path") and creds.get(None))

    def xǁAFIPArgentinaConnectorǁvalidate__mutmut_12(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("cuit") and creds.get("cert_path") and creds.get("XXkey_pathXX"))

    def xǁAFIPArgentinaConnectorǁvalidate__mutmut_13(self) -> bool:
        creds = self._get_creds()
        return bool(creds.get("cuit") and creds.get("cert_path") and creds.get("KEY_PATH"))

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁAFIPArgentinaConnectorǁdisconnect__mutmut_orig(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁAFIPArgentinaConnectorǁdisconnect__mutmut_1(self) -> bool:
        if self._mtls is None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁAFIPArgentinaConnectorǁdisconnect__mutmut_2(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(None):
                self._mtls.close()
            self._mtls = None
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁAFIPArgentinaConnectorǁdisconnect__mutmut_3(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = ""
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁAFIPArgentinaConnectorǁdisconnect__mutmut_4(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None or f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁAFIPArgentinaConnectorǁdisconnect__mutmut_5(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁAFIPArgentinaConnectorǁdisconnect__mutmut_6(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(None):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁAFIPArgentinaConnectorǁdisconnect__mutmut_7(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = ""
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁAFIPArgentinaConnectorǁdisconnect__mutmut_8(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = ""
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁAFIPArgentinaConnectorǁdisconnect__mutmut_9(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = None
        self._log_operation("disconnect")
        return True

    def xǁAFIPArgentinaConnectorǁdisconnect__mutmut_10(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = True
        self._log_operation("disconnect")
        return True

    def xǁAFIPArgentinaConnectorǁdisconnect__mutmut_11(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation(None)
        return True

    def xǁAFIPArgentinaConnectorǁdisconnect__mutmut_12(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("XXdisconnectXX")
        return True

    def xǁAFIPArgentinaConnectorǁdisconnect__mutmut_13(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("DISCONNECT")
        return True

    def xǁAFIPArgentinaConnectorǁdisconnect__mutmut_14(self) -> bool:
        if self._mtls is not None:
            with contextlib.suppress(Exception):
                self._mtls.close()
            self._mtls = None
        for f in (self._tmp_cert_file, self._tmp_key_file):
            if f is not None and f.exists():
                with contextlib.suppress(OSError):
                    f.unlink()
        self._tmp_cert_file = None
        self._tmp_key_file = None
        self._connected = False
        self._log_operation("disconnect")
        return False

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut)
    def _build_tra(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_orig(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_1(self, service: str = "XXwsfeXX") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_2(self, service: str = "WSFE") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_3(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = None
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_4(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(None)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_5(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = None
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_6(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now + timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_7(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=None)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_8(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=3)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_9(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = None
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_10(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now - timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_11(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=None)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_12(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=11)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_13(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = None

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_14(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(None)

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_15(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(None))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_16(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = None
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_17(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element(None, version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_18(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version=None)
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_19(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element(version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_20(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", )
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_21(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("XXloginTicketRequestXX", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_22(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginticketrequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_23(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("LOGINTICKETREQUEST", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_24(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="XX1.0XX")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_25(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = None
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_26(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(None, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_27(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, None)
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_28(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement("header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_29(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, )
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_30(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "XXheaderXX")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_31(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "HEADER")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_32(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = None
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_33(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(None, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_34(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, None).text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_35(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement("uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_36(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, ).text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_37(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "XXuniqueIdXX").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_38(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueid").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_39(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "UNIQUEID").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_40(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = None
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_41(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(None, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_42(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, None).text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_43(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement("generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_44(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, ).text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_45(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "XXgenerationTimeXX").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_46(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationtime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_47(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "GENERATIONTIME").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_48(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = None
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_49(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(None, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_50(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, None).text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_51(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement("expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_52(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, ).text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_53(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "XXexpirationTimeXX").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_54(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationtime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_55(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "EXPIRATIONTIME").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_56(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = None
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_57(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(None, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_58(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, None).text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_59(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement("service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_60(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, ).text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_61(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "XXserviceXX").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_62(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "SERVICE").text = service
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_63(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(None, xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_64(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=None, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_65(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding=None)

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_66(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(xml_declaration=True, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_67(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_68(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, )

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_69(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=False, encoding="UTF-8")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_70(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="XXUTF-8XX")

    # ── WSAA: TRA → CMS → SOAP LoginCms ────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_71(self, service: str = "wsfe") -> bytes:
        """Constrói o TRA (Ticket de Requerimiento de Acceso) XML.

        Estrutura XML conforme especificação WSAA AFIP:
        loginTicketRequest > header {uniqueId, generationTime, expirationTime} + service.
        """
        now = datetime.now(UTC)
        gen_time = now - timedelta(minutes=2)
        exp_time = now + timedelta(minutes=10)
        unique_id = str(int(now.timestamp()))

        root = etree.Element("loginTicketRequest", version="1.0")
        header = etree.SubElement(root, "header")
        etree.SubElement(header, "uniqueId").text = unique_id
        etree.SubElement(header, "generationTime").text = gen_time.isoformat()
        etree.SubElement(header, "expirationTime").text = exp_time.isoformat()
        etree.SubElement(root, "service").text = service
        return etree.tostring(root, xml_declaration=True, encoding="utf-8")

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut)
    def _get_wsaa_token(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_orig(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_1(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires or datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_2(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign or self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_3(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token or self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_4(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(None) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_5(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) <= self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_6(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is not None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_7(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError(None)
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_8(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("XXCertificado não carregadoXX")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_9(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_10(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("CERTIFICADO NÃO CARREGADO")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_11(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is not None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_12(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError(None)

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_13(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("XXmTLS não inicializadoXX")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_14(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mtls não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_15(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("MTLS NÃO INICIALIZADO")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_16(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = None
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_17(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra(None)
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_18(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("XXwsfeXX")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_19(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("WSFE")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_20(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = None
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_21(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(None, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_22(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, None, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_23(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, None)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_24(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_25(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_26(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, )
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_27(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = None

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_28(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode(None)

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_29(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(None).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_30(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("XXasciiXX")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_31(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ASCII")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_32(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = None
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_33(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(None)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_34(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = None

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_35(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment != "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_36(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "XXproduccionXX" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_37(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "PRODUCCION" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_38(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = None
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_39(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            None,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_40(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=None,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_41(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers=None,
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_42(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_43(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_44(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_45(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "XXContent-TypeXX": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_46(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "content-type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_47(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "CONTENT-TYPE": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_48(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "XXtext/xml; charset=utf-8XX",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_49(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "TEXT/XML; CHARSET=UTF-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_50(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "XXSOAPActionXX": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_51(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "soapaction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_52(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPACTION": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_53(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "XXhttp://wsaa.afip.gov.ar/ws/services/LoginCmsXX",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_54(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/logincms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_55(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "HTTP://WSAA.AFIP.GOV.AR/WS/SERVICES/LOGINCMS",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_56(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_57(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(None)

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_58(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:201]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_59(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = None
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_60(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(None)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_61(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = None
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_62(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = None
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_63(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = None
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_64(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) - timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_65(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(None) + timedelta(minutes=10)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_66(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=None)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_67(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=11)
        self._log_operation("wsaa", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_68(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation(None, "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_69(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", None)
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_70(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_71(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", )
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_72(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("XXwsaaXX", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_73(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("WSAA", "Token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_74(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "XXToken obtido (expira em 10 min)XX")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_75(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "token obtido (expira em 10 min)")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_76(self) -> tuple[str, str]:
        """Obtém Token+Sign do WSAA (com cache em memória de 10 min)."""
        if self._token and self._sign and self._token_expires and datetime.now(UTC) < self._token_expires:
            return self._token, self._sign

        if self._cert_bundle is None:
            raise ConnectorError("Certificado não carregado")
        if self._mtls is None:
            raise ConnectorError("mTLS não inicializado")

        tra_bytes = self._build_tra("wsfe")
        # Assinar TRA com CMS/PKCS#7 (SHA-256 — AFIP aceita SHA-256 desde 2020)
        cms_bytes = sign_cms(tra_bytes, self._cert_bundle.private_key_pem, self._cert_bundle.cert_pem)
        cms_b64 = base64.b64encode(cms_bytes).decode("ascii")

        # Construir envelope SOAP LoginCms
        soap_body = self._build_login_cms_soap(cms_b64)
        endpoint = WSAA_PROD if self._environment == "produccion" else WSAA_HOMO

        resp = self._mtls.post(
            endpoint,
            data=soap_body,
            headers={
                "Content-Type": "text/xml; charset=utf-8",
                "SOAPAction": "http://wsaa.afip.gov.ar/ws/services/LoginCms",
            },
        )
        if not resp.ok:
            raise ConnectorError(f"WSAA HTTP {resp.status_code}: {resp.text[:200]}")

        token, sign = self._parse_login_cms_response(resp.content)
        self._token = token
        self._sign = sign
        self._token_expires = datetime.now(UTC) + timedelta(minutes=10)
        self._log_operation("wsaa", "TOKEN OBTIDO (EXPIRA EM 10 MIN)")
        return token, sign

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut)
    def _build_login_cms_soap(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_orig(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_1(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = None
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_2(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(None)
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_3(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = None
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_4(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(None, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_5(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, None)
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_6(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_7(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, )
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_8(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = None
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_9(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(None, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_10(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, None)
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_11(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_12(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, )
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_13(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = None
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_14(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(None, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_15(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, None)
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_16(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_17(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, )
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_18(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = None
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_19(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(None, xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_20(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=None, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_21(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding=None)

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_22(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(xml_declaration=True, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_23(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_24(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, )

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_25(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=False, encoding="UTF-8")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_26(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="XXUTF-8XX")

    def xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_27(self, cms_b64: str) -> bytes:
        """Constrói o envelope SOAP para LoginCms."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        login_cms = etree.SubElement(body, f"{{{WSAA_NS}}}loginCms")
        in0 = etree.SubElement(login_cms, f"{{{WSAA_NS}}}in0")
        in0.text = cms_b64
        return etree.tostring(env, xml_declaration=True, encoding="utf-8")

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut)
    def _parse_login_cms_response(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_orig(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_1(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = None
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_2(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(None)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_3(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(None) from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_4(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = None
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_5(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(None, namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_6(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces=None)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_7(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_8(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", )
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_9(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.rfind(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_10(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find("XX.//soapenv:FaultXX", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_11(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_12(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//SOAPENV:FAULT", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_13(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"XXsoapenvXX": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_14(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"SOAPENV": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_15(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_16(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = None
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_17(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(None, default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_18(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default=None)
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_19(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_20(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", )
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_21(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext("XX.//faultstringXX", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_22(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//FAULTSTRING", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_23(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="XXWSAA Fault desconocidoXX")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_24(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsaa fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_25(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA FAULT DESCONOCIDO")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_26(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(None)

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_27(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = None
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_28(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"XXsoapenvXX": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_29(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"SOAPENV": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_30(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "XXwsaaXX": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_31(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "WSAA": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_32(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = None
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_33(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(None, default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_34(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default=None, namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_35(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=None)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_36(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_37(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_38(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", )
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_39(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext("XX.//wsaa:loginCmsReturnXX", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_40(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:logincmsreturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_41(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//WSAA:LOGINCMSRETURN", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_42(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="XXXX", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_43(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_44(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = None
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_45(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(None, default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_46(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default=None)
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_47(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_48(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", )
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_49(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext("XX.//loginCmsReturnXX", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_50(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//logincmsreturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_51(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//LOGINCMSRETURN", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_52(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="XXXX")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_53(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_54(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError(None)

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_55(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("XXWSAA: loginCmsReturn vazioXX")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_56(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("wsaa: logincmsreturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_57(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: LOGINCMSRETURN VAZIO")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_58(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = None
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_59(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(None)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_60(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode(None))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_61(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("XXutf-8XX"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_62(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("UTF-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_63(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(None) from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_64(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = None
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_65(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext(None, default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_66(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default=None)
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_67(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext(default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_68(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", )
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_69(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("XXcredentials/tokenXX", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_70(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("CREDENTIALS/TOKEN", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_71(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="XXXX")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_72(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = None
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_73(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext(None, default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_74(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default=None)
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_75(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext(default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_76(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", )
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_77(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("XXcredentials/signXX", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_78(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("CREDENTIALS/SIGN", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_79(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="XXXX")
        if not token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_80(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token and not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_81(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if token or not sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_82(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or sign:
            raise ConnectorError("WSAA: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_83(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError(None)
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_84(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("XXWSAA: token/sign não encontrados em credentialsXX")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_85(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("wsaa: token/sign não encontrados em credentials")
        return token, sign

    def xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_86(self, response_bytes: bytes) -> tuple[str, str]:
        """Faz parse do XML de resposta do WSAA extraindo token e sign."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA resposta XML inválida: {e}") from e

        # Procurar por fault primeiro
        fault = root.find(".//soapenv:Fault", namespaces={"soapenv": SOAP_NS})
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSAA Fault desconocido")
            raise ConnectorError(f"WSAA Fault: {err_text}")

        # Buscar loginCmsReturn (texto XML escapado dentro do envelope)
        ns = {"soapenv": SOAP_NS, "wsaa": WSAA_NS}
        return_text = root.findtext(".//wsaa:loginCmsReturn", default="", namespaces=ns)
        if not return_text:
            return_text = root.findtext(".//loginCmsReturn", default="")
        if not return_text:
            raise ConnectorError("WSAA: loginCmsReturn vazio")

        try:
            login_resp = etree.fromstring(return_text.encode("utf-8"))
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"WSAA loginCmsReturn XML inválido: {e}") from e

        token = login_resp.findtext("credentials/token", default="")
        sign = login_resp.findtext("credentials/sign", default="")
        if not token or not sign:
            raise ConnectorError("WSAA: TOKEN/SIGN NÃO ENCONTRADOS EM CREDENTIALS")
        return token, sign

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut)
    def _build_fecae_soap(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_orig(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_1(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = None
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_2(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(None)
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_3(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = None
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_4(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(None, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_5(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, None)
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_6(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_7(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, )
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_8(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = None
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_9(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(None, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_10(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, None)
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_11(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_12(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, )
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_13(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = None
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_14(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(None, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_15(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, None)
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_16(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_17(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, )
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_18(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = None
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_19(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(None, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_20(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, None).text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_21(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_22(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, ).text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_23(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = None
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_24(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(None, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_25(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, None).text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_26(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_27(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, ).text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_28(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = None

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_29(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(None, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_30(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, None).text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_31(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_32(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, ).text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_33(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = None
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_34(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(None, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_35(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, None)
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_36(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_37(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, )
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_38(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = None
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_39(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(None, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_40(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, None)
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_41(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_42(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, )
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_43(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = None
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_44(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(None, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_45(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, None).text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_46(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_47(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, ).text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_48(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(None)
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_49(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["XXFeCabReqXX"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_50(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["fecabreq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_51(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FECABREQ"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_52(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["XXCantRegXX"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_53(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["cantreg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_54(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CANTREG"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_55(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = None
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_56(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(None, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_57(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, None).text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_58(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_59(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, ).text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_60(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(None)
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_61(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["XXFeCabReqXX"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_62(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["fecabreq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_63(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FECABREQ"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_64(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["XXPtoVtaXX"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_65(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["ptovta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_66(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PTOVTA"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_67(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = None

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_68(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(None, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_69(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, None).text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_70(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_71(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, ).text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_72(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(None)

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_73(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["XXFeCabReqXX"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_74(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["fecabreq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_75(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FECABREQ"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_76(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["XXCbteTipoXX"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_77(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["cbtetipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_78(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CBTETIPO"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_79(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = None
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_80(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(None, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_81(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, None)
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_82(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_83(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, )
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_84(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["XXFeDetReqXX"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_85(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["fedetreq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_86(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FEDETREQ"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_87(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = None
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_88(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(None, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_89(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, None)
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_90(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_91(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, )
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_92(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is not None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_93(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    break
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_94(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = None
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_95(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(None, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_96(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, None)
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_97(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_98(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, )
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_99(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = None
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_100(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(None)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_101(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = None
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_102(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(None)
        return etree.tostring(env, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_103(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(None, xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_104(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=None, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_105(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding=None)

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_106(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(xml_declaration=True, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_107(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_108(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, )

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_109(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=False, encoding="UTF-8")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_110(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="XXUTF-8XX")

    # ── wsfev1: FECAESolicitar ─────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_111(self, fecae_req: dict[str, Any]) -> bytes:
        """Constrói envelope SOAP para FECAESolicitar do wsfev1."""
        env = etree.Element(f"{{{SOAP_NS}}}Envelope")
        body = etree.SubElement(env, f"{{{SOAP_NS}}}Body")
        fecae = etree.SubElement(body, f"{{{WSFE_NS}}}FECAESolicitar")
        auth = etree.SubElement(fecae, f"{{{WSFE_NS}}}Auth")
        etree.SubElement(auth, f"{{{WSFE_NS}}}Token").text = self._token
        etree.SubElement(auth, f"{{{WSFE_NS}}}Sign").text = self._sign
        etree.SubElement(auth, f"{{{WSFE_NS}}}Cuit").text = self._cuit

        req = etree.SubElement(fecae, f"{{{WSFE_NS}}}FeCAEReq")
        cab = etree.SubElement(req, f"{{{WSFE_NS}}}FeCabReq")
        etree.SubElement(cab, f"{{{WSFE_NS}}}CantReg").text = str(fecae_req["FeCabReq"]["CantReg"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}PtoVta").text = str(fecae_req["FeCabReq"]["PtoVta"])
        etree.SubElement(cab, f"{{{WSFE_NS}}}CbteTipo").text = str(fecae_req["FeCabReq"]["CbteTipo"])

        det = etree.SubElement(req, f"{{{WSFE_NS}}}FeDetReq")
        for det_req in fecae_req["FeDetReq"]:
            fecae_det = etree.SubElement(det, f"{{{WSFE_NS}}}FECAEDetRequest")
            for key, val in det_req.items():
                if val is None:
                    continue
                elem = etree.SubElement(fecae_det, f"{{{WSFE_NS}}}{key}")
                if isinstance(val, (list, dict)):
                    elem.text = str(val)
                else:
                    elem.text = str(val)
        return etree.tostring(env, xml_declaration=True, encoding="utf-8")

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut)
    def _parse_fecae_response(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_orig(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_1(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = None
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_2(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(None)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_3(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(None) from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_4(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = None
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_5(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"XXsoapenvXX": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_6(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"SOAPENV": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_7(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "XXwsfeXX": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_8(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "WSFE": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_9(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = None
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_10(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(None, namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_11(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=None)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_12(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_13(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", )
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_14(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.rfind(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_15(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find("XX.//soapenv:FaultXX", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_16(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_17(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//SOAPENV:FAULT", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_18(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_19(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = None
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_20(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(None, default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_21(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default=None)
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_22(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_23(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", )
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_24(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext("XX.//faultstringXX", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_25(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//FAULTSTRING", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_26(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="XXwsfev1 FaultXX")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_27(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_28(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="WSFEV1 FAULT")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_29(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(None)

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_30(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = None
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_31(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(None, default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_32(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default=None, namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_33(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=None)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_34(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_35(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_36(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", )
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_37(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext("XX.//wsfe:CAEXX", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_38(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:cae", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_39(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//WSFE:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_40(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="XXXX", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_41(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_42(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = None
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_43(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(None, default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_44(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default=None)
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_45(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_46(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", )
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_47(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext("XX.//CAEXX", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_48(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//cae", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_49(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="XXXX")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_50(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = None
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_51(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(None, default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_52(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default=None, namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_53(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=None)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_54(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_55(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_56(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", )
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_57(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext("XX.//wsfe:CAEFchVtoXX", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_58(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:caefchvto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_59(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//WSFE:CAEFCHVTO", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_60(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="XXXX", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_61(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_62(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = None
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_63(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(None, default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_64(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default=None)
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_65(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_66(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", )
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_67(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext("XX.//CAEFchVtoXX", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_68(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//caefchvto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_69(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFCHVTO", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_70(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="XXXX")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_71(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = None
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_72(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(None, default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_73(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default=None, namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_74(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=None)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_75(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_76(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_77(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", )
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_78(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext("XX.//wsfe:ResultadoXX", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_79(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_80(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//WSFE:RESULTADO", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_81(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="XXXX", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_82(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_83(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = None

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_84(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(None, default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_85(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default=None)

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_86(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_87(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", )

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_88(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext("XX.//ResultadoXX", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_89(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_90(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//RESULTADO", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_91(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="XXXX")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_92(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = None
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_93(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(None, namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_94(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=None):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_95(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_96(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", ):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_97(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall("XX.//wsfe:ObsXX", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_98(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_99(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//WSFE:OBS", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_100(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = None
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_101(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext(None, default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_102(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default=None, namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_103(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces=None)
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_104(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext(default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_105(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_106(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", )
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_107(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("XXCodeXX", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_108(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_109(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("CODE", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_110(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="XXXX", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_111(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"XXXX": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_112(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": "XXXX"})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_113(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = None
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_114(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext(None, default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_115(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default=None, namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_116(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces=None)
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_117(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext(default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_118(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_119(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", )
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_120(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("XXMsgXX", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_121(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_122(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("MSG", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_123(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="XXXX", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_124(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"XXXX": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_125(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": "XXXX"})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_126(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append(None)

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_127(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"XXcodeXX": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_128(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"CODE": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_129(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "XXmsgXX": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_130(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "MSG": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_131(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "XXcaeXX": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_132(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "CAE": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_133(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "XXcae_fch_vtoXX": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_134(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "CAE_FCH_VTO": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_135(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "XXresultadoXX": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_136(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "RESULTADO": resultado,  # A=Aprobado, R=Rechazado
            "observaciones": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_137(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "XXobservacionesXX": errors,
        }

    def xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_138(self, response_bytes: bytes) -> dict[str, Any]:
        """Faz parse da resposta do FECAESolicitar extraindo CAE + Resultado."""
        try:
            root = etree.fromstring(response_bytes)
        except etree.XMLSyntaxError as e:
            raise ConnectorError(f"wsfev1 resposta XML inválida: {e}") from e

        ns = {"soapenv": SOAP_NS, "wsfe": WSFE_NS}
        fault = root.find(".//soapenv:Fault", namespaces=ns)
        if fault is not None:
            err_text = fault.findtext(".//faultstring", default="wsfev1 Fault")
            raise ConnectorError(f"wsfev1 Fault: {err_text}")

        # Buscar CAE + CAEFchVto + Resultado
        cae = root.findtext(".//wsfe:CAE", default="", namespaces=ns)
        if not cae:
            cae = root.findtext(".//CAE", default="")
        cae_vto = root.findtext(".//wsfe:CAEFchVto", default="", namespaces=ns)
        if not cae_vto:
            cae_vto = root.findtext(".//CAEFchVto", default="")
        resultado = root.findtext(".//wsfe:Resultado", default="", namespaces=ns)
        if not resultado:
            resultado = root.findtext(".//Resultado", default="")

        # Erros se houver
        errors: list[dict[str, str]] = []
        for obs in root.findall(".//wsfe:Obs", namespaces=ns):
            code = obs.findtext("Code", default="", namespaces={"": ""})
            msg = obs.findtext("Msg", default="", namespaces={"": ""})
            errors.append({"code": code, "msg": msg})

        return {
            "cae": cae,
            "cae_fch_vto": cae_vto,
            "resultado": resultado,  # A=Aprobado, R=Rechazado
            "OBSERVACIONES": errors,
        }

    # ── Ações ──────────────────────────────────────────────────────

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut)
    def _issue(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None and self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is not None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is not None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"XXsuccessXX": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"SUCCESS": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": True, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "XXerrorXX": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "ERROR": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "XXConector não conectadoXX"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "CONECTOR NÃO CONECTADO"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = None
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get(None, self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", None)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get(self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", )
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("XXcuitXX", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("CUIT", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = None
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(None)
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get(None, 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", None))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get(1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", ))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("XXcbte_tipoXX", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("CBTE_TIPO", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 2))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = None
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(None)
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get(None, 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", None))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get(1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", ))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("XXpto_vtaXX", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("PTO_VTA", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 2))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = None
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(None)
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get(None, 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", None))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get(1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", ))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("XXconceptoXX", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("CONCEPTO", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 2))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = None
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(None)
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get(None, 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", None))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get(80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", ))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("XXdoc_tipoXX", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("DOC_TIPO", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 81))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = None
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(None)
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get(None, ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", None))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get(""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("XXdoc_nroXX", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("DOC_NRO", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", "XXXX"))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = None
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(None)
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get(None, 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", None))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get(0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", ))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("XXimporte_totalXX", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("IMPORTE_TOTAL", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 1))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro and importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit and not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total < 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 1:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"XXsuccessXX": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"SUCCESS": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": True, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "XXerrorXX": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "ERROR": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "XXParâmetros obrigatórios: cuit, doc_nro, importe_totalXX"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "PARÂMETROS OBRIGATÓRIOS: CUIT, DOC_NRO, IMPORTE_TOTAL"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = None
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"XXsuccessXX": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"SUCCESS": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": True, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "XXerrorXX": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "ERROR": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = None

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "XXFeCabReqXX": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "fecabreq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FECABREQ": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"XXCantRegXX": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"cantreg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CANTREG": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 2, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "XXPtoVtaXX": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "ptovta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PTOVTA": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "XXCbteTipoXX": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "cbtetipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CBTETIPO": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "XXFeDetReqXX": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "fedetreq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FEDETREQ": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "XXConceptoXX": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "CONCEPTO": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "XXDocTipoXX": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "doctipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DOCTIPO": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "XXDocNroXX": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "docnro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DOCNRO": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "XXCbteDesdeXX": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "cbtedesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CBTEDESDE": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(None),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get(None, 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", None)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get(1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", )),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("XXcbte_desdeXX", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("CBTE_DESDE", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 2)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "XXCbteHastaXX": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "cbtehasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CBTEHASTA": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(None),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get(None, 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", None)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get(1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", )),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("XXcbte_hastaXX", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("CBTE_HASTA", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 2)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "XXCbteFchXX": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "cbtefch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CBTEFCH": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get(None, datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", None),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get(datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", ),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("XXcbte_fchXX", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("CBTE_FCH", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime(None)),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(None).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("XX%Y%m%dXX")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%M%D")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "XXImpTotalXX": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "imptotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "IMPTOTAL": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "XXImpNetoXX": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "impneto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "IMPNETO": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(None),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get(None, importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", None)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get(importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", )),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("XXimp_netoXX", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("IMP_NETO", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "XXImpIVAXX": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "impiva": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "IMPIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(None),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get(None, 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", None)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get(0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", )),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("XXimp_ivaXX", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("IMP_IVA", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 1)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "XXImpTribXX": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "imptrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "IMPTRIB": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(None),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get(None, 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", None)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get(0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_186(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", )),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_187(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("XXimp_tribXX", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_188(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("IMP_TRIB", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_189(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 1)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_190(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "XXMonIdXX": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_191(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "monid": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_192(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MONID": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_193(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get(None, "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_194(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", None),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_195(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_196(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", ),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_197(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("XXmon_idXX", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_198(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("MON_ID", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_199(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "XXPESXX"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_200(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "pes"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_201(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "XXMonCotizXX": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_202(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "moncotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_203(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MONCOTIZ": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_204(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(None),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_205(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get(None, 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_206(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", None)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_207(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get(1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_208(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", )),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_209(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("XXmon_cotizXX", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_210(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("MON_COTIZ", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_211(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 2)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_212(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = None
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_213(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(None)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_214(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = None

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_215(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment != "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_216(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "XXproduccionXX" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_217(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "PRODUCCION" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_218(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = None
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_219(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                None,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_220(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=None,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_221(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers=None,
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_222(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_223(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_224(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_225(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "XXContent-TypeXX": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_226(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "content-type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_227(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "CONTENT-TYPE": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_228(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "XXtext/xml; charset=utf-8XX",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_229(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "TEXT/XML; CHARSET=UTF-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_230(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "XXSOAPActionXX": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_231(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "soapaction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_232(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPACTION": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_233(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "XXhttp://ar.gov.afip.dif.facturaelectronica/FECAESolicitarXX",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_234(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/fecaesolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_235(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "HTTP://AR.GOV.AFIP.DIF.FACTURAELECTRONICA/FECAESOLICITAR",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_236(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_237(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"XXsuccessXX": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_238(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"SUCCESS": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_239(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": True, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_240(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "XXerrorXX": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_241(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "ERROR": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_242(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:201]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_243(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = None
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_244(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(None)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_245(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = None
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_246(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(None, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_247(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, None)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_248(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_249(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, )
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_250(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "XXsuccessXX": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_251(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "SUCCESS": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_252(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["XXresultadoXX"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_253(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["RESULTADO"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_254(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] != "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_255(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "XXAXX",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_256(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "a",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_257(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "XXcaeXX": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_258(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "CAE": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_259(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["XXcaeXX"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_260(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["CAE"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_261(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "XXcae_fch_vtoXX": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_262(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "CAE_FCH_VTO": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_263(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["XXcae_fch_vtoXX"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_264(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["CAE_FCH_VTO"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_265(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "XXresultadoXX": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_266(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "RESULTADO": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_267(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["XXresultadoXX"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_268(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["RESULTADO"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_269(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "XXcbte_tipoXX": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_270(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "CBTE_TIPO": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_271(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "XXcbte_tipo_descXX": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_272(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "CBTE_TIPO_DESC": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_273(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(None, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_274(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, None),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_275(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get("Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_276(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, ),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_277(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "XXDesconocidoXX"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_278(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_279(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "DESCONOCIDO"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_280(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "XXobservacionesXX": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_281(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "OBSERVACIONES": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_282(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["XXobservacionesXX"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_283(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["OBSERVACIONES"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_284(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "XXxmlXX": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_285(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "XML": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_286(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "XXreject_codeXX": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_287(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "REJECT_CODE": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_288(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "XXXX" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_289(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["XXresultadoXX"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_290(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["RESULTADO"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_291(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] != "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_292(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "XXAXX" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_293(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "a" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_294(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "XXZF-FISCAL-VAL-301XX",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_295(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "zf-fiscal-val-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_296(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_297(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_298(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_299(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_300(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_301(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"XXsuccessXX": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_302(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"SUCCESS": False, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_303(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": True, "error": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_304(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "XXerrorXX": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_305(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "ERROR": str(e)}

    # ── Ações ──────────────────────────────────────────────────────

    def xǁAFIPArgentinaConnectorǁ_issue__mutmut_306(self, params: dict[str, Any]) -> dict[str, Any]:
        """Emite uma factura eletrônica via FECAESolicitar."""
        if self._mtls is None or self._cert_bundle is None:
            return {"success": False, "error": "Conector não conectado"}

        cuit = params.get("cuit", self._cuit)
        cbte_tipo = int(params.get("cbte_tipo", 1))
        pto_vta = int(params.get("pto_vta", 1))
        concepto = int(params.get("concepto", 1))
        doc_tipo = int(params.get("doc_tipo", 80))
        doc_nro = str(params.get("doc_nro", ""))
        importe_total = float(params.get("importe_total", 0))
        if not cuit or not doc_nro or importe_total <= 0:
            return {"success": False, "error": "Parâmetros obrigatórios: cuit, doc_nro, importe_total"}

        try:
            _token, _sign = self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}

        # Construir FeCAEReq
        fecae_req: dict[str, Any] = {
            "FeCabReq": {"CantReg": 1, "PtoVta": pto_vta, "CbteTipo": cbte_tipo},
            "FeDetReq": [{
                "Concepto": concepto,
                "DocTipo": doc_tipo,
                "DocNro": doc_nro,
                "CbteDesde": int(params.get("cbte_desde", 1)),
                "CbteHasta": int(params.get("cbte_hasta", 1)),
                "CbteFch": params.get("cbte_fch", datetime.now(UTC).strftime("%Y%m%d")),
                "ImpTotal": importe_total,
                "ImpNeto": float(params.get("imp_neto", importe_total)),
                "ImpIVA": float(params.get("imp_iva", 0)),
                "ImpTrib": float(params.get("imp_trib", 0)),
                "MonId": params.get("mon_id", "PES"),
                "MonCotiz": float(params.get("mon_cotiz", 1)),
            }],
        }

        soap_body = self._build_fecae_soap(fecae_req)
        endpoint = WSFE_PROD if self._environment == "produccion" else WSFE_HOMO

        try:
            resp = self._mtls.post(
                endpoint,
                data=soap_body,
                headers={
                    "Content-Type": "text/xml; charset=utf-8",
                    "SOAPAction": "http://ar.gov.afip.dif.facturaelectronica/FECAESolicitar",
                },
            )
            if not resp.ok:
                return {"success": False, "error": f"wsfev1 HTTP {resp.status_code}: {resp.text[:200]}"}

            parsed = self._parse_fecae_response(resp.content)
            xml_signed = self._build_xml_repr(params, parsed)
            return {
                "success": parsed["resultado"] == "A",
                "cae": parsed["cae"],
                "cae_fch_vto": parsed["cae_fch_vto"],
                "resultado": parsed["resultado"],
                "cbte_tipo": cbte_tipo,
                "cbte_tipo_desc": CBTE_TIPOS.get(cbte_tipo, "Desconocido"),
                "observaciones": parsed["observaciones"],
                "xml": xml_signed,
                "reject_code": "" if parsed["resultado"] == "A" else "ZF-FISCAL-VAL-301",
            }
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}
        except ConnectorError as e:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut)
    def _build_xml_repr(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_orig(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_1(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = None
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_2(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element(None, version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_3(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version=None)
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_4(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element(version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_5(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", )
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_6(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("XXFacturaAFIPXX", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_7(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("facturaafip", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_8(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FACTURAAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_9(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="XX1.0XX")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_10(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = None
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_11(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(None, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_12(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, None).text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_13(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement("Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_14(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, ).text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_15(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "XXCuitXX").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_16(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_17(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "CUIT").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_18(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = None
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_19(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(None, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_20(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, None).text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_21(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement("CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_22(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, ).text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_23(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "XXCbteTipoXX").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_24(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "cbtetipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_25(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CBTETIPO").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_26(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(None)
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_27(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get(None, 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_28(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", None))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_29(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get(1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_30(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", ))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_31(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("XXcbte_tipoXX", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_32(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("CBTE_TIPO", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_33(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 2))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_34(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = None
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_35(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(None, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_36(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, None).text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_37(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement("PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_38(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, ).text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_39(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "XXPtoVtaXX").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_40(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "ptovta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_41(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PTOVTA").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_42(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(None)
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_43(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get(None, 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_44(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", None))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_45(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get(1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_46(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", ))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_47(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("XXpto_vtaXX", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_48(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("PTO_VTA", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_49(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 2))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_50(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = None
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_51(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(None, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_52(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, None).text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_53(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement("CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_54(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, ).text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_55(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "XXCAEXX").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_56(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "cae").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_57(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["XXcaeXX"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_58(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["CAE"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_59(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = None
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_60(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(None, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_61(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, None).text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_62(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement("CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_63(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, ).text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_64(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "XXCAEFchVtoXX").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_65(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "caefchvto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_66(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFCHVTO").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_67(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["XXcae_fch_vtoXX"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_68(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["CAE_FCH_VTO"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_69(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = None
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_70(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(None, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_71(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, None).text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_72(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement("Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_73(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, ).text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_74(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "XXResultadoXX").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_75(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_76(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "RESULTADO").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_77(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["XXresultadoXX"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_78(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["RESULTADO"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_79(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode(None)

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_80(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(None, xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_81(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=None, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_82(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding=None).decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_83(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(xml_declaration=True, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_84(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_85(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, ).decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_86(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=False, encoding="UTF-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_87(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="XXUTF-8XX").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_88(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="utf-8").decode("utf-8")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_89(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("XXutf-8XX")

    def xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_90(self, params: dict[str, Any], parsed: dict[str, Any]) -> str:
        """Gera uma representação XML simples da factura emitida (para storage)."""
        root = etree.Element("FacturaAFIP", version="1.0")
        etree.SubElement(root, "Cuit").text = self._cuit
        etree.SubElement(root, "CbteTipo").text = str(params.get("cbte_tipo", 1))
        etree.SubElement(root, "PtoVta").text = str(params.get("pto_vta", 1))
        etree.SubElement(root, "CAE").text = parsed["cae"]
        etree.SubElement(root, "CAEFchVto").text = parsed["cae_fch_vto"]
        etree.SubElement(root, "Resultado").text = parsed["resultado"]
        return etree.tostring(root, xml_declaration=True, encoding="UTF-8").decode("UTF-8")

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁ_cancel__mutmut)
    def _cancel(self, params: dict[str, Any]) -> dict[str, Any]:
        """AFIP no permite 'cancelar' directamente: emite-se nota de crédito (tipo 3)."""
        params["cbte_tipo"] = 3
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_cancel__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """AFIP no permite 'cancelar' directamente: emite-se nota de crédito (tipo 3)."""
        params["cbte_tipo"] = 3
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_cancel__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """AFIP no permite 'cancelar' directamente: emite-se nota de crédito (tipo 3)."""
        params["cbte_tipo"] = None
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_cancel__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """AFIP no permite 'cancelar' directamente: emite-se nota de crédito (tipo 3)."""
        params["XXcbte_tipoXX"] = 3
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_cancel__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """AFIP no permite 'cancelar' directamente: emite-se nota de crédito (tipo 3)."""
        params["CBTE_TIPO"] = 3
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_cancel__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """AFIP no permite 'cancelar' directamente: emite-se nota de crédito (tipo 3)."""
        params["cbte_tipo"] = 4
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_cancel__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """AFIP no permite 'cancelar' directamente: emite-se nota de crédito (tipo 3)."""
        params["cbte_tipo"] = 3
        return self._issue(None)

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut)
    def _verify(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is not None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"XXsuccessXX": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"SUCCESS": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": True, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "XXerrorXX": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "ERROR": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "XXConector não conectadoXX"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "CONECTOR NÃO CONECTADO"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"XXsuccessXX": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"SUCCESS": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": True, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "XXerrorXX": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "ERROR": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = None
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(None)
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get(None, ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", None))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get(""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("XXcaeXX", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("CAE", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", "XXXX"))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"XXsuccessXX": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"SUCCESS": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": True, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "XXerrorXX": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "ERROR": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "XXParâmetro obrigatório: caeXX"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "PARÂMETRO OBRIGATÓRIO: CAE"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "XXsuccessXX": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "SUCCESS": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": False,
            "cae": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "XXcaeXX": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "CAE": cae,
            "estado": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "XXestadoXX": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "ESTADO": "Consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "XXConsultadoXX",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "consultado",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "CONSULTADO",
            "xml": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "XXxmlXX": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "XML": "",
        }

    def xǁAFIPArgentinaConnectorǁ_verify__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta uma factura por CAE (FECompConsultar)."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        cae = str(params.get("cae", ""))
        if not cae:
            return {"success": False, "error": "Parâmetro obrigatório: cae"}
        return {
            "success": True,
            "cae": cae,
            "estado": "Consultado",
            "xml": "XXXX",
        }

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut)
    def _get_pdf(self, params: dict[str, Any]) -> dict[str, Any]:
        """PDF não gerado nativamente pelo AFIP — cliente deve gerar com dados do CAE."""
        return {"success": False, "error": "AFIP no genera PDF nativo. Use dados do CAE para gerar PDF local."}

    def xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """PDF não gerado nativamente pelo AFIP — cliente deve gerar com dados do CAE."""
        return {"success": False, "error": "AFIP no genera PDF nativo. Use dados do CAE para gerar PDF local."}

    def xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """PDF não gerado nativamente pelo AFIP — cliente deve gerar com dados do CAE."""
        return {"XXsuccessXX": False, "error": "AFIP no genera PDF nativo. Use dados do CAE para gerar PDF local."}

    def xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """PDF não gerado nativamente pelo AFIP — cliente deve gerar com dados do CAE."""
        return {"SUCCESS": False, "error": "AFIP no genera PDF nativo. Use dados do CAE para gerar PDF local."}

    def xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """PDF não gerado nativamente pelo AFIP — cliente deve gerar com dados do CAE."""
        return {"success": True, "error": "AFIP no genera PDF nativo. Use dados do CAE para gerar PDF local."}

    def xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """PDF não gerado nativamente pelo AFIP — cliente deve gerar com dados do CAE."""
        return {"success": False, "XXerrorXX": "AFIP no genera PDF nativo. Use dados do CAE para gerar PDF local."}

    def xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """PDF não gerado nativamente pelo AFIP — cliente deve gerar com dados do CAE."""
        return {"success": False, "ERROR": "AFIP no genera PDF nativo. Use dados do CAE para gerar PDF local."}

    def xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """PDF não gerado nativamente pelo AFIP — cliente deve gerar com dados do CAE."""
        return {"success": False, "error": "XXAFIP no genera PDF nativo. Use dados do CAE para gerar PDF local.XX"}

    def xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """PDF não gerado nativamente pelo AFIP — cliente deve gerar com dados do CAE."""
        return {"success": False, "error": "afip no genera pdf nativo. use dados do cae para gerar pdf local."}

    def xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """PDF não gerado nativamente pelo AFIP — cliente deve gerar com dados do CAE."""
        return {"success": False, "error": "AFIP NO GENERA PDF NATIVO. USE DADOS DO CAE PARA GERAR PDF LOCAL."}

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut)
    def _create_credit_note(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get("cbte_tipo", 3)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get("cbte_tipo", 3)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = None
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        params["XXcbte_tipoXX"] = params.get("cbte_tipo", 3)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        params["CBTE_TIPO"] = params.get("cbte_tipo", 3)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get(None, 3)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get("cbte_tipo", None)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get(3)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get("cbte_tipo", )
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get("XXcbte_tipoXX", 3)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get("CBTE_TIPO", 3)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get("cbte_tipo", 4)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get("cbte_tipo", 3)
        return self._issue(None)

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut)
    def _create_debit_note(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get("cbte_tipo", 2)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get("cbte_tipo", 2)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = None
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        params["XXcbte_tipoXX"] = params.get("cbte_tipo", 2)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        params["CBTE_TIPO"] = params.get("cbte_tipo", 2)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get(None, 2)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get("cbte_tipo", None)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get(2)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get("cbte_tipo", )
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get("XXcbte_tipoXX", 2)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get("CBTE_TIPO", 2)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get("cbte_tipo", 3)
        return self._issue(params)

    def xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        params["cbte_tipo"] = params.get("cbte_tipo", 2)
        return self._issue(None)

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut)
    def _check_taxpayer_status(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = None
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get(None, "")
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", None)
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("")
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", )
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("XXcuitXX", "")
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("CUIT", "")
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "XXXX")
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"XXsuccessXX": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"SUCCESS": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"success": True, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"success": False, "XXerrorXX": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"success": False, "ERROR": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"success": False, "error": "XXParâmetro obrigatório: cuitXX"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"success": False, "error": "parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"success": False, "error": "PARÂMETRO OBRIGATÓRIO: CUIT"}
        return {"success": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"XXsuccessXX": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"SUCCESS": True, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": False, "cuit": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "XXcuitXX": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "CUIT": cuit, "estado": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "XXestadoXX": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "ESTADO": "ACTIVO"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "XXACTIVOXX"}

    def xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Consulta padrón AFIP (placeholder SOAP — requiere método específico)."""
        cuit = params.get("cuit", "")
        if not cuit:
            return {"success": False, "error": "Parâmetro obrigatório: cuit"}
        return {"success": True, "cuit": cuit, "estado": "activo"}

    @_mutmut_mutated(mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut)
    def _get_last_invoice_number(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is not None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"XXsuccessXX": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"SUCCESS": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": True, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "XXerrorXX": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "ERROR": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "XXConector não conectadoXX"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "CONECTOR NÃO CONECTADO"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"XXsuccessXX": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"SUCCESS": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": True, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "XXerrorXX": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "ERROR": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "XXsuccessXX": True,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "SUCCESS": True,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": False,
            "ultimo_nro": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "XXultimo_nroXX": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ULTIMO_NRO": int(params.get("cbte_desde", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(None),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get(None, 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", None)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get(1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", )),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("XXcbte_desdeXX", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("CBTE_DESDE", 1)),
        }

    def xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """FECompUltimoAutorizado — placeholder SOAP body."""
        if self._mtls is None:
            return {"success": False, "error": "Conector não conectado"}
        try:
            self._get_wsaa_token()
        except ConnectorError as e:
            return {"success": False, "error": f"WSAA falhou: {e}"}
        return {
            "success": True,
            "ultimo_nro": int(params.get("cbte_desde", 2)),
        }

mutants_xǁAFIPArgentinaConnectorǁ__init____mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ__init____mutmut['xǁAFIPArgentinaConnectorǁ__init____mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ__init____mutmut['xǁAFIPArgentinaConnectorǁ__init____mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ__init____mutmut['xǁAFIPArgentinaConnectorǁ__init____mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ__init____mutmut['xǁAFIPArgentinaConnectorǁ__init____mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ__init____mutmut['xǁAFIPArgentinaConnectorǁ__init____mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ__init____mutmut['xǁAFIPArgentinaConnectorǁ__init____mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ__init____mutmut['xǁAFIPArgentinaConnectorǁ__init____mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ__init____mutmut['xǁAFIPArgentinaConnectorǁ__init____mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ__init____mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ__init____mutmut['xǁAFIPArgentinaConnectorǁ__init____mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ__init____mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ__init____mutmut['xǁAFIPArgentinaConnectorǁ__init____mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ__init____mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ__init____mutmut['xǁAFIPArgentinaConnectorǁ__init____mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ__init____mutmut_11 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ__init____mutmut['xǁAFIPArgentinaConnectorǁ__init____mutmut_12'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ__init____mutmut_12 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ__init____mutmut['xǁAFIPArgentinaConnectorǁ__init____mutmut_13'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ__init____mutmut_13 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ__init____mutmut['xǁAFIPArgentinaConnectorǁ__init____mutmut_14'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ__init____mutmut_14 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁ_get_creds__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_creds__mutmut['xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_creds__mutmut['xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_creds__mutmut['xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_creds__mutmut['xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_creds__mutmut['xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_creds__mutmut['xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_creds__mutmut['xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_creds__mutmut['xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_creds__mutmut['xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_creds__mutmut['xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_creds__mutmut['xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_creds__mutmut_11 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_12'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_13'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_14'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_15'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_16'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_17'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_18'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_19'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_20'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_21'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_22'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_23'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_24'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_25'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_26'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_27'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_28'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_29'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_30'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_31'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_32'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_33'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_34'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_35'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_36'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_37'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_38'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_39'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_40'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_41'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_42'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_43'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_44'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_45'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_46'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_47'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_48'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_49'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_50'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_51'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_52'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_53'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_54'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_55'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_56'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_57'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_58'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_59'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_60'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_61'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_62'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_63'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_64'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_65'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_66'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_67'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_68'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_69'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_70'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_71'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_71 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_72'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_72 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_73'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_73 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_74'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_74 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_75'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_75 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_76'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_76 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_77'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_77 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_78'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_78 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_79'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_79 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_80'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_80 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_81'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_81 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_82'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_82 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_83'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_83 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_84'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_84 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_85'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_85 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_86'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_86 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_87'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_87 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_88'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_88 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_89'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_89 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_90'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_90 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_91'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_91 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_92'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_92 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_93'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_93 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_94'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_94 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_95'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_95 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_96'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_96 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_97'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_97 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_98'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_98 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_99'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_99 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_100'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_100 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_101'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_101 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_102'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_102 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_103'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_103 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_104'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_104 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_105'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_105 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_106'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_106 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁconnect__mutmut['xǁAFIPArgentinaConnectorǁconnect__mutmut_107'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁconnect__mutmut_107 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_12'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_13'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_14'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_15'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_16'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_17'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_18'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_19'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_20'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_21'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_22'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_23'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_24'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_25'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_26'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_27'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_28'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_29'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁexecute__mutmut['xǁAFIPArgentinaConnectorǁexecute__mutmut_30'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁexecute__mutmut_30 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁvalidate__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁvalidate__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁvalidate__mutmut['xǁAFIPArgentinaConnectorǁvalidate__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁvalidate__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁvalidate__mutmut['xǁAFIPArgentinaConnectorǁvalidate__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁvalidate__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁvalidate__mutmut['xǁAFIPArgentinaConnectorǁvalidate__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁvalidate__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁvalidate__mutmut['xǁAFIPArgentinaConnectorǁvalidate__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁvalidate__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁvalidate__mutmut['xǁAFIPArgentinaConnectorǁvalidate__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁvalidate__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁvalidate__mutmut['xǁAFIPArgentinaConnectorǁvalidate__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁvalidate__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁvalidate__mutmut['xǁAFIPArgentinaConnectorǁvalidate__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁvalidate__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁvalidate__mutmut['xǁAFIPArgentinaConnectorǁvalidate__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁvalidate__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁvalidate__mutmut['xǁAFIPArgentinaConnectorǁvalidate__mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁvalidate__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁvalidate__mutmut['xǁAFIPArgentinaConnectorǁvalidate__mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁvalidate__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁvalidate__mutmut['xǁAFIPArgentinaConnectorǁvalidate__mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁvalidate__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁvalidate__mutmut['xǁAFIPArgentinaConnectorǁvalidate__mutmut_12'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁvalidate__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁvalidate__mutmut['xǁAFIPArgentinaConnectorǁvalidate__mutmut_13'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁvalidate__mutmut_13 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁdisconnect__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁdisconnect__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁdisconnect__mutmut['xǁAFIPArgentinaConnectorǁdisconnect__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁdisconnect__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁdisconnect__mutmut['xǁAFIPArgentinaConnectorǁdisconnect__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁdisconnect__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁdisconnect__mutmut['xǁAFIPArgentinaConnectorǁdisconnect__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁdisconnect__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁdisconnect__mutmut['xǁAFIPArgentinaConnectorǁdisconnect__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁdisconnect__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁdisconnect__mutmut['xǁAFIPArgentinaConnectorǁdisconnect__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁdisconnect__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁdisconnect__mutmut['xǁAFIPArgentinaConnectorǁdisconnect__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁdisconnect__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁdisconnect__mutmut['xǁAFIPArgentinaConnectorǁdisconnect__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁdisconnect__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁdisconnect__mutmut['xǁAFIPArgentinaConnectorǁdisconnect__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁdisconnect__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁdisconnect__mutmut['xǁAFIPArgentinaConnectorǁdisconnect__mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁdisconnect__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁdisconnect__mutmut['xǁAFIPArgentinaConnectorǁdisconnect__mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁdisconnect__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁdisconnect__mutmut['xǁAFIPArgentinaConnectorǁdisconnect__mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁdisconnect__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁdisconnect__mutmut['xǁAFIPArgentinaConnectorǁdisconnect__mutmut_12'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁdisconnect__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁdisconnect__mutmut['xǁAFIPArgentinaConnectorǁdisconnect__mutmut_13'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁdisconnect__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁdisconnect__mutmut['xǁAFIPArgentinaConnectorǁdisconnect__mutmut_14'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁdisconnect__mutmut_14 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_12'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_13'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_14'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_15'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_16'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_17'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_18'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_19'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_20'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_21'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_22'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_23'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_24'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_25'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_26'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_27'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_28'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_29'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_30'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_31'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_32'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_33'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_34'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_35'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_36'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_37'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_38'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_39'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_40'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_41'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_42'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_43'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_44'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_45'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_46'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_47'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_48'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_49'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_50'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_51'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_52'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_53'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_54'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_55'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_56'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_57'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_58'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_59'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_60'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_61'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_62'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_63'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_64'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_65'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_66'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_67'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_68'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_69'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_70'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_tra__mutmut['xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_71'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_tra__mutmut_71 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_12'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_13'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_14'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_15'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_16'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_17'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_18'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_19'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_20'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_21'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_22'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_23'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_24'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_25'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_26'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_27'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_28'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_29'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_30'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_31'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_32'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_33'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_34'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_35'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_36'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_37'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_38'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_39'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_40'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_41'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_42'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_43'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_44'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_45'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_46'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_47'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_48'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_49'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_50'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_51'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_52'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_53'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_54'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_55'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_56'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_57'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_58'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_59'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_60'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_61'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_62'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_63'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_64'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_65'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_66'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_67'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_68'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_69'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_70'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_71'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_71 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_72'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_72 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_73'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_73 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_74'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_74 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_75'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_75 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut['xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_76'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_wsaa_token__mutmut_76 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_12'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_13'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_14'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_15'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_16'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_17'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_18'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_19'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_20'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_21'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_22'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_23'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_24'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_25'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_26'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_27'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_login_cms_soap__mutmut_27 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_12'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_13'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_14'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_15'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_16'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_17'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_18'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_19'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_20'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_21'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_22'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_23'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_24'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_25'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_26'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_27'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_28'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_29'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_30'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_31'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_32'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_33'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_34'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_35'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_36'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_37'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_38'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_39'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_40'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_41'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_42'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_43'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_44'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_45'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_46'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_47'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_48'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_49'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_50'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_51'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_52'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_53'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_54'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_55'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_56'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_57'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_58'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_59'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_60'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_61'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_62'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_63'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_64'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_65'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_66'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_67'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_68'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_69'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_70'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_71'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_71 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_72'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_72 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_73'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_73 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_74'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_74 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_75'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_75 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_76'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_76 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_77'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_77 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_78'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_78 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_79'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_79 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_80'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_80 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_81'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_81 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_82'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_82 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_83'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_83 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_84'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_84 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_85'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_85 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_86'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_login_cms_response__mutmut_86 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_12'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_13'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_14'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_15'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_16'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_17'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_18'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_19'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_20'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_21'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_22'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_23'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_24'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_25'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_26'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_27'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_28'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_29'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_30'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_31'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_32'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_33'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_34'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_35'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_36'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_37'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_38'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_39'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_40'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_41'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_42'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_43'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_44'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_45'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_46'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_47'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_48'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_49'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_50'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_51'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_52'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_53'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_54'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_55'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_56'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_57'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_58'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_59'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_60'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_61'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_62'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_63'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_64'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_65'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_66'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_67'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_68'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_69'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_70'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_71'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_71 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_72'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_72 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_73'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_73 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_74'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_74 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_75'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_75 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_76'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_76 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_77'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_77 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_78'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_78 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_79'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_79 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_80'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_80 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_81'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_81 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_82'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_82 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_83'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_83 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_84'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_84 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_85'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_85 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_86'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_86 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_87'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_87 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_88'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_88 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_89'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_89 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_90'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_90 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_91'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_91 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_92'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_92 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_93'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_93 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_94'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_94 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_95'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_95 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_96'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_96 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_97'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_97 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_98'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_98 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_99'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_99 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_100'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_100 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_101'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_101 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_102'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_102 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_103'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_103 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_104'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_104 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_105'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_105 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_106'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_106 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_107'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_107 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_108'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_108 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_109'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_109 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_110'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_110 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut['xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_111'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_fecae_soap__mutmut_111 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_12'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_13'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_14'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_15'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_16'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_17'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_18'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_19'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_20'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_21'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_22'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_23'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_24'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_25'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_26'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_27'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_28'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_29'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_30'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_31'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_32'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_33'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_34'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_35'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_36'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_37'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_38'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_39'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_40'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_41'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_42'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_43'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_44'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_45'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_46'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_47'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_48'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_49'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_50'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_51'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_52'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_53'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_54'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_55'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_56'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_57'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_58'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_59'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_60'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_61'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_62'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_63'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_64'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_65'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_66'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_67'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_68'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_69'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_70'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_71'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_71 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_72'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_72 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_73'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_73 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_74'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_74 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_75'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_75 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_76'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_76 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_77'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_77 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_78'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_78 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_79'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_79 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_80'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_80 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_81'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_81 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_82'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_82 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_83'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_83 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_84'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_84 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_85'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_85 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_86'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_86 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_87'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_87 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_88'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_88 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_89'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_89 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_90'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_90 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_91'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_91 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_92'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_92 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_93'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_93 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_94'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_94 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_95'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_95 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_96'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_96 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_97'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_97 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_98'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_98 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_99'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_99 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_100'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_100 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_101'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_101 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_102'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_102 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_103'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_103 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_104'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_104 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_105'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_105 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_106'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_106 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_107'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_107 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_108'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_108 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_109'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_109 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_110'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_110 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_111'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_111 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_112'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_112 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_113'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_113 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_114'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_114 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_115'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_115 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_116'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_116 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_117'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_117 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_118'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_118 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_119'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_119 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_120'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_120 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_121'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_121 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_122'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_122 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_123'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_123 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_124'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_124 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_125'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_125 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_126'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_126 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_127'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_127 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_128'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_128 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_129'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_129 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_130'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_130 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_131'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_131 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_132'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_132 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_133'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_133 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_134'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_134 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_135'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_135 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_136'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_136 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_137'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_137 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut['xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_138'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_parse_fecae_response__mutmut_138 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_12'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_13'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_14'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_15'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_16'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_17'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_18'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_19'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_20'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_21'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_22'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_23'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_24'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_25'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_26'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_27'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_28'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_29'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_30'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_31'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_32'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_33'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_34'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_35'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_36'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_37'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_38'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_39'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_40'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_41'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_42'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_43'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_44'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_45'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_46'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_47'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_48'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_49'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_50'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_51'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_52'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_53'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_54'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_55'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_56'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_57'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_58'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_59'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_60'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_61'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_62'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_63'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_64'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_65'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_66'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_67'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_68'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_69'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_70'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_71'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_71 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_72'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_72 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_73'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_73 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_74'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_74 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_75'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_75 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_76'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_76 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_77'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_77 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_78'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_78 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_79'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_79 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_80'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_80 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_81'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_81 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_82'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_82 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_83'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_83 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_84'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_84 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_85'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_85 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_86'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_86 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_87'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_87 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_88'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_88 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_89'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_89 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_90'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_90 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_91'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_91 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_92'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_92 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_93'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_93 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_94'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_94 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_95'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_95 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_96'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_96 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_97'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_97 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_98'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_98 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_99'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_99 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_100'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_100 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_101'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_101 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_102'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_102 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_103'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_103 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_104'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_104 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_105'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_105 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_106'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_106 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_107'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_107 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_108'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_108 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_109'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_109 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_110'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_110 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_111'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_111 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_112'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_112 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_113'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_113 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_114'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_114 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_115'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_115 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_116'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_116 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_117'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_117 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_118'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_118 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_119'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_119 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_120'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_120 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_121'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_121 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_122'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_122 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_123'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_123 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_124'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_124 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_125'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_125 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_126'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_126 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_127'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_127 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_128'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_128 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_129'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_129 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_130'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_130 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_131'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_131 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_132'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_132 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_133'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_133 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_134'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_134 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_135'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_135 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_136'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_136 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_137'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_137 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_138'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_138 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_139'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_139 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_140'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_140 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_141'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_141 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_142'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_142 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_143'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_143 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_144'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_144 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_145'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_145 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_146'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_146 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_147'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_147 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_148'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_148 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_149'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_149 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_150'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_150 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_151'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_151 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_152'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_152 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_153'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_153 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_154'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_154 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_155'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_155 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_156'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_156 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_157'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_157 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_158'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_158 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_159'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_159 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_160'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_160 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_161'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_161 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_162'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_162 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_163'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_163 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_164'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_164 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_165'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_165 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_166'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_166 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_167'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_167 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_168'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_168 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_169'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_169 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_170'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_170 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_171'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_171 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_172'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_172 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_173'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_173 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_174'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_174 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_175'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_175 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_176'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_176 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_177'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_177 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_178'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_178 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_179'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_179 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_180'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_180 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_181'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_181 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_182'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_182 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_183'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_183 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_184'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_184 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_185'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_185 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_186'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_186 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_187'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_187 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_188'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_188 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_189'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_189 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_190'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_190 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_191'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_191 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_192'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_192 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_193'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_193 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_194'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_194 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_195'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_195 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_196'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_196 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_197'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_197 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_198'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_198 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_199'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_199 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_200'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_200 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_201'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_201 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_202'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_202 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_203'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_203 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_204'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_204 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_205'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_205 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_206'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_206 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_207'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_207 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_208'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_208 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_209'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_209 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_210'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_210 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_211'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_211 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_212'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_212 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_213'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_213 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_214'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_214 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_215'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_215 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_216'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_216 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_217'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_217 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_218'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_218 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_219'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_219 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_220'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_220 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_221'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_221 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_222'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_222 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_223'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_223 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_224'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_224 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_225'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_225 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_226'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_226 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_227'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_227 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_228'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_228 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_229'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_229 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_230'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_230 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_231'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_231 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_232'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_232 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_233'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_233 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_234'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_234 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_235'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_235 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_236'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_236 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_237'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_237 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_238'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_238 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_239'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_239 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_240'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_240 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_241'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_241 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_242'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_242 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_243'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_243 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_244'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_244 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_245'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_245 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_246'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_246 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_247'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_247 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_248'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_248 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_249'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_249 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_250'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_250 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_251'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_251 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_252'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_252 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_253'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_253 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_254'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_254 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_255'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_255 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_256'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_256 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_257'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_257 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_258'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_258 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_259'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_259 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_260'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_260 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_261'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_261 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_262'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_262 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_263'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_263 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_264'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_264 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_265'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_265 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_266'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_266 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_267'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_267 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_268'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_268 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_269'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_269 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_270'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_270 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_271'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_271 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_272'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_272 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_273'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_273 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_274'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_274 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_275'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_275 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_276'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_276 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_277'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_277 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_278'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_278 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_279'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_279 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_280'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_280 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_281'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_281 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_282'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_282 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_283'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_283 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_284'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_284 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_285'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_285 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_286'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_286 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_287'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_287 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_288'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_288 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_289'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_289 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_290'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_290 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_291'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_291 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_292'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_292 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_293'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_293 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_294'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_294 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_295'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_295 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_296'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_296 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_297'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_297 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_298'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_298 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_299'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_299 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_300'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_300 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_301'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_301 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_302'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_302 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_303'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_303 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_304'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_304 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_305'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_305 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_issue__mutmut['xǁAFIPArgentinaConnectorǁ_issue__mutmut_306'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_issue__mutmut_306 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_12'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_13'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_14'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_15'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_16'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_17'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_18'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_19'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_20'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_21'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_22'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_23'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_24'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_25'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_26'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_27'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_28'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_29'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_30'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_31'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_32'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_33'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_34'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_35'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_36'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_37'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_38'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_39'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_40'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_41'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_42'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_43'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_44'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_45'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_45 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_46'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_46 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_47'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_47 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_48'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_48 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_49'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_49 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_50'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_50 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_51'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_51 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_52'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_52 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_53'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_53 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_54'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_54 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_55'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_55 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_56'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_56 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_57'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_57 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_58'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_58 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_59'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_59 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_60'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_60 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_61'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_61 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_62'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_62 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_63'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_63 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_64'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_64 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_65'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_65 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_66'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_66 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_67'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_67 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_68'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_68 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_69'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_69 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_70'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_70 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_71'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_71 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_72'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_72 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_73'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_73 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_74'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_74 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_75'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_75 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_76'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_76 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_77'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_77 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_78'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_78 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_79'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_79 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_80'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_80 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_81'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_81 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_82'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_82 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_83'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_83 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_84'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_84 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_85'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_85 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_86'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_86 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_87'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_87 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_88'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_88 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_89'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_89 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut['xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_90'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_build_xml_repr__mutmut_90 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁ_cancel__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_cancel__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_cancel__mutmut['xǁAFIPArgentinaConnectorǁ_cancel__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_cancel__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_cancel__mutmut['xǁAFIPArgentinaConnectorǁ_cancel__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_cancel__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_cancel__mutmut['xǁAFIPArgentinaConnectorǁ_cancel__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_cancel__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_cancel__mutmut['xǁAFIPArgentinaConnectorǁ_cancel__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_cancel__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_cancel__mutmut['xǁAFIPArgentinaConnectorǁ_cancel__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_cancel__mutmut_5 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_12'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_13'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_14'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_15'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_16'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_17'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_18'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_19'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_20'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_21'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_22'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_23'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_24'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_25'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_26'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_27'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_27 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_28'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_28 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_29'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_29 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_30'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_30 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_31'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_31 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_32'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_32 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_33'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_33 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_34'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_34 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_35'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_35 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_36'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_36 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_37'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_37 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_38'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_38 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_39'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_39 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_40'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_40 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_41'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_41 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_42'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_42 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_43'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_43 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_44'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_44 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_verify__mutmut['xǁAFIPArgentinaConnectorǁ_verify__mutmut_45'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_verify__mutmut_45 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut['xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut['xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut['xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut['xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut['xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut['xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut['xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut['xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_pdf__mutmut_8 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_credit_note__mutmut_11 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut['xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_create_debit_note__mutmut_11 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_12'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_13'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_14'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_15'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_16'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_17'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_18'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_19'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_20'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_21'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_22'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_23'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_24'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_25'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut['xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_26'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_check_taxpayer_status__mutmut_26 # type: ignore # mutmut generated

mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['_mutmut_orig'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_orig # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_1'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_1 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_2'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_2 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_3'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_3 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_4'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_4 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_5'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_5 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_6'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_6 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_7'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_7 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_8'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_8 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_9'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_9 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_10'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_10 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_11'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_11 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_12'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_12 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_13'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_13 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_14'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_14 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_15'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_15 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_16'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_16 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_17'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_17 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_18'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_18 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_19'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_19 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_20'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_20 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_21'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_21 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_22'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_22 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_23'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_23 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_24'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_24 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_25'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_25 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_26'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_26 # type: ignore # mutmut generated
mutants_xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut['xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_27'] = AFIPArgentinaConnector.xǁAFIPArgentinaConnectorǁ_get_last_invoice_number__mutmut_27 # type: ignore # mutmut generated


AFIP_ARGENTINA_SCHEMA = ConnectorSchema(
    name="afip_argentina",
    version="2.0.0",
    description="Emite facturas electrónicas AFIP (WSAA+wsfev1) con CMS+SOAP+mTLS real",
    category="latam",
    icon="file-text",
    author="Zenic-Flujo",
    actions=[
        ActionDefinition(name="issue", description="Emite factura electrónica (FECAESolicitar)", category="write"),
        ActionDefinition(name="cancel", description="Emite nota de crédito (cancelación AFIP)", category="write"),
        ActionDefinition(name="verify", description="Verifica factura por CAE", category="read"),
        ActionDefinition(name="get_pdf", description="Genera PDF (local, não nativo AFIP)", category="read"),
        ActionDefinition(name="check_taxpayer_status", description="Estado del contribuyente", category="read"),
        ActionDefinition(name="get_last_invoice_number", description="Último número autorizado", category="read"),
    ],
    auth_requirements=[
        AuthRequirement(
            auth_type="mtls",
            required_fields=["cuit", "cert_path", "key_path"],
            optional_fields=["environment"],
            description="CUIT + Certificado digital AFIP (PEM) + clave privada — mTLS obrigatório",
        )
    ],
)
